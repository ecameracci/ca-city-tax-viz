# City source manifests

These JSON files summarize defensible public data sources for future city adapters. They are research/manifests, not raw data downloads.

A manifest may list revenue/value as unavailable or semantically different. That should disable affected lenses rather than filling the app with proxy numbers that look Edmonton-equivalent.
