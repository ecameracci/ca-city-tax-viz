# Decisions Index

Append-only, one line per locked decision: when, what, the one-sentence why
(including what was rejected), and where the full reasoning lives. This file
**duplicates no rationale** — it exists so a contributor can find where a
decision is argued without reading every doc. When a decision locks, add a
line; when one is superseded, strike it (`~~...~~`) and add the successor —
don't delete history.

Status of anything still open lives in the owning SPEC doc, not here.

---

## Analysis unit & denominators

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-05 (Phase 1) | **Neighbourhood, not parcel, as the unit** — parcel boundary polygons are licensed (ADP/AltaLIS), not open data; roll points + boundary polygons are. | `docs/PARCEL_LEVEL_OPPORTUNITIES.md`, `data/DATA.md` |
| 2026-05 (Phase 1) | **Boundary-polygon acres as the default denominator**, EPSG:3400 projected before any area calc — one consistent acre across every metric. | `docs/ARCHITECTURE.md` Key Decisions |
| 2026-05 (Phase 1) | **Condo records included as-is** — many units on one land record inflating $/acre reflects real density, not an error. | `docs/ARCHITECTURE.md` Key Decisions |
| 2026-07-05 | **Repeat-aware lot dedupe heuristic** for any lot-area sum (condo rows repeat the parent lot; majority-null multi-unit points are ineligible, excluded + reported). | `docs/FINDINGS_lot_dedupe.md` |
| 2026-07-08 | **Ground-acre stays the default; lot-acre ships as an editorial alternative** ("per developable acre" toggle, suppressed below 15% parcel land) — ground-acre is cardinality-robust, not Urban3 lineage. | `docs/FINDINGS_denominator_cardinality.md` |

## Revenue methodology

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-06 | **Municipal-only tax**, not education requisition. | `docs/SPEC_revenue.md` |
| 2026-06 | **Keep both metrics** (assessed value/acre AND revenue/acre, web toggle) — the gap between them IS the class-differential mill rates; hiding one is less transparent. | `docs/SPEC_revenue.md` |
| 2026-06 | ~~Tax-exempt: flag via `is_exempt` + include~~ **Superseded 2026-06-29**: exempt institutional land is absent from the taxable roll entirely (the flag catches 3 parcels), so exempt-heavy hoods understate — recorded as a limitation, separation done by the zoning layer instead. | `docs/SPEC_revenue.md`, `docs/FINDINGS_exempt_institutional.md` |
| 2026-06-29 | **Set-aside = never-land + not-yet-land ≥ 0.90 of area** (zoning overlay; grey, off the colour scale) — the near-zero spike is undeveloped land, not exempt land. Zoning is a refreshed input so developing land graduates off the list automatically. | `docs/SPEC_revenue.md` update, `docs/FINDINGS_revenue_scale.md` |
| 2026-06-29 | **Explicit hand-assigned code→category dictionaries, never keyword/prefix heuristics** (zone codes; later road classes and runoff coefficients follow the same rule) — place-names like "Energy & Technology *Park*" break fuzzy matching. Unknown codes warn loudly. | `docs/FINDINGS_revenue_scale.md`, `src/load_zoning.py` |

## Services & utility lenses

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-07-01 | **Roads metric = City-maintained collector + local centreline metres only** — arterials computed but excluded (shared infrastructure), alleys/railway excluded at the row filter. | `docs/SPEC_services.md` |
| 2026-07-05 | **Stormwater = the Bylaw 20865 charge modeled from open data** (A × I × R, I = 1.0, SIAP credits out of scope) — always labeled MODELED, never billed-accuracy. | `docs/SPEC_utilities.md` Lens 1 |
| 2026-07-05 | **Fire = demand only** (dispatches/acre/yr): no on-scene-arrival timestamp exists in the data, so a response-time/coverage claim is NOT buildable — don't oversell. Medical ~57% share is a caveat, not a filter; window pinned to last 3 full years (auto-rolling could dilute with a partial year). | `docs/SPEC_services.md` "Fire lens" (all four locked decisions) |
| 2026-07-06 | **Water lens: residential scope only; tariff vintage pinned independently of the roll year** (a forward-looking modeled bill, unlike mill rates which must match the roll). | `docs/SPEC_utilities.md` Lens 2 |
| 2026-07-07 | **Franchise lens: columns only, no display layer** — modeled electricity/gas revenue is collinear with dwelling count, so a map of it would just be a dwelling map; value is citywide totals + per-hood attribution. Shares ONE dwelling model with the water lens. | `docs/SPEC_utilities.md` Lens 3 |
| 2026-07-02/03 | **Ratio metric (revenue per road metre) is derived client-side in the browser** — the acres cancel, so no pipeline stage computes it; scale anchors computed at page load so they track refreshes. | `docs/ARCHITECTURE.md` data flow, FINDINGS §6.4 |
| 2026-07-10 | **"Total services" stays PER-SERVICE (Ratio-view denominator picker: road metre \| fire event)** — modeled EPCOR dollars (storm/water) never sit under the levy (money-flow honesty); a combined denominator waits on published city unit costs (V2, rejected for now as a new cost model without sources in hand). | `docs/SPEC_utilities.md` decision 3, `docs/FINDINGS_revenue_scale.md` §6.7 |
| 2026-07-11 | **Transit = scheduled supply only (mean-weekday GTFS stop-events/acre; bus + LRT combined, per-mode internal)** — no stop-level ridership exists (citywide-monthly only), so never present as usage; on-demand zones absent from GTFS (limitation); full lens incl. web display, AMENDING the 2026-07-09 release-scope lock that kept transit out (Peter's call). | `docs/SPEC_services.md` "Transit lens" (both locked decisions) |
| 2026-07-11 | **LRT track lines added as a transit context layer (Capital/Metro/Valley); HER heritage streetcar EXCLUDED** — the track lines match the measured ETS LRT service only; the volunteer-run High Level Bridge streetcar is not in the GTFS routes we count, so drawing it would show unmeasured track. Context layer, not part of the metric. | `docs/SPEC_services.md` "Transit lens" Display; DATA.md §9 |

## Development & infill lens

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-07-12 | **Building-activity lens = neighbourhood choropleth** (new dwelling units/acre, `24uj-dj8v` permits), not a per-permit point layer — reuses the existing choropleth render; lat/long retained for a possible future point/heatmap variant. | `docs/SPEC_development.md` Lens A |
| 2026-07-12 | **Infill lens = suitability × activity *mismatch*, shown both ways** (suitable-but-quiet AND less-suitable-but-getting-built) — a signed diverging metric, not a static suitability score; the mismatch is the story. Base-suitability definition still OPEN. | `docs/SPEC_development.md` Lens B |
| 2026-07-12 | **Combined lens cost side = modeled city service cost** (reuse service columns / V2 unit costs), NOT permit `construction_value` — money-flow honesty (same rule as 2026-07-10); construction_value reserved for a possible separate private-capital view. | `docs/SPEC_development.md` Lens C |
| 2026-07-12 | **Lens A set-aside handling = full override, coloured** — on the Development view the set-aside mask greys nothing; every hood is coloured by its activity value (greenfield included), set-aside status stays in the tooltip. Semantically correct (undeveloped land IS where units land) + future-proofs newly-platted greenfield; empirically low-impact now (only 6 hoods / 43 units of 59,696 are set-aside-with-activity — the growth hoods sit below the 0.90 threshold). | `docs/SPEC_development.md` Lens A |
| 2026-07-12 | **Lens A activity window = last 5 full calendar years (2021–2025), pinned + summed** — Σ `units_added` over the window, `PERMIT_YEARS` in `main.py`; drift-guarded (a window year with zero permits hard-errors), not auto-rolling-with-partial-year (fire-lens precedent 2026-07-05). Bump each January. | `docs/SPEC_development.md` Lens A |
| 2026-07-13 | **Lens A window toggle = 5yr base + 3yr recent** (Peter) — a second pinned window (`PERMIT_YEARS_RECENT` = 2023–2025, the "current activity" cut) ships alongside the 5yr base as a `#devwindow` web picker; emits `_3yr`-suffixed columns, base stays unsuffixed, gated on the `_3yr` columns being present. Both windows apply to both metrics (units/permits) and bump together each January. | `docs/SPEC_development.md` "Lens A polish" |
| 2026-07-13 | **Lens B suitability proxy = built floor-area ratio (FAR)** (Peter) — the "underused / room to add" first-cut proxy: `far` = Σ building floor area (`Total Gross Area`) ÷ deduped lot area per hood, computed in `build_hood_lot_acres` on the same eligible-point dedupe as the lot-acre denominator. Low FAR = underused. Chosen over median-`year_built` (re-derives the greenfield/median-age story) and value/lot-acre (conflates with land value); the assessment roll has no land/improvement split so the classic teardown I/L ratio is unavailable. Pipeline emits raw `far`; the web computes the diverging mismatch `z(−far) − z(activity)` live. Known first-cut caveat: raw low-FAR includes river-valley/greenfield fringe (park/undeveloped, not infill opportunities) — the web view owns that exclusion. | `docs/SPEC_development.md` Lens B |
| 2026-07-13 | **Lens B `Infill` view = diverging `z(suitability)−z(activity)`, set-aside excluded** — one signed diverging map (teal = suitable-but-quiet opportunity, orange = building-where-less-suitable pressure), both z-terms standardised over the non-set-aside population (48 River Valley/Henday greenfield hoods dropped, 358 kept), symmetric p95 clamp. `is_residential` rejected as the exclusion (drops DOWNTOWN). Residual caveat: low FAR conflates mature-underused with new-empty-suburb, so the opportunity end over-flags developing/edge hoods (EVERGREEN, KINGLET GARDENS) — the pressure end (DOWNTOWN) is the trustworthy read; framed as relative/exploratory. | `docs/SPEC_development.md` Lens B |
| 2026-07-14 | **REOPENED — Infill *clamp* clause → per-arm scaling** (Peter). Reopens ONLY the "symmetric p95 clamp" clause of the 2026-07-13 Infill line above; the rest of that decision (diverging `z(suit)−z(act)`, set-aside exclusion, `is_residential`-rejected, asymmetric residential gate) STANDS. The S48 Fable decision audit found the score is structurally asymmetric — the suitability term is capped at +0.97 (far ≥ 0) while the activity term is unbounded — so a single symmetric clamp (3.04 on shipped data) makes the teal/opportunity arm unable to exceed ~50% saturation (0 teal vs 18 orange saturations; the legend's full-teal endpoint is unreachable by construction). Fix = clamp each arm at its OWN p95 (teal at p95 of positive scores, orange at p95 of \|negative\|) + verdict thresholds expressed in per-arm/`t` space. Implementation handed to a Fable session. | `docs/SPEC_development.md` Lens B; `docs/FABLE_infill_perarm_scaling.md` |
| 2026-07-14 | **Infill colour scaling = PER-ARM p95 clamps + verdict cut at `t = ±0.4`** — closes the REOPENED line above. Each arm clamps at its own p95 (`clampPos` = p95 of positive scores ≈ 1.49, `clampNeg` = p95 of \|negative\| ≈ 4.34 on shipped units×5yr), so the teal endpoint is reachable; tooltip verdict branches on clamped `t` at ±0.4 (median hood t ≈ 0.29 sits clear of the cut, vs on the old raw +0.5 line). Web-only (`infillStats`/`infillT`); pressure ordering unchanged (DOWNTOWN still #1); `verify-infill.js` 44/44. | `docs/SPEC_development.md` Lens B |
| 2026-07-15 | **Development fine-grain = a layers-panel "Detail" toggle inside the existing view, NOT a new view button** (Peter: "add them as a layer switch this time... I may want to move the others to this style later") — the deliberate pattern probe for migrating other lenses. Toggling swaps the choropleth for the Glass composition: uniformly neutral hood plane (the Lens A no-grey set-aside override stands; hover stays on the hood) under 100 m `GridCellLayer` spikes of geocoded permits, linear height / sqrt colour, driven by the existing units|permits × 5yr|3yr pickers. Cells hold GEOCODED permits only — the newest permits lag geocoding (~21% of 5yr units at build time) — with per-window coverage written into `dev_grid.json` and disclosed in the blurb; positions are never faked with hood centroids. | `docs/SPEC_development.md` "Lens A detail grid" |

## Growth infrastructure financing ("debt lens")

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-07-14 | **NO debt-per-parcel/neighbourhood map — build "growth infrastructure financing transparency" instead, as two clearly-labelled components**: (1) a spatial growth-area financing panel (fire-hall off-site levy catchments, IIMP developer-vs-City split, Blatchford contrast, sanitary-trunk callout) and (2) a non-spatial citywide debt context annotation (FIR debt trend + peer benchmark, labelled citywide). Municipal debt is issued citywide, never tagged to parcels/subdivisions in public data — any spatial debt number would be fabricated, not sourced. The framing ("financing transparency", not "debt attribution") is the load-bearing methodological claim and must be explicit in UI copy. Out of scope: spatial allocation of the $4.6B citywide total; Local Improvement levies (possible future phase). | `docs/fable_brief_debt_lens.md` |
| 2026-07-15 | **Off-site levy catchments (D0) are APPROXIMATED as neighbourhood unions, not traced** — the only published boundaries are the Schedule A raster in Bylaw 19340 (no GIS layer anywhere). Where the far-greenfield grid is one giant hood per corner, catchments that share a hood are **merged to the grid** (EETP + Northeast EETP → "EETP"; Horse Hill + Northeast Horse Hill → "Horse Hill"), 12 → 10 units. Every feature is labelled "approximated to neighbourhood boundaries; boundaries advisory". | `docs/FINDINGS_offsite_levy_catchments.md`, `scripts/build_levy_catchments.py` |

## Display honesty

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-06-25 | **Linear elevation, no power curve** — a super-linear transform exaggerates (2× reads ~4× at k=2), unacceptable for scrutinised civic numbers; "Downtown's spike IS the story". Spike emphasis remains OPEN, held. | `docs/ARCHITECTURE.md` Phase 2 visual tuning |
| 2026-06-25 | **Display-geometry-only transforms: 45 m setback then 10 m simplify, in that order** — order cuts the served file 4× (9,229 vs 38,607 vertices); all metrics computed from true full-resolution area upstream, so display geometry can never change a number. | `docs/ARCHITECTURE.md`, `docs/PERFORMANCE.md` |
| Phase 2 design | **MapLibre + deck.gl directly, not Kepler.gl** (a UI wrapper — less control); **extruded real neighbourhood polygons, not H3 hexes** (the shapes mean something to Edmonton readers); **no basemap v1**. | `docs/ARCHITECTURE.md` Phase 2 design decisions |

## Controls & lens grouping ("organize the lenses" pass)

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-07-22 | **Glass folds into Money as a render-mode, not a top-level view** (`#views` 7→6) — Glass already reuses Money's `#toggle` metric, `#coloradj` sqrt scaling, and denominator (it IS "Money, translucent, grid-denominated"), so it becomes a grid/translucent toggle inside Money like Development's 100 m detail. Resolves combos C (`#toggle` live-but-inert), D (two denominators), G (view-vs-render-mode) at once. Public tag unchanged (Money is public). RECORDED not yet built — code edit deferred to the single "build once" pass after the full regroup is settled. | `docs/CONTROLS_MATRIX.md` §5.G, §7 |
| 2026-07-23 | **Infill folds into Development as a full-only mode, not a top-level view** (`#views` 6→5) — already shares `#devmetric`/`#devwindow`. UNLIKE Glass→Money, the fold crosses a build-tag boundary: Development is public, Infill is full-only, so the Infill toggle is `BUILD`-flag-gated to appear ONLY in `/full/` (public Development = activity only; specialist = activity + Infill). The two-build per-control tag mechanism does exactly this. RECORDED not yet built — deferred to the "build once" pass. | `docs/CONTROLS_MATRIX.md` §7, §2 |
| 2026-07-23 | **Palette picker (`#palette`) moves off the top chrome into an accessibility menu — not deleted** — Cividis is the colour-vision-deficiency-safe ramp, so removing the picker outright is an a11y regression; instead it's tucked behind an accessibility affordance. Default stays Inferno (applies without opening the menu). Declutters the always-visible chrome by one T3 pod; the a11y menu becomes the future home for other display-aid toggles. RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7 |
| 2026-07-23 | **`Labels` toggle also moves into the accessibility menu** — a display/legibility aid, same home as the palette picker; collapses `#lens` to a single `Residential only` toggle. RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7 |
| 2026-07-23 | **Control stack reorders to tier order: View → Variant → Presentation** — fixes the scramble where the T1 view switcher sat 5th below three presentation modifiers. Top→bottom: ① `#views`; ② per-view variants (`#layers` + `#toggle`); ③ presentation (`#coloradj` + `Residential only`); ④ accessibility button out of flow. Makes `#toggle` and `#coloradj` Money-scoped (combo C resolved — no more live-but-inert metric picker over other views). RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7, §5.A |
| 2026-07-23 | **`Residential only` fade lens renamed → "Highlight residential"** — resolves the combo-E name clash with the `Residential $` Money metric (both now Money-scoped, adjacent). Label-only, no mechanics/scope change; the `Residential $` metric keeps its name (`$` disambiguates, pairs with `Non-res $`). RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7, §5.E |
| 2026-07-23 | **Development panel: grid checkbox + spike picker → one 3-way "Detail" selector** (Neighbourhood / 100 m grid — activity / Stock age) — replaces progressive-disclosure-by-checkbox with sibling radio options; Stock age hides Metric+Window as an explicit choice, not a surprise morph. Resolves combos A (Year built no longer buried) + B (morph now chosen). Phone-motivated (nested checkbox reveal is a weak small-screen tap target) — structure-before-mobile so the CSS pass inherits a clean layout. RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7, §5.A/B; `docs/MOBILE_USABILITY.md` |
| 2026-07-23 | **Industrial (`#devmetric`) tagged full-only** — pulled from public Development (joins Infill as a `/full/`-only Dev extra). It's choropleth-only, so keeping it public would leave the new 3-way Detail selector with dead options; removing it makes public Development airtight (units + permits, both grid-capable). `/full/` keeps it. Resolves combo F. RECORDED not yet built. | `docs/CONTROLS_MATRIX.md` §7, §5.F |
| 2026-07-23 | **All 8 grouping decisions ABOVE are now BUILT** (branch `regroup-build-s65`, one reflow) — plus the `BUILD` mode flag (`public\|full`, `?build=public` local override; full-only controls gate on it). Realization notes: the top stack became a `#controls` flex column (tier order via CSS `order:`, not DOM order); Glass and Infill stayed *internal* views (`state.view`) surfaced as Money/Development mode toggles, so all their layer/blur/legend machinery is reused untouched; the a11y menu is a "Display" popover. Verify + shot suites updated and green in both builds. **NOT on master** — merging is gated on the two-build deploy plumbing (which must set `BUILD=public` for the root copy), else the public root ships the specialist controls. | `web/index.html`, `docs/CONTROLS_MATRIX.md` §7, `TODO.md` |
| 2026-07-24 | **Uses view tagged full-only (provisional — "for now")** — the Uses `#views` button is now `BUILD`-flag-gated (`\|\| !FULL_BUILD`, same idiom as Industrial), so **public `#views` = Money · Services · Ratio · Development** (4) and `/full/` keeps all 5. Peter's call: trims the public surface while the view-picker's visual weight/placement is reconsidered (see the top-bar note in `MOBILE_USABILITY.md`). Marked provisional — Uses may return to public. Data columns unchanged (uses composition still ships in the base data; only the button is hidden). | `web/index.html` uses-button guard, `docs/CONTROLS_MATRIX.md` §2 |
| 2026-07-24 | **Camera reset is two bottom-left framing presets, not a compass** — Center 3D → tilted default (`HOME`), Center 2D → straight-down **north-up** plan (`HOME_2D`, bearing 0). Both recenter the *whole* camera (position/zoom/tilt/rotation), framing the whole city; only tilt/rotation differ. 2D snaps north-up because flattening at the −18° tilt-era bearing reads as a skew. `HOME`/`HOME_2D` are the single source for the map constructor + both buttons. Shipped first as a single "Flip 2D/3D" toggle (kept bearing → skew), then split. Placement bottom-left above the legend (Peter's call). A `NavigationControl` compass (in-place bearing reset + rotation discoverability) remains open. | `web/index.html` `#viewbtns`/`HOME`/`HOME_2D`, `docs/UI.md` "Camera framing buttons" |
| 2026-07-24 | **Functional `verify-*.js` can't catch clickability — a dedicated hit-test guard does** — the suite actuates controls with `page.$eval(sel, b => b.click())` (a JS `.click()` that bypasses `pointer-events`/z-order), so a control that is visible-but-unclickable passes them all. This let the S65 refactor ship `#layers` (Detail/Denominator/dev/services) unclickable to the live site (dropped from the `pointer-events:auto` flip list). `verify-controls-clickable.js` real-hit-tests every visible control per view; **run it after any control-chrome/CSS refactor.** | `tools/profiling/verify-controls-clickable.js`, `docs/UI.md` "Control clickability regression" |
| 2026-07-25 | **Glass (Money → 100 m grid) drops the prism-opacity slider; translucency is fixed at 60%** — Peter: "no longer necessary in this lens". 60% *is* the translucency that makes the plane-under-spikes composition legible and nobody tuned it. Safe because `applyView` re-applies `VIEWS.glass.opacity` on every entry, so hiding the control can't strand a stale value (e.g. Ratio's 5%); `state.prismOpacity` still drives the grid layer, so the render is unchanged. The "Money plane" header hides with it, and the blurb's "The slider sets spike opacity." sentence was removed — prose must not point at a control that isn't there. Ratio (5%), Uses' residential prisms (35%) and Development's activity grid keep theirs. | `web/index.html` `applyView` (`prismSlider`) + `GLASS_BLURBS`, `tools/profiling/verify-glass-no-slider.js`, `docs/UI.md` "Spike opacity" |
| 2026-07-25 | **Control type size encodes tier — `#views` is the biggest thing on screen** — Tier 1 (`#views`) 11.5px → **14px/9px 18px**, above the Tier 2 metric bar (12.5px) and the Tier 3 modifiers (11.5px); it previously tied for smallest, which is why it under-read as chrome rather than as the primary switch. Phones override back to 12.5px/7px 11px — at 14px the four public views don't fit one 390px row, and wrapping the primary control costs more than the size buys. The Options header goes 11.5px → 13px and gains `min-width:180px`, which widens the FOLDED pod **leftward** (right-anchored, so extra width grows left) — chosen over shifting the pod left, which would break the flush right edge with the bars above when the panel is open. | `web/index.html` `#views button`/`#opt-fold` + `@media` seam, `docs/UI.md` "Control hierarchy sizing", `docs/MOBILE_USABILITY.md` |
| 2026-07-25 | **A control that doesn't apply HIDES rather than greying out — starting with the residential lens** — greyed `#4a4a5e` on a dark panel reads as *broken*, not *unavailable*; it was reported from the live site as "the highlight residential button doesn't work". Worse, Money's **100 m grid** (Glass) disables the lens *without leaving the Money view*, so it looked dead in place. `#lens` now `display:none` outside Money/Ratio (allowlist, not denylist — a new view hides the control by default); `disabled` stays in sync so it can't be actuated while hidden; lens state persists across the hide/show round-trip. **`#coloradj` still greys out — whether it follows is open.** | `web/index.html` `applyView` (`lensApplies`), `tools/profiling/verify-lens-visibility.js`, `docs/CONTROLS_MATRIX.md` §3 |
| 2026-07-25 | **The Colour-scaling button's LABEL is its state readout — the separate caption is gone** — "Colour: sqrt scaling" / "Colour: linear" replaces a `#coloradj-state` span that said the same thing at 2.5× the width ("On — colour spread across distribution"). It made the pod stick out: `#coloradj` 417px → **169px**, `#optpanel` 645px → 398px, and the width no longer jitters as you toggle. Long form kept in the button `title`; the map blurb already spells out the linear case. Dropped at every breakpoint, so the phone-only rule that hid the caption went too. | `web/index.html` `#coloradj-btn`/`syncColorAdjust`, `docs/UI.md` "Colour Adjustment toggle" |
| 2026-07-25 | **Compass is a custom 3-button row (arrows + needle), NOT `NavigationControl`** — the standing proposal was the stock MapLibre control; rejected because it is light-themed against our dark chrome, offers a drag-to-rotate target rather than explicit *arrow buttons*, and bundles zoom +/− we don't need (scroll/pinch cover zoom). Ours: two arrows that step the bearing to the next **30° detent** in the pressed direction (the arrow you press = the way the map turns), plus a needle that snaps north **IN PLACE** (bearing only — the deliberate contrast with Center 2D, which reframes the whole city). Closes the long-standing "rotation is undiscoverable" gap. Icons are inline SVG because the headless font lacks the curved-arrow codepoints. | `web/index.html` `#compass`/`ROT_STEP`/`ROT_MS`, `tools/profiling/verify-compass.js`, `docs/UI.md` "Compass with rotation arrows" |

## Deployment & operations

| When | Decision | Full reasoning |
|------|----------|----------------|
| 2026-06-28 | **Push, not pull; static, not served** — a weekly GitHub Action regenerates committed data and deploys static Pages; no server, no database, no runtime backend. Data changes ~annually; traffic is a CDN problem, not a compute problem. | `docs/SPEC_deployment.md` (incl. the rejected dedicated-server alternative) |
| 2026-07-01 | **Git is the change detector; `status.json` is heartbeat + provenance + banner** — re-run on schedule, commit only if output changed, publish the vintage. | `docs/SPEC_deployment.md` "Decisions settled" |
| 2026-07-01 | **Mill rates are a manual, reviewed input — never auto-fetched**; a roll-year/rate mismatch puts CI in a HOLD (keep serving committed data + banner), never a regenerate. | `docs/SPEC_deployment.md` "Year alignment", `scripts/check_year_alignment.py` |
| 2026-07-06 | **Year pins are manual** (`ASSESSMENT_YEAR`, `FIRE_YEARS` in `main.py`) — anything auto-rolling can go wrong silently; see the RUNBOOK's January checklist. | `main.py` comments, `docs/RUNBOOK.md` |
| 2026-07-09 | **Public release ships on the existing Pages site — no new hosting or engineering** pre-release. | `docs/PLAN_public_release.md` |
| 2026-07-13 | **Infill Lens B opportunity-end cleanup = an ASYMMETRIC RESIDENTIAL GATE, not a maturity gate** — a prototype showed the opportunity pollution is structurally-low-FAR *non-residential* land (industrial/fringe, all decades), not new suburbs, so a median-`year_built` gate fails. Non-residential hoods are barred from the teal opportunity end (grey) but kept on the orange pressure end + in the z population (keeps DOWNTOWN). Web-only; no new pipeline column. | `docs/SPEC_development.md` Lens B |
| 2026-07-16 | **Residential revenue = RESIDENTIAL + OTHER RESIDENTIAL class dollars** (MA DERELICT excluded — billed at the punitive Non-Res rate on purpose); **displayed as a third Money metric** ("Residential $", `res_revenue_per_acre`) plus a residential-share line in every Money tooltip. A numerator decomposition — deliberately distinct from the `is_residential` zoned-area fade lens; the blurb must carry the "subset of Revenue, not all of what the land pays" line. | `data/DATA.md` §4 "Residential-revenue decomposition", `docs/UI.md` "Residential revenue metric" |
| 2026-07-17 | **Regional comparison lens PARKED on St. Albert licensing** — the LandScape REST service is a lookup-tool backend, not a catalogued open dataset; treat it as not licensed for bulk reuse and **never commit St. Albert per-parcel data to the public repo** until the City confirms otherwise. Strathcona's licensing is clean (OGL-Alberta) but its data is weak (residential-only centroids, dedup landmine). | `docs/SPIKE_regional_lens.md` (Licensing + Un-park conditions) |
| 2026-07-17 | **Infill verdict copy is DESCRIPTIVE, and suite conversions are DISCLOSED, not added** — tooltip verdicts drop recommendation grammar ("infill opportunity" → "Room to add, quiet lately"); work types (07)/(08)/(09) stay out of the activity term (0.9% of units, zero verdict flips — fork option i) with a blurb disclosure clause; D2 denominator mismatch + D5 z-compression recorded as SPEC caveats. Closes the S48/S56 audit copy debt. | `docs/SPEC_development.md` Lens B "Round-2 audit caveats" |
| 2026-07-17 | ~~**Stock-age spikes live in the Development view's detail grid as a "Spikes" picker (New homes \| Year built), riding on `value_grid.json`** — median `year_built` per 100 m cell (`median_year_built`, appended last; null where no known year, never year-0; row-weighted median moots the condo dup regimes); height = recency + colour both LINEAR in year (interval scale — no sqrt), ramp-top yellow = newest, colour low anchor p2.5; Metric/Window pickers hide while age is up. Display only — distinct from the rejected Lens B median-age gate (2026-07-13).~~ **WITHDRAWN 2026-07-27 — see the row below. The option is gone from the UI; the `median_year_built` column is still produced and still in `value_grid.json`.** | `docs/SPEC_development.md` "Stock-age spikes", `data/DATA.md` §4 |
| 2026-07-21 | **Stock-age spike HEIGHTS baseline at the p2.5 anchor (shared with colour), not the true oldest cell** — the true min is a lone pre-war outlier (2026: 1904, p1 already 1944), so a true-min baseline floated the whole stock ~40 yr off the floor and every spike read uniformly tall (median cell at 68% of peak; new-build only ~1.4× a 1970s hood). Height + colour now share one p2.5 floor; median drops to ~48%, new-build towers. The TOP is never clamped (heights-never-percentile-*clamped* rule holds) — only the floor moves up, oldest ~2.5% sit flat. NOT a power curve (2026-06-25 no-power decision stands). | `docs/FINDINGS_stock_age_spike_scaling.md`, `web/index.html` `ageGridLayer` |
| 2026-07-21 | **Development lens A gains a long "Since 2009" activity window** — a third `#devwindow` option (2009–2025) alongside 5yr/3yr, the cumulative "density added over the era" cut that reproduces the inspiration lens's 2009–2023 "homes added" map. ANCHORED (start pinned at `PERMIT_START_YEAR = 2009`, end DERIVED from `PERMIT_YEARS[-1]` so the January bump extends it — no separate pin, partial-year safety inherited). `_long` columns for all three metrics. **[revised 2026-07-22]** a FIRST-CLASS window: it drives both the hood choropleth and its own 100 m detail grid. The initial cut made it choropleth-only on the belief early-year permit geocoding was sparse; the data disproved it (2009–2023 at 95–98% geocoded, the lag is on the NEWEST permits — 2025 ~72% — so the long grid is the best-covered of the three at 84%). | `data/DATA.md` §9 "Long window", `docs/SPEC_development.md` "Activity window" |
| 2026-07-18 | **Non-residential revenue = the Non-Residential-rate slices** (COMMERCIAL + MA DERELICT + DESIGNATED IND PROPERTIES, via `NONRES_RATE_LABELS`; farmland is its own class, exempt is $0 → identity `levy == res + nonres + farmland`); **displayed as a fourth Money metric ("Non-res $", `nonres_revenue_per_acre`)** + Glass grid cells, mirroring Residential $ (2026-07-16). The honest **class-complete** cut — there is NO industrial-vs-commercial split in the assessment roll, so an industrial-only *dollar* cut would need a zoning join (a possible later refinement, not this). SPEC_industrial.md A1. | `data/DATA.md` §4 "Non-residential decomposition", `docs/UI.md` "Non-residential revenue metric" |
| 2026-07-22 | **Two builds from one repo/Pages site — public (curated subset, root) + specialist (full, unlisted `/full/`)** — single source (`web/index.html` + a `BUILD` mode flag), two generated copies at deploy time, no fork. Unlisted, NOT access-controlled (repo is already public → nothing secret; stays on free Pages, still "no new hosting"). The *mechanism* is locked; the *content* split (which lens is public vs full) is COUPLED to the control-regrouping work — each control carries a `public\|full` tag decided in the same `CONTROLS_MATRIX.md` pass, not separately. Provisional split: public = Money/Services/Ratio/Uses/Glass/Development, full-only = Infill + deep data-detail. | `docs/PLAN_public_release.md` §2a, `docs/CONTROLS_MATRIX.md` |
| 2026-07-22 | **Deploy is split from data-refresh — a push-triggered code-only deploy (`.github/workflows/deploy.yml`)** — a push touching site code (`web/**`, excluding `web/data/**`) re-uploads the committed `web/` tree to Pages with NO download/regen (seconds, not the 30-min pipeline); `refresh.yml` still owns the weekly data path. The committed `web/data/*.geojson` is the enabler — a code push needs no data run. Only a lens needing a *brand-new* dataset still requires a data run; selective/partial regen (which datasets to fetch) is explicitly deferred. Shared `concurrency: refresh-map-data` so code + data deploys never race. | `.github/workflows/deploy.yml`, `docs/SPEC_deployment.md` "Two deploy paths" |
| 2026-07-18 | **Industrial construction shown via PERMIT VELOCITY, not assessment dollars** — the roll can't be decomposed to industrial (no split), so the industrial signal is new 400-series `building_type` permits per acre (count only; `ind_permits_per_acre`), a third `#devmetric` "Industrial" **Development-view choropleth ONLY**: no detail-grid cells, and NOT an Infill activity (an industrial permit isn't residential infill — entering Infill resets the metric + hides the button). `INDUSTRIAL_BUILDING_TYPES` enumerated by full string (codes duplicate: Parkade 490 ≠ industrial). SPEC_industrial.md A3. | `data/DATA.md` §10 "Industrial permit velocity", `docs/SPEC_industrial.md` A3 |
| 2026-07-25 | ~~**The data credit is the button LABEL, not content behind a click**~~ **SUPERSEDED the same day — see the "Data & Methods" row below.** The label was 294px wide and sat on top of the legend at phone widths. — the Sources pod's resting state reads `Data: City of Edmonton Open Data · <year>`; the popover holds only the caveats/links. Everything the map renders is City of Edmonton open data under the **Open Government Licence**, which asks for visible attribution, and a link the reader has to open first attributes nothing. Same "label is the readout" idiom as `#coloradj`. Closes P1.2 (the live map had no link to repo, sources or methodology). | `docs/UI.md` "Sources & attribution pod", `docs/PLAN_public_release.md` §4 P1 #2 |
| 2026-07-25 | **On-screen data vintages read from `status.json` at runtime — never a literal in the markup** — `data_year`/`rate_year`/`zoning_year`/`generated` come from the manifest the pipeline already writes (and the map already fetches for the banner). A hardcoded year is wrong every January and nothing fails loudly when it is (`RUNBOOK.md` year-roll). Each line degrades independently: no manifest → vintages blank, credit still reads. `verify-about.js` asserts the served HTML contains no year literal in the pod. | `web/index.html` status fetch, `tools/profiling/verify-about.js` |
| 2026-07-25 | **Absolutely-positioned chrome WRAPPERS get `pointer-events: none`; only interactive leaves opt back in** — `#botleft` (compass + Center 2D/3D + legend) had no such rule, so its box (as wide as the legend) swallowed clicks: at 390px it covered the bottom-right pods and made the new credit button unclickable, and at every size it stole map drags in that corner. Generalises the existing `.panel`/`#controls` idiom to every anchored wrapper. Found by a real `page.click()` — the JS-`.click()` suites cannot see it. | `web/index.html` `#botleft`, `docs/UI.md` "Sources & attribution pod" |
| 2026-07-25 | **The Sources pod is a SHORT entry point ("Data & Methods"); the credit lives one tap in** — supersedes the same-day "credit is the label" decision. Rationale for the reversal is geometry, not principle: the full credit label measured **294px** wide, and at 390px the bottom-left `#legend` reaches x=304, so the button was painted straight on top of the legend text (reported by Peter). A short label clears it (246..367 vs a cluster ending at 237). Attribution is undiminished — the City of Edmonton credit + Open Government Licence are the first thing in the panel, and the map's own MapLibre attribution uses the same collapsed-behind-a-control pattern. Panel heading "Read the numbers carefully" → **"Important caveats"** (Peter). | `web/index.html` `#about`, `tools/profiling/verify-about.js` |
| 2026-07-25 | **On phones `#botleft` is width-bounded (`max-width: calc(100vw - 175px)`) so the legend wraps instead of running under the bottom-right pods** — `#legend`'s width is set by its longest line ("Set aside — natural / undeveloped land", ~282px), which at 390px reaches into the right column. The gradient bar + scale are a fixed 200px and are unaffected; only the aside wraps. Complements the same-day `pointer-events` fix: that stopped the cluster stealing *clicks*, this stops it colliding *visually*. | `web/index.html` `@media (max-width:640px)`, `docs/MOBILE_USABILITY.md` §2 |
| 2026-07-26 | **Pipeline liveness is guarded on BOTH sides: a heartbeat PAT prevents the 60-day auto-disable, and the frontend detects staleness independently** — `refresh.yml` checks out with `${{ secrets.HEARTBEAT_TOKEN \|\| github.token }}` (fine-grained, contents:write, this repo only; falls back cleanly when absent, so forks are unaffected), because `GITHUB_TOKEN` pushes don't reliably count as repo activity. But prevention has its own silent failure — the PAT expires — so the browser ages `status.json`'s `last_checked` and raises the banner past `STALE_DAYS = 14` (two missed weekly runs). **The detector must not be the thing that can go out**, which is why it is client-side: it fires for any cause of stoppage (disabled schedule, dead token, broken run), needs no secret and nothing to renew. A backend-set banner (year-hold) outranks it; every uncertain input (missing/garbled/future `last_checked`, unreachable manifest) stays silent rather than invent a warning. Also replaced the commit step's `git push \|\| echo` — it swallowed auth failures green, which is how the heartbeat would have died unnoticed. | `docs/SPEC_deployment.md` "Staying awake" (as-built), `docs/RUNBOOK.md` §3, `tools/profiling/verify-staleness-banner.js` |
| 2026-07-26 | **The collapsed Sources pod is licence-compliant — VERIFIED against the OGL – City of Edmonton text, not inferred** (v1.0 July 2022, an adaptation of OGL–Canada 2.0; `data.edmonton.ca/stories/s/City-of-Edmonton-Open-Data-Terms-of-Use/msh8-if28/`). The licence requires only "acknowledge the source... by including any attribution statement specified... and, where possible, provide a link to this licence" — **it says nothing about placement**, so the 2026-07-25 "credit lives one tap in" decision holds and the credit does NOT return to the map surface. Reading it caught two real gaps in what shipped, both fixed here: **no link to the licence** (plainly "possible" — the pod already linked two other things), and **the prescribed statement was paraphrased**. Where the provider specifies no statement of its own, the licence mandates exact wording — *"Contains information licensed under the Open Government Licence – City of Edmonton."* — and Socrata carries only `attribution: "City of Edmonton"` (a provider-name field, not a statement) across seven datasets, so the fallback governs. Now present **verbatim** and asserted as such: **a reworded version is not compliance.** Also added, NOT required by the licence: a non-endorsement line, since the licence grants no right to imply official status and a civic map of City data is exactly what a reader might take for official. | `docs/UI.md` "What other maps actually do", `web/index.html` `#about-menu`, `tools/profiling/verify-about.js` |
| 2026-07-26 | **`#coloradj` HIDES where it doesn't apply, and the whole Tier-3 column collapses when both its pods are hidden** — closes the "decide as a pair with the lens" item. `Colour: sqrt scaling` was the last pod in that column still greying, and greyed `#4a4a5e` on a dark panel reads as *broken*, not *unavailable* (the misread that produced the 2026-07-25 lens bug report). `#coloradj` bites in money/glass and `#lens` in money/ratio, so the column is genuinely EMPTY in services/uses/development/infill — hiding the pods alone would have widened the hole the Glass slider removal left, so `#opt-pres` collapses with them. `syncPresColumn` reads the pods' own **computed display** rather than re-deriving the view rules, so it cannot drift out of step with the two gates. `#optpanel` itself needs no equivalent: every view with an empty T3 column still has a `#layers` section, and the verify asserts that so the assumption fails loudly if a future view breaks it. `disabled` stays in sync; scaling state survives the hide/show round-trip. | `web/index.html` `syncColorAdjust`/`syncPresColumn`, `docs/CONTROLS_MATRIX.md` §3/§5.1, `tools/profiling/verify-lens-visibility.js` |
| 2026-07-26 | **`#views` STAYS at 12.5px on phones (one row), and Center 2D KEEPS reframing rather than flattening pitch in place** — two standing "small UI decisions" closed as deliberate no-ops, recorded so they aren't re-opened as oversights. `#views`: at the desktop 14px the four public views don't fit one 390px row, and wrapping the primary control costs more than 1.5px buys; 12.5px still sits above the 11.5px modifiers, so Tier-1 primacy still reads. Center 2D: the compass needle (2026-07-25) already covers in-place north-up, so the in-place half is served — with Center 2D and Center 3D both reframing, the map keeps a genuine "I'm lost, put it back" recovery that flattening in place would remove. | `TODO.md` "SMALL OPEN UI DECISIONS" (now closed), `docs/MOBILE_USABILITY.md` §3 |
| 2026-07-26 | **The Money metric picker is TWO-LEVEL: `Revenue \| Value` with `Total \| Residential \| Non-residential` nested under Revenue** — replaces the flat 4-button row (`Revenue · Value · Residential $ · Non-res $`), which asserted four peers where the data has a parent and its parts: `levy == res + nonres + farmland` (2026-07-18), so the last two are SUBSETS of Revenue, not siblings of it. **Value is a leaf and the cut row hides under it** — the pipeline emits no res/nonres decomposition of assessed value, so the asymmetry is data-driven, not arbitrary. Labels drop the `$` suffix (the parent carries it) — the descriptiveness complaint that prompted this. **`METRICS` stays FLAT and `state.metric` stays the single source of truth; only the chrome is hierarchical** — promoting it to a real two-field state would duplicate the column-key derivation in `lotKey`/`moneyColKey`/`buildLayers`. Revenue re-selected from Value restores the **last cut used** (sticky, Total on load) rather than silently resetting. **Mobile got cheaper, not dearer:** the old flat row already wrapped to 2 rows at 390/360px (buttons totalled 377px); it is now 2 rows in Revenue (pod 60→55px) and **1 row in Value** (31px). Also corrected a public-facing inaccuracy: Non-residential was described as "the complement of Residential $", but farmland is a separate class so the two do not sum to Total. | `web/index.html` `#toggle`/`#metric-row`/`#revcut` + `syncMetricButtons`, `docs/CONTROLS_MATRIX.md` §1/§3/§4, `tools/profiling/verify-money-metric-group.js` |
| 2026-07-26 | **The `Highlight residential` fade lens is REMOVED, and `#coloradj` moves to the BOTTOM of the Options panel** — supersedes the 2026-07-25 "the lens hides instead of greying" row and the same-day T3-column-collapse row (the column it collapsed no longer exists). The lens faded neighbourhoods below 50% residential *zoned area*: a **binary** cut, now redundant against two **continuous** alternatives that both post-date it — the **Residential** revenue cut in `#toggle` (shows residential dollars directly, 2026-07-26) and the tooltip's `X% of revenue is residential`. Peter: "redundant / not as good as our %residential". Removal took `state.residential`, `applyLens`, `residentialClampFor` + its cache, the `LENS_FADE_*` constants, the fade branches in `fillFor`/ratio fill/roof edges, the residential anchor set in `ratioScale`, and both legend fade branches. **`is_residential` STAYS** — it is load-bearing for Infill's opportunity gate. With the lens gone `#opt-pres` held one pod, so the wrapper and `syncPresColumn` were deleted and `#coloradj` became a direct child of `#opt-body`, placed LAST: it is the only control that changes how data is *drawn* rather than which data is *shown*, so it reads after the sections it modifies. **The Options panel went two-column → one, 398px → 216px wide at 1440px.** (Pre-existing and NOT introduced here: "Ground acres" wraps to two lines — verified against master before the change.) | `web/index.html` `#opt-body`/`syncColorAdjust`, `docs/CONTROLS_MATRIX.md` §1/§3/§5, `tools/profiling/verify-coloradj.js` |
| 2026-07-27 | ~~**Tier-1 geographic reference (river + Anthony Henday ring) is drawn OVER the data layers, depth-tested, on by default**~~ **PARTLY SUPERSEDED the same day — see the "BRACKET" row below: the river moved to the BOTTOM of the stack. Everything else here still stands.** — the map has no basemap tiles, only a dark backdrop, so a first-time viewer had no way to tell which part of the city a prism sat in. Three findings drove the design, each measured rather than assumed. **(1) Composed LAST, not first.** The instinct is to put reference geography underneath, but the hood polygons tile the whole city: underneath, the layer changed **0.38%** of pixels (99.6% occluded) versus **1.22%** on top. **(2) `depthTest` ON**, unlike the other context layers (fire dots, LRT lines) which are billboarded markers — this is *ground*, and with depth testing off the river painted straight across the faces of the downtown towers, a river in mid-air. **(3) Composed in `buildLayers()` wrapping `buildViewLayers()`**, not prepended per branch, so a future view cannot ship without it. Toggle lives in the Display menu beside `Neighbourhood labels` (view-independent map furniture; `#layers` is per-view) but defaults **ON** where labels default off — orientation should not need hunting for. Tiers 2 (districts/arterials) and 3 (neighbouring municipalities) are NOT in this change. | `web/index.html` `referenceLayers`/`buildLayers`, `data/DATA.md` §14, `tools/profiling/verify-reference-layer.js` |
| 2026-07-27 | **The ring road is extracted by an explicit segment-type allowlist plus a named Highway 14 concurrency, and the builder asserts the ring CLOSES** — name-matching alone gives 275.7 km across 590 segments, because every ramp, cutoff and collector-distributor lane is *named* for the highway it serves; drawn thin that is a tangle of spurs at each interchange, not a ring. An explicit `road_segment_type_description` allowlist (`Roadway (Standard)` + both `Structure -` types, which carry the mainline over crossings) cuts it to 149 km — the ~75 km ring times its two carriageways. That still left a **2.9 km hole in the east leg = 73 screen pixels**, which reads as a rendering bug: Highway 216 runs **concurrent with Highway 14** there and the feed labels the shared carriageway for Highway 14 only (confirmed by probing the hole — every sample point sits 1–35 m from a Hwy 14 centreline). Only `HIGHWAY 14 NORTHBOUND`/`SOUTHBOUND` are the concurrency; `EAST`/`WESTBOUND` is Highway 14 leaving the ring, and pulling it in grows a 5 km spur, pruned **by topology** (a ring has no loose ends) rather than by a coordinate cutoff or length threshold. **The closure check is the point:** 149 km looks perfectly plausible for a 75 km ring drawn twice, so no length total would ever have revealed the hole — only "does every arc end meet another arc end" did. Same explicit-enumeration philosophy as `load_roads`'s `CLASS_GROUP`. | `scripts/build_reference_layers.py`, `data/DATA.md` §14 "Known Quirks" |
| 2026-07-27 | **The reference layers BRACKET the data — river at the bottom, ring road on top — and the river runs 60 km past the city** — supersedes the same-day "composed LAST" row, which was right for the ring road and wrong for the river. Drawing the water over the data **cut into the neighbourhood geometry and glitched** along the edges it shares with the valley polygons (Peter). The reason is that the hood fabric **already traces the river**: the valley is set-aside parkland, so there is a river-shaped seam through the choropleth, and painting the water on top fought a shape that was already there. Underneath, the river shows *through* that seam and — the part worth keeping — runs on past the city limit where nothing occludes it. The ring road has no such seam (hoods tile straight across it) so it stays on top, where it is 1.22% of pixels instead of 0.38%. `depthTest` splits with them: ON for the ring road (prisms must occlude it), OFF for the river (it is the backdrop, drawn before everything, so it neither occludes nor z-fights). **Margin 3 km → 60 km**, sized against the default camera rather than guessed: at HOME zoom 10.2 the scale is ~79 m/px, so the 1440px viewport spans ~114 km flat and the 52° pitch pushes the horizon further, against a city half-width of only ~15 km. **River colour lifted `[26,34,48]` → `[50,66,94]`** because the extended tails sit on the bare `#0a0a0f` backdrop where the old value was invisible — extending a river nobody can see achieves nothing; it is still ~5× the backdrop luminance and far below the set-aside grey. | `web/index.html` `referenceUnderLayers`/`referenceOverLayers`, `data/DATA.md` §14, `tools/profiling/verify-reference-layer.js` |
| 2026-07-27 | **Regional place names ride the REFERENCE toggle, not the neighbourhood-label one, and share the hood labels' single TextLayer and declutter sweep** — the two halves of this are pulled in opposite directions and both matter. *Sharing the layer* is the 2026-07-27 Tier-2 collision decision honoured: a second, independent label layer cannot see the first one's boxes, so "ST. ALBERT" would sit on top of a hood name the moment both are shown. *Splitting the toggle* is the correction to what that decision silently implied: `state.labels` ships **false**, so routing orientation names through it would hide the thing a first-time viewer most needs behind a checkbox they never flip, while Tier 1's river and ring road are deliberately **on**. The two concerns — how names avoid overlapping, and whether names are on — were conflated; they are now separate. Mechanically: `labelPool()` gates each class on its own flag, the sweep orders by an explicit `prio` (places win) because its existing key is polygon area and a Point has none, and `labelLayer()` gates on the pool being non-empty rather than on `state.labels`. Places render at 12 px vs 15, dimmer, uppercase. | `web/index.html` (`labelPool`, `placeAnchors`, `visibleLabels`, `PRIO_PLACE`), `tools/profiling/verify-reference-layer.js`, `docs/CONTROLS_MATRIX.md` |
| 2026-07-27 | **`PLACES` is a closed hand-written list queried across three status sublayers, not a radius sweep of municipalities near Edmonton** — which names belong on the map is a cartographic judgement (how populated should the frame feel?), so it is stated rather than derived; a bbox query would silently gain and lose names as the province edits boundaries and the map's composition would drift with it. Same closed-enumeration philosophy as `load_roads`'s `CLASS_GROUP` and the Henday's `HENDAY_MAINLINE_TYPES`. Three sublayers because Alberta models municipal **status, not size**: 78 `City` for five of them, 56 `Town` for Devon, **66 `Urban Service Area` for Sherwood Park** — which is neither a town nor a city but an urban service area of Strathcona County, so the obvious single-layer implementation finds nothing for it. Matching is on **equality**, since sublayer 66 also carries `Sherwood Park (Bremner)`. A name that stops resolving **raises**. | `scripts/build_reference_layers.py` (`PLACES`, `_fetch_places`), `data/DATA.md` §14 "Known Quirks", `tests/test_build_reference_layers.py` |
| 2026-07-27 | **The Development view's Stock-age option is WITHDRAWN — the Detail selector goes back to two choices (Neighbourhood / 100 m grid — activity)** — supersedes the 2026-07-17 row above, which is struck through in place. Peter's call: it "wasn't working well as an option". Removed at the UI and render layer only — the `median_year_built` column stays in `export_value_grid.py` and in the shipped `value_grid.json` (173 kB, 6.9% of that file, 2.2% of total payload), because the data is the expensive half and a better presentation later should not need a pipeline regen. **The unplanned win: Development no longer fetches `value_grid.json` at all.** `ensureAgeData()` prefetched that 2.5 MB file on every entry to the view purely so the Stock-age button could be offered up front; Development's own spikes come from `dev_grid.json` (362 kB), and `gridData` has no other consumer outside Glass. Also folded away: the age-only sqrt exemption in `devT` (year is an interval scale), the Metric/Window suppression in `syncDevControls`, and the age branch of the legend. | `web/index.html` (`applyDevDetail`, `syncDevControls`, `devBlurb`, `refreshLegend`), `docs/SPEC_development.md`, `docs/UI.md` |
| 2026-07-27 | **The Development view's 100 m activity spikes default to 50% opacity** — `VIEWS.development.opacity` was `null` ("no prisms in this view"), which was wrong: the view has had grid spikes since the Detail selector shipped, and because no default was ever applied they inherited whatever `state.prismOpacity` last held — 100% on a fresh load, or 5% if the viewer had passed through Ratio. Setting it to 50 makes entry deterministic and lets the neutral hood plane read through the spikes. The slider still shows in grid mode, so this is a starting point, not a fixed value like Glass's 60%. | `web/index.html` (`VIEWS.development`) |
| 2026-07-27 | **The label sweep re-runs whenever the POOL is non-empty, and place labels SCALE with the camera** — two bugs with one visible symptom (Peter: "only see St. Albert, until I turn the names on and off. Also they a bit too big on zoom out"), both consequences of the same-day two-toggle split. **(1)** The `moveend` re-cull was still gated on `state.labels`, which ships **false** — the last surviving place where "labels off ⇒ no text layer" was assumed. With hood labels off, the sweep therefore ran once at load and **froze**: measured, the drawn set stayed the same 6 names from zoom 10.2 down to 6.0 while a fresh sweep would have returned 7, 5, 4 or 2. Only an unrelated rebuild (toggling the checkbox) unstuck it. **(2)** Text in `sizeUnits: "pixels"` does not shrink with the map, so zooming out left the names oversized against a shrinking city *and* swelled their collision boxes until the sweep dropped nearly all of them (2 of 7 at z=7) — and which survived fell out of **file order**, since every place ties on `prio` and carries `area: 0`. Scaling 12 px → 7 px between zoom 10 and 7 fixes the look and the loss together: all 7 now survive at z=8, where 5 did. The effective size is resolved **once** in the sweep so the collision box and the glyphs can never disagree, and rides out on a **copy** — the anchor arrays are memoized and shared, so a write-back would leak one camera's zoom into the next. This is the app's first zoom-dependent render behaviour; Tier 1's river and ring road remain deliberately zoom-independent. | `web/index.html` (`placeSize`, `visibleLabels`, the `moveend` hook), `tools/profiling/verify-reference-layer.js` |
| 2026-07-27 | **The label sweep culls labels that land under the HTML chrome, testing the UNPADDED glyph box against a closed list of panel rects** — closes the standing "sweep is blind to DOM chrome" item. `visibleLabels()` decluttered labels against each other but knew nothing about the panels over the canvas, so a label could win its spot and then be painted under the Options panel (FORT SASKATCHEWAN) or into the title blurb. An occluded label is **skipped like the existing offscreen cull**, not dropped after the fact: it can neither be read nor sensibly block a label that isn't occluded. **Two calls worth keeping.** (1) *Which boxes.* `#layers`/`#coloradj` are deliberately absent from `CHROME_IDS` — they are borderless sections inside `#optpanel`, which paints for all of them, so its box is the real obstacle and theirs would leave the gaps uncovered; `#title` and `#legend` paint **no background** and are included anyway, because chrome that makes a label unreadable is the test, not opacity, and text-over-text is the reported case. (2) *Unpadded, unlike the label-vs-label sweep.* `LABEL_PAD` is breathing room between two labels; a panel edge is not another label. Charging it that clearance culled **DOWNTOWN** on a phone — its padding clipped the Options panel by 3px while its glyphs cleared it by 5. The closed list is guarded against rot by a verify check asserting it **covers every `.panel` in the document** (named, or an ancestor named), so a future panel fails loudly instead of silently reopening the bug. **Measured cost: none** — readable-label counts are identical before and after (32/32 desktop, 25/25 mobile at 390×844); only invisible labels are removed. The "frees its spot" rationale is architecturally right but yielded no measurable gain, because a spot under a panel has nothing competing for it. | `web/index.html` (`CHROME_IDS`, `chromeBoxes`, `visibleLabels`), `tools/profiling/verify-reference-layer.js`, `docs/MOBILE_USABILITY.md` |
| 2026-07-27 | **`touch-action: manipulation` on the control chrome kills accidental double-tap ZOOM, and the map keeps the gesture** — iOS Safari retains double-tap-to-zoom even on a `width=device-width` viewport, so tapping a control twice quickly zoomed the whole page. Reported by Peter for the compass buttons, and those are the clearest case because `rotate()` is **built** for repeat presses (the `spin`/`spinAt` chaining exists so a second press queues a further 30° rather than restarting) — the browser was hijacking an interaction the UI explicitly supports. **The map canvas is deliberately excluded:** double-tap to zoom is the standard gesture there (Google Maps, Apple Maps, Mapbox, and MapLibre's own default), so removing it would break an expectation people carry in; `#map` is a sibling of every chrome root, so it keeps `touch-action: auto`, and double-click zoom is asserted still working (10.2 → 11.2). **Not `user-scalable=no`** — that would disable pinch zoom too and fail WCAG 1.4.4; `manipulation` suppresses only the double-tap gesture and leaves the page zoomable. Declared at **both** the chrome roots and the controls themselves, and the redundancy is deliberate: the roots catch double-taps on non-interactive text (the title blurb and legend, which iOS also zooms, and which is the "just by accident" half of the report), while the element-level rule avoids depending on ancestor intersection — that is what the spec says and what browsers do, but `touch-action` is not inherited, so nothing headless can confirm it, and a control that silently missed out is exactly the reported bug. Side benefit: drops the legacy ~300ms tap delay. **CONFIRMED ON DEVICE 2026-07-27** — Peter, on a phone: *"double tap on phone no longer zooms in for the buttons, only the map."* That closes **both halves**: the chrome no longer hijacks the gesture, and the map deliberately still does. Headless Chromium cannot reproduce the iOS gesture, so the suite asserts the mechanism (55/55 controls, hidden ones included) rather than the outcome — the device check is what settled it, per the tooltip precedent. **Pinch zoom is still unverified** by anyone, though `user-scalable=no` was deliberately avoided. | `web/index.html` (`.panel, #controls, #botleft` touch rules), `tools/profiling/verify-controls-clickable.js`, `docs/MOBILE_USABILITY.md` §2 |
| 2026-07-27 | **Development moves to SECOND in `#views` and opens on dwelling units / Since 2009 / 100 m grid; the window buttons run shortest→longest** — Peter's call. Growth sits beside the money story instead of behind two other lenses, and the view now opens on its most concrete frame: individual 100 m permit cells over the whole permit record, rather than a five-year neighbourhood choropleth. Window order was `5yr, 3yr, long`, which read as arbitrary; now `3yr, 5yr, long`. `VIEWS.development.opacity = 50` (set 2026-07-26) already suited a grid-first default. **Two latent ordering bugs surfaced, both of the same shape — chrome read before it was final.** (1) `applyView` computed the opacity slider's visibility from `devGridActive()`, which needs `dev_grid.json`, **before awaiting that fetch** — invisible while the grid was opt-in (entry never had it active), but with grid as the default the spikes rendered with **no opacity control until you left the view and came back**. Extracted as `syncPrismRow(v)` and called again after the fetch settles. (2) `applyView` wrote the title/blurb **after** `buildLayers()`, so the label sweep measured the OUTGOING view's title box; entering Development grows `#title` from bottom 196 to 462 at 1440×900 (its blurb is far longer), leaving SPRUCE GROVE painted into the new text until some later rebuild. The blurb is now written **before** the layer build, here and in `applyUsesPrisms` / `applyDevDetail` / `applyService` / `applySvcDriver`. **The verify lesson:** the existing chrome-dodge check re-ran `visibleLabels()`, which re-reads `chromeBoxes()` in the same tick and so can only agree with itself — it could never catch this. The new per-view checks compare the **DRAWN** layer against live chrome, the same DRAWN-vs-fresh formulation that caught the frozen sweep. | `web/index.html` (`#views`, `#devwindow`, `state.devWindow`/`devGrid`, `syncPrismRow`, `applyView`), `tools/profiling/verify-development.js`, `docs/CONTROLS_MATRIX.md` |
| 2026-07-28 | **The cardinality guards are pinned by REGIME, in bands, at DISPLAY grain — not by WEM's or any condo's dollar figure** — `scripts/check_value_anchors.py`, wired into `refresh.yml` after regeneration. The two known record-to-parcel distortions (WEM needles, condo shared-lot duplication) were audited in 2026-07-08 and found handled or largely absent, but nothing watched them afterwards: the numbers lived only in findings docs, and `pytest` cannot see the roll (gitignored). **The refresh job can** — it runs `download_data.py` — which is what made a guard possible at all. **What is pinned is the regime, because the dedupe is currently a no-op**: `FINDINGS_lot_dedupe.md` §4.3 records that "never dedupe" produces identical output, so `SHARE_MAX_M2` is insurance calibrated against a regime that has not arrived. The failure mode is therefore not the bug returning but the data shifting under an unwatched heuristic (most plausibly the January year-roll), so the primary canary is `dup_parcel_points` — the count of points where a parcel-sized lot value repeats across units, i.e. whether that dedupe branch does any work at all. **Bands, never equalities**, since reassessment moves every dollar annually and equality checks would red the build each January; ratios and counts are chosen to survive a uniform revaluation. **Direction-aware:** only the dangerous side hard-fails (exit 5); the benign side warns and asks for a re-pin, the `check_unmatched_names.py` policy. **The needle anchor is measured at CELL grain against the exported `value_grid.json`, and this was a correction, not a preference** — the natural point-grain version is anchored to an artifact: a Westmount point carries $0.9M on a sub-milliacre lot = **$3,478M/lot-acre, 5.7× the real top**, which never reaches the map because 100 m cells pool it away (exported top cells are the Downtown tower's 612/149/143, matching §4.2 exactly). Pinning it would have failed builds over something no viewer can see — `FINDINGS_lot_dedupe.md` §4.3's own "validate at the display grain" lesson, re-learned. Missing inputs **skip** rather than fail, matching `main.py`'s deliberate ground-acre-only degradation. | `scripts/check_value_anchors.py`, `data/expected_value_anchors.json`, `.github/workflows/refresh.yml`, `tests/test_check_value_anchors.py`, `docs/FINDINGS_lot_dedupe.md` |
| 2026-07-28 | **A drift failure surfaces to viewers through the EXISTING heartbeat, not a bespoke banner — and the guard runs before the heartbeat step so that works** — Peter's question ("should this tie into the existing 'last year's data' fail type?"). It already does, structurally: `check_value_anchors.py` sits after regeneration but **before** `generate_status.py`, so a drift exit leaves `last_checked` un-bumped and nothing committed; the site keeps serving last-good data and the frontend's own `stalenessBanner()` fires at `STALE_DAYS`. **That ordering is load-bearing** — running the guard after the status step would bump the heartbeat and make the failure invisible, which is the same reason `check_unmatched_names` sits before regen. **But drift is deliberately NOT the year-hold fail type.** The year-hold is a *known, benign, expected* condition (upstream published a new assessment year, the data is correct and merely last year's), so it keeps the job GREEN and sets a specific reassuring banner. Drift is *unknown* — the numbers are not trusted, they are withheld, and a human is needed — so it hard-fails, because a green job with a friendly banner would read as routine when it is not. Viewer-facing the two converge (last-good data either way); owner-facing they must differ. **No per-guard banner was added, on purpose:** every refresh failure already tells the same viewer story through the heartbeat, and a bespoke cardinality banner would fragment it. The residual gap is purely *timing* — `STALE_DAYS = 14` against a weekly cron means one missed run is tolerated and two consecutive misses warn — and that is a GLOBAL knob that should move for every failure type at once, not a property of this guard. | `.github/workflows/refresh.yml` (step order), `scripts/check_value_anchors.py`, `web/index.html` (`stalenessBanner`, `STALE_DAYS`), `docs/RUNBOOK.md` §3 |
| 2026-07-28 | **Services and Ratio pulled to FULL-only — public `#views` = Money · Development — and the pull takes their numbers off the public Money tooltip with them** — Peter's call while prepping the release, **provisional "for now"**, the same framing as the 2026-07-24 Uses pull, and it supersedes the 2026-07-23 "FINALIZED" split that had both views public. Mechanically it is the established one-line idiom (`|| !FULL_BUILD` beside the existing data guard), so nothing is stripped from the file and `?build=full` still reaches both views locally. **The part that needed a decision was not the buttons.** Ratio's headline metric was also being published on the *default public view*: the Money tooltip appended `road m / acre` and `$X revenue / road metre` to every hover, so pulling the lens while keeping the number would have left the figure visible with the view that explains it hidden — worse than either shipping or not shipping it. Both rows are now `FULL_BUILD`-gated; the supply row goes with the ratio row because on its own it is an unexplained figure from a lens the public build no longer has. **Same reasoning applied to the Data & Methods copy:** the modelled service-cost/stormwater/water caveat described only full-only layers, and the source list credited the road network, fire dispatch and the transit schedule — all of it warning about or crediting things a public visitor cannot reach. Both are gated at the element level (`#about-modelled`, `#about-src-services`), and the source sentence stays grammatical in both builds because the trailing "all published by the City of Edmonton" does the summarizing work, so no "and" is needed before the dash. **Zoning is deliberately NOT in the gated span** — it also classifies the set-aside land the public build greys out, so it is still an honest public credit. **Free consequence: the public build never fetches `roads.geojson`** — that fetch was already lazy on first Services/Ratio entry, and those views are now unreachable there. | `web/index.html` (the `#views` services/ratio guard, `tooltipFor`'s Money arm, `#about-modelled`, `#about-src-services`), `docs/CONTROLS_MATRIX.md` §2, `docs/PLAN_public_release.md` §2a |
| 2026-07-28 | **The public build ships at TWO views — Money · Development — and the three pulled lenses return ONE AT A TIME after launch, not as a batch** — Peter: *"2 views is fine for release, lock it in. We'll add the other stuff later, like one lense at a time."* This **settles** the three provisional full-only tags (Uses 07-24, Services + Ratio 07-28) rather than leaving them pending, and supersedes the 2026-07-23 "FINALIZED" 4-view split for the last time. **The staged-return rule is the substantive half.** Three lenses came out in five days, each a one-line gate, and nothing forced anyone to ask whether the public build still made sense as a whole — un-pulling them the same way would repeat that in reverse and land four lenses at the root in one push, none of them individually reviewed against the release copy. One lens per release means each gets its own decision, its own verification in the public build, and its own reason. **The mechanism costs nothing to reverse** (`|| !FULL_BUILD` beside a data guard; nothing is stripped from the file, `?build=full` still reaches everything locally), so the constraint here is deliberately editorial, not technical. **What a 2-view public build actually is:** the money story with its class cuts and both denominators, plus where the city is growing — the two lenses whose numbers are audited end to end. Everything modelled (stormwater, water, service cost) or derived (ratios, zoning composition) sits behind `/full/` with its work-in-progress badge. That is the "bulletproof or defer" bar from §3 of the release plan, applied at the lens level instead of the caveat level. | `docs/PLAN_public_release.md` §2a, `docs/CONTROLS_MATRIX.md` §2, `web/index.html` (the `#views` guards) |
| 2026-07-28 | **The temporal lens normalizes as SHARE OF THE CITYWIDE ASSESSMENT BASE, not CPI-deflated dollars — inflation is the second-order problem here** — Peter asked *"does inflation matter for people doing this?"* and the honest answer inverts the usual reflex. The first-order fact is that **the mill rate is a residual**: council sets a budget, rate = levy ÷ total assessed base, so a citywide revaluation is **fiscally neutral** (the rate absorbs it) and a neighbourhood's tax burden moves **only when its assessment diverges from the city average**. A nominal per-hood series conflates those two, and **CPI-deflating does not separate them** — it answers a purchasing-power question, not a tax-share one. Share-of-base is unit-free, needs no deflator, and has no vintage to keep current. **Constant dollars are reserved for one case only:** raw $/acre *levels* compared across years. Two traps recorded with it — assessed values embed **house-price** inflation, which has diverged sharply from CPI, so deflating asset prices with a consumer index reads rigorous and is apples/oranges; and on the cost side later, city inputs track a **Municipal Price Index**, not CPI, which would be the wrong index rather than merely imprecise. **The denominator must be NAMED in the UI:** Downtown is **3.22% of the total base** but **9.30% of the commercial base** (2025), and public reporting quotes the second kind — publishing 3.22% beside an article citing 5.2% would make the project look wrong when it is not. Framing is **descriptive only**: the share line plus the sourced driver (office vacancy), never "downtown is dying" or "the rest of the city subsidizes downtown". | `docs/SPEC_temporal.md` §6, `docs/ANALYSIS_BACKLOG.md` |
| 2026-07-28 | **A dataset-completeness check must test against an INDEPENDENT source, because the obvious self-audit is blind to the failure mode that actually occurs** — learned by shipping the wrong detector first. "An account present in year N−1 **and** N+1 but absent from N is a defect; buildings do not blink" is sound, needs no external data, and catches properties demolished since — but it **requires the account to come back**, so it cannot see a dropout that never returns. That is precisely this defect's shape: it reported **5** defects for 2024 where the true figure is **2,321**, and the first defect map would have been read as "only 2025 is broken." The fix is a second detector — present in N−1 **and** in the independent current roll (`q7d6-ambg`), absent from N — which catches persistent dropouts and applies to **every** year, not just the newest; its own blind spot is properties demolished since, which is why both are kept and the **union** reported. **Generalizable rule: a completeness check whose only evidence is the dataset under test can prove presence, never absence.** Also recorded: per-year figures from an N−1-baselined test are **incremental, not cumulative** — an account already missing in 2024 cannot be flagged again in 2025, which is why 2025 reads 131 while ~2,448 are cumulatively absent from it. | `tools/audit_historical_roll_gaps.py`, `docs/SPEC_temporal.md` §0.1, `data/DATA.md` §0 |
| 2026-07-28 | **The temporal lens's four open questions all close on the cheapest honest cut: share of the TOTAL citywide base · assessed VALUE · TOTAL not per-acre · 2024 OMITTED** — the combination that needs no deflator, no area assumption and no mill-rate table, so nothing but the splice and the guard stands in front of Phase 1. **Value over revenue** because value reaches 2012 where revenue starts 2014 (`pwis-wc4c`) and drags in every class-differential caveat, for a series that moves with the same shape — the mill rate is a residual, so revenue is value times a factor council chose. **Total base as the plotted line, commercial-base share as a labelled number in the pinned panel** — satisfies the standing "name the denominator" rule (Downtown is 3.22% of total but 9.30% of commercial, and public reporting quotes the second) without putting two lines in a 14-point sparkline. **No `$/acre` line**: the deflator would be legitimate there, but it also needs a stated justification for dividing 14 years of moving boundaries by a *current* area, and neither is worth buying yet. **2024 omitted — this REVERSED the balanced panel that `SPEC_temporal.md` §0.2 recommended when it was written**, and the reversal is the durable part: share-of-base is **self-normalizing per year** (numerator and denominator come from the same slice), so the metric needs each year's roll to be *complete*, not the account universe to be *constant* — a fixed panel would punch the same $2.93B hole (53% Downtown) into twelve clean years to rescue one broken one, and would answer the stranger question "what share would Downtown be if these 2,448 properties had never existed." **Flag and uncertainty-band were rejected on display grain**: at sparkline size a known-incomplete marker or a band is invisible, so the glance shows a wrong number however well the pinned panel is annotated. Interpolation stays ruled out. | `docs/SPEC_temporal.md` §0.2 + §6 (§7 now empty), `TODO.md` |
| 2026-07-28 | **The temporal splice keys publishability on WHETHER A COMPLETE SOURCE COVERS A YEAR, not on a hardcoded omit-list — so 2025 is published today and drops out next January** — `src/load_temporal.publishable_years`. The distinction (`HISTORICAL_DEFECT_YEARS` = measured 2024+2025, vs the omitted set = derived per live year) closes a trap found while building the guard: the current roll covers exactly ONE year, so 2025 is publishable *because it is live*, not because its historical slice is sound (it is short 131 accounts). A plain "omit 2024" rule would, at the January roll-forward, quietly fall back to the defective historical copy of 2025 and re-acquire the defect on a year that renders correctly today — **the same silent-fallback shape that let the original defect survive two publication cycles**. Consequence accepted and logged: 2025's repaired numbers live only in the live roll, so without a committed year-by-year archive the series permanently loses 2025 next January. **Also locked: an unmatched hood name stays in the DENOMINATOR.** 24 historical names have no current boundary; the metric is share of the *citywide* base, so an unrenderable hood is still real value, and dropping it from the base would silently inflate every other hood's share. Only the numerator is restricted to hoods that render. **And the hood-rename layer is enforced inside `build_temporal_table`, not merely in the loaders** — OLIVER → WÎHKWÊNTÔWIN (12,000 accounts) is the one invariant that fails SILENTLY when skipped, reading as one neighbourhood dying and another appearing from nothing. | `src/load_temporal.py`, `scripts/check_temporal_years.py`, `docs/SPEC_temporal.md` §0.3-§0.4, `docs/ARCHITECTURE.md` |
| 2026-07-28 | **The temporal guard pins HISTORICAL years tight with a DROP as the dangerous direction, and leaves the LIVE year deliberately unpinned** — `scripts/check_temporal_years.py` + `data/expected_temporal_years.json`. Settled years (2012-2023) are an archive that should not move at all, and a settled year losing accounts is precisely the 2024 signature, so they get a ±0.5% band and only the low side fails; growth warns and asks for a re-pin (`check_unmatched_names` policy). The live year is a live snapshot that genuinely moves — **347 Downtown accounts moved between the 2026-07-06 and 2026-07-28 rolls** — and reassessment moves every dollar each January, so a band there would cry wolf continuously; it is checked structurally instead (source provenance, and the roll must hold MORE accounts than the historical copy, since the historical copy is the defective one). **A per-hood account-drop threshold was measured and REJECTED**: clean years routinely show hoods dropping 20-97% through renames, annexation and parcel consolidation (2013 had 9 hoods over 10%), so the per-hood signal is dominated by legitimate churn. **The exact account-level control is NOT duplicated here** — it needs the 5.5M-row pull and ~20 minutes; `tools/audit_historical_roll_gaps.py` stays the instrument of record and this is the cheap weekly sentry over the same failure mode. | `scripts/check_temporal_years.py`, `.github/workflows/refresh.yml`, `docs/SPEC_temporal.md` §0.3 |
| 2026-07-28 | **The temporal lens ARCHIVES each live assessment year to `data/temporal_archive.json` (~74 kB/yr, committed), so a year the historical file gets wrong survives the roll-forward** — closes the January trap logged in `SPEC_temporal.md` §0.4. The current roll covers exactly ONE year, so 2025 was publishable only while live; without a capture the series would permanently lose it each January (verified by simulation: with the archive, live-2026 publishes 2012-2023 + **2025** + 2026 and 2025 is served `source="archive"`; without it 2025 is gone). Three rules make it safe. **(1) FREEZE** — only the live year is ever written; a captured year is never rewritten, because once the roll advances we no longer hold a complete source and any rewrite could only be a silent downgrade. **(2) CAPTURE EVERYTHING, USE IT SELECTIVELY** — every live year is captured (which years turn out defective is not knowable in advance, which is this dataset's whole lesson), but the archive only WINS for `HISTORICAL_DEFECT_YEARS`; preferring a capture for a year the historical file gets right would MIX VINTAGES, since the roll carries titles created after publication (~8,200 accounts today), putting a step in the series that is an artifact of sourcing rather than of Edmonton. **(3) AUTOMATIC** — written on ordinary weekly runs, not as a January chore, because a step that must be performed once at a date months away is a step that does not happen. **`refresh.yml`'s commit step had to be widened**: it staged `web/data` only, so the capture would have been written and then died with the runner — silently, and defeating the entire mechanism. `data/**` does not trigger `deploy.yml` (which fires on `web/**` only), so the widening costs no redundant deploy. | `src/load_temporal.py` (`write_archive`, `load_archive`), `scripts/check_temporal_years.py --write-archive`, `.github/workflows/refresh.yml`, `docs/SPEC_temporal.md` §0.4 |
| 2026-07-29 | **`web/index.html`'s CSS is extracted to `web/styles.css` — stage 1 of splitting the 4,000-line single file, and the only stage taken** — the file was 83% JS / 10% CSS / 7% markup, and the CSS is the one part with zero coupling to the rest, so it moves at near-zero risk and directly serves the queued mobile-chrome work (which previously had to navigate 3,300 lines of deck.gl layer code to reach 400 lines of styles). **The motivation is navigability and blast radius, NOT tokens** — the file was already read in grep-located windows of 100–400 lines, not end-to-end, so the split saves little context and may cost slightly more for the common change that touches CSS + DOM + handler together (three file opens vs three offsets). **`DEFAULT_BUILD` must stay in `index.html`**: `scripts/build_site.py` regexes it there and hard-fails unless it finds exactly one. Both builds work unchanged — the public root copies the whole `web/` tree, and `/full/` (index.html only) resolves `styles.css` through its existing `<base href="../" />`. Verified by pixel-identical screenshots vs master at 1440px and 390px, plus 21/23 verify scripts clean (the 2 failures are pre-existing and identical on master). **Stage 2 — splitting the 3,305-line JS along its nine existing section banners into ES modules — is deliberately NOT taken**; it carries the real risk (11 verify scripts reference `index.html`) and should wait until stage 1 proves it helps. | `web/styles.css`, `web/index.html`, `scripts/build_site.py` |
| 2026-07-29 | **The temporal lens's pinned panel: left column under the title, three dismissals, the sparkline on EVERY view's tooltip — and two rendering invariants that fail silently.** Phase 3 shipped, so the lens is complete. **Placement** is the left column under `#title` (`top: 210px`), the one region no chrome claims, so pinning can never bury a control — and the offset was **measured** (the title's box is 176–179px across all five views; a first pass at 128px overlapped it), with `verify-temporal.js` asserting the clearance so a longer blurb fails loudly. **Dismissal is deliberate, three ways** (the ×, Escape, or a second click on the pinned hood): clicking **another** hood re-pins because pin-then-browse is the point, and an **empty-map click is inert** because a pinned surface should not vanish on the tail of a drag. Escape matters more here than for the popovers — this panel is opened by clicking the *map*, so it has no button to un-press. **The sparkline rides every view's tooltip**, appended in one wrapper over the six per-view branches: a hood's history is a fact about the neighbourhood, not the lens, and a teaser nobody sees in the view they are in is not a teaser. The row carries `click to pin`, because click-to-pin is undiscoverable otherwise (same reason the compass exists as buttons). **Phone = a near-opaque bottom sheet (0.985)**: its desktop home is where the control column lives at ≤640px, and — not cosmetic — at the desktop 0.92 the legend and both bottom-right pods read straight *through* the panel's own text. Same lesson as `#about-menu`, one step further: **0.92 is enough over the map, not over other chrome.** **INVARIANT 1 — 2024 must LOOK absent:** x is scaled from the **year value**, never the array index, and the line is drawn as **runs split at every gap**, so 2023→2025 spans twice an ordinary step and no stroke bridges the hole. The band covers the **missing year only** (half-step bounds) — shading the whole run would claim 2023 and 2025 are missing too. The break is **derived from the year steps**, not a hard-coded `2024`, so January's roll-forward needs no edit. Index positioning and a bridging polyline are **both invisible to the eye** on a 13-point series, which is why the verify script *measures* the ratio. **INVARIANT 2 — the y axis is NOT zero-based, so both endpoints are labelled:** most hoods are under 1% of the base, so zero-basing would flatten 406 series to a flat line; scaling to the series' own range makes the shape legible and printing min/max keeps that honest. The labels get their own left gutter (at `x=0` the max label landed on the line). **`web/data/temporal.json` ships to the public root although the public build never fetches it** — `/full/` resolves `./data/temporal.json` through its `<base href="../" />`, so the root copy is the one the full build reads; verified in the *built* tree, not assumed. | `web/index.html`, `web/styles.css`, `tools/profiling/verify-temporal.js`, `docs/SPEC_temporal.md` §2 + §5 |
| 2026-07-30 | **A `#hoodmode` toggle decides where a neighbourhood's detail appears, and panel mode REDUCES the hover to the view's headline number rather than suppressing it.** Peter, 2026-07-30: *"I don't want both the panel and pop up appearing at the same time… a button that will convert you to panel mode, or back to pop up mode"*, then *"reduce the popup to just the primary metric once you go panel."* **The reduction, not suppression, is the decision that matters**: suppressing the tooltip would make panel mode a *downgrade* in every view, since the tooltip is what carries `$/acre`, the residential share and road metres — so hood-to-hood browsing would go blind. Reducing keeps browsing useful and still satisfies the actual objection, which was never "two surfaces at once" but **two dense blocks competing**. In panel mode the sparkline and the `click to pin` hint also drop out, because the panel already draws the chart and the hint would advertise a mode you are already in. ⚠️ **The reduction is implemented per-view EXPLICITLY, not as "row 0 of the tooltip"** — that is right for money, ratio, uses, development and infill, but **services' rows lead with `road_m_per_acre` whenever roads are present, regardless of which service drives the ramp**, so a positional rule would print road supply under a stormwater-coloured map; services reads `state.svcDriver`. `verify-hoodmode.js` asserts both halves of that trap (that the rows really do lead with roads, AND that the headline follows the driver). **Three gestures with deliberately different scopes:** the **×** clears the pinned hood and stays in panel mode on a prompt; **Escape** and **the button** leave the mode. A click in **popup** mode enters panel mode *and* pins, which is what keeps the tooltip's own "click to pin" hint truthful and makes the panel reachable without first finding the control. Panel mode with nothing pinned shows a **prompt**, because a button that appears to do nothing reads as broken. The pod is **hidden until `temporal.json` loads** (and so never appears in the public build) — it must not offer a mode that does not exist. **This changed `verify-temporal.js`'s contract and the script caught it**: a second click on the pinned hood no longer closes the panel, and two expectations were rewritten deliberately, one of them made stricter (inertness is now "state unchanged", not "stays closed"). | `web/index.html` (`applyHoodMode`, `primaryRow`), `web/styles.css`, `tools/profiling/verify-hoodmode.js`, `docs/CONTROLS_MATRIX.md` §3 |
| 2026-07-30 | **The per-hood change metric will be RELATIVE change in share-of-base, with the 45 hoods that have no 2012 baseline rendered in the established off-scale grey** — decided by Peter against measured alternatives (`ANALYSIS_BACKLOG.md` §10, run 2026-07-30). The bind is real: **relative change is undefined for 45 of 406 hoods (11%)** whose 2012 share is zero — Blatchford, Decoteau, Keswick, Glenridding Ravine, Rosenthal, the Anthony Henday segments — and **those are exactly the hoods a change map most needs to show**; but **percentage-point change, which is defined everywhere, does not separate hoods at all**: the median hood moves **−0.032 pp** against Downtown's **−1.791 pp** (**56×**), and **15% of hoods move under 0.01 pp in thirteen years**, so a pp choropleth is Downtown blazing over ~380 visually identical neighbourhoods. **Rejected: pp with a sqrt/rank transform**, because that rescales a metric which genuinely does not separate rather than fixing it — presentation papering over distribution, immediately next door to the locked linear-elevation honesty choice. **Also rejected: relative measured from each hood's first non-zero year**, defensible but it silently puts a 3-year and a 13-year change on one ramp. Cost accepted: 45 grey holes, i.e. **visible absence instead of a wrong number.** Two findings recorded with it, both of which killed an earlier recommendation: **peak-drawdown does NOT rescue a peaked series** (Spearman **+0.919** against net change — nearly the same ranking), and **endpoint vs OLS slope is +0.959 overall** but **+0.719 on the 34 hoods (8%) with a real hump**, so a hump needs a *second* number (peak value + peak year, which the panel already shows) rather than a cleverer single one. **Two windows are justified by measurement**: long vs short ranks at +0.734 and the sign **flips for 55 of 406 hoods (14%)**. | `docs/ANALYSIS_BACKLOG.md` §10, `TODO.md` |
| 2026-07-30 | **The change lens ships as a full-only SUB-MODE of Money (`#moneymode`) rendered as a FLAT diverging choropleth, not spikes or a sixth view button** (Peter). | `docs/SPEC_temporal.md` §6b |
| 2026-07-30 | **The change rate is COMPOUND, not arithmetic** — measurement reversed the obvious implementation; the arithmetic form is unbounded above and gave the diverging ramp arms 108x apart. | `docs/SPEC_temporal.md` §6b |
| 2026-07-30 | **Both degenerate endpoints of the change metric are off-scale holes with distinct stated reasons, and neither may read as set-aside land** — the no-baseline hoods are its opposite. | `docs/SPEC_temporal.md` §6b |
| 2026-07-30 | **Closed `TODO.md` items are ARCHIVED to `docs/TODO_archive.md`, leaving a one-line `## Done` entry** — TODO.md is read first every session and had grown to 1,839 lines, 40% of it closed work; the *never redo a closed item* rule still works by grepping `## Done`. Fixed tax 2,224 → 1,526 lines. | `tools/todo_archive.py`, `CLAUDE.md`, `docs/TOKEN_EFFICIENCY.md` |
| 2026-07-30 | **`docs/CODEMAP.md` is a generated symbol index for `web/index.html`, regenerated by a `PostToolUse` hook — NOT guarded by a staleness test** — a doc artifact must never be able to halt the data pipeline, and `pytest` is `refresh.yml`'s guard-before-regenerating step. The tests check the extractor against current source instead. | `tools/codemap.py`, `tests/test_codemap.py`, `.claude/settings.json`, `docs/TOKEN_EFFICIENCY.md` rule 5 |
| 2026-07-30 | **The verify suite is run through `tools/profiling/verify.js`, which wraps the 26 scripts rather than adding a `--quiet` flag to each** — 26 edits to 26 working tests to save output is a bad trade; classification is on `/^FAIL/` and exit code, never the `ALL CHECKS PASSED` banner. | `tools/profiling/verify.js`, `docs/TOKEN_EFFICIENCY.md` rule 7 |
| 2026-07-31 | **The temporal + change lenses are PROMOTED to the public build — a content-split TAG change, not a re-opened lock** (Peter: *"can we actually move those value change over time features to the public build"*, choosing **both**). The 2026-07-22 row locks the two-build *mechanism*; the content split is explicitly per-control and revisable, with precedent both ways (Uses → full-only marked *"provisional… may return to public"*; Transit **amended** an earlier release-scope lock). Cheap by construction: `temporal.json` (42 kB gzipped) **already ships to the public root**, and the full-only controls are **hidden, not stripped**, so the change is removing `FULL_BUILD` conjuncts at three gates — the fetch (panel + sparkline) and two in `syncChangeControls` (the change choropleth), which keeps the two halves independently promotable. **CORRECTED same day: NOT gated.** This row first named the 45-no-baseline-hoods hover copy as an open blocker, carried from S78. It is not open — S79 wrote it during the change lens's own build and it is live in `/full/` (tooltip `No 2012 baseline`; panel `... held none of the assessment base that year`; legend naming them *"the new-growth areas, NOT set-aside land"*, which satisfies the 2026-07-30 prohibition). The stale note was propagated into this index without reading the code — **the fourth carried item in this lineage whose stated cause had already expired**, and the reason the *reproduce before acting* rule applies to blockers, not just bugs. | `docs/SPEC_temporal.md` §6b, `TODO.md` |
| 2026-07-31 | **Mobile chrome step 3 (the left-edge clip) is CLOSED as NOT REPRODUCIBLE — no code change** — the S74 symptom (`#controls`/`#coloradj` at left −51, `#toggle` at −10) does not occur on current master in either build, in any visible view, with the Options pod unfolded; the widest rows sit at 177→371 on 390px. **Third time an open item's stated cause proved stale**, so the standing *reproduce and re-measure before acting* rule paid for itself again. Two measurement traps recorded because either one silently fakes a pass: `#optpanel` is `.folded` by default at ≤640px so its rows report `0,0` with **no layout box** (the folded state cannot show the clip), and the public build keeps full-only controls in the DOM but **hidden**, so selecting by presence hangs Playwright's `click()`. Headless Chromium measures text wider than the real stack, so a no-clip verdict there errs safe. | `TODO.md`, `docs/MOBILE_USABILITY.md` §3 |
| 2026-07-31 | **All three pre-existing verify failures were STALE TEST EXPECTATIONS, not app bugs — the suite is green at 26 scripts / 0 failures** — and all three failed the same way: **a test restated a value the app owns instead of reading it.** `verify-ind-permits.js`'s two failures were ONE cause, the window suffix: `state.devWindow` defaults to `"long"`, so the live columns are `*_per_acre_long` while the script hardcoded the bare names — the colour check therefore recomputed p97.5 over a *different distribution* and failed on a delta (`want 148,39,97 got 140,37,97`) that read like ramp drift for two sessions. `verify-glass-no-slider.js` probed straight after switching to Development and called it "the neighbourhood choropleth", but the 100 m grid is that view's DEFAULT (`devGrid: true`), so it was measuring the grid, where the slider is correct. `verify-coloradj.js` (found this day, never previously listed) asserted `#coloradj` is the last child of `#opt-body`, which S78's `#hoodmode` addition broke. **Fixes take the value from the app** — `devCol()`/`DEV_COLS` rather than literals, an explicit detail-mode click rather than the view default — so a future window or default cannot break them again; two checks were ADDED to keep the app-supplied column honest. ⚠️ **None of the three was visible on screen**, so a red verify script is evidence about the TEST as often as about the app: diagnose before fixing either. Also recorded: `verify-coloradj.js` went unnoticed from S78 to S79 **because S79's gate ran 3 of the 26 scripts** — the standing cost of a targeted gate. | `tools/profiling/verify-ind-permits.js`, `verify-glass-no-slider.js`, `TODO.md` |
| 2026-07-31 | **`tools/todo_archive.py` OVERWROTE the archive instead of appending — a data-loss bug in the tool `CLAUDE.md` tells every session to run — now fixed and guarded** — it built the archive from a hardcoded header plus the newly-closed items and wrote that, never reading the existing file. **Harmless on its FIRST run (S79, empty file) and destructive on every run after**: caught on 2026-07-31 when it took `docs/TODO_archive.md` from **757 lines to 81**, deleting 747 lines that are the ONLY copy of 19 closed items' reasoning (TODO.md keeps one-line stubs by design). Restored from git; the fix reads the prior file, splits at the header's closing `---`, and appends newest-moved-first. **The durable part is the guard, not the fix:** the tool now REFUSES to write if the result would be shorter than what is already there, because the append logic is exactly the thing that was wrong and a size invariant does not depend on it being right. ⚠️ **Generalisable: a first run proves nothing about an append-only tool** — the empty-file path and the has-prior-content path are different programs, and only the second one is ever run again. A unit test was deferred deliberately: the tool's paths are module-level constants, and making it testable means refactoring code this change was not asked to touch. | `tools/todo_archive.py`, `docs/TODO_archive.md`, `CLAUDE.md` |
| 2026-07-31 | **On touch, opening the history panel takes TWO deliberate gestures: a tap PEEKS into a card, and a tap on that card commits** (Peter: *"i actually want … the panel on mobile to be harder to activate … on mobile, you only [have] the choice of tapping"*). **Measured before designing**: on a phone the `.deck-tooltip` node never exists at all, so the desktop two-stage gesture (hover to preview → click to commit) genuinely collapses into one tap that opens the whole panel — tap a hood and it is up. `#peek` restores the missing stage and reuses `primaryRow`, so the card's headline and panel mode's reduced hover are **one definition** and cannot drift. **Prior art checked rather than asserted**: Google Maps, Apple Maps and Zillow all go pin → small card → full sheet, and the shared reason is that the *second* target is a big card instead of the small polygon that opened it. ⚠️ **Gated on `(hover: none)`, NOT on `max-width`** — this is the file's SECOND seam and it answers a different question from the ≤640px one: that block is about how much screen there is, this is about what is pointing at it (a finger needs a 44px target on a tablet; a mouse does not need one in a narrow window). ⚠️ **The gate is on the way IN only** — with panel mode already on, taps pin directly, since gating there would make pin-then-browse cost two taps a hood and the panel's own prompt says to tap a neighbourhood. ⚠️ **Every touch path is IDEMPOTENT rather than a toggle**: a touch tap can deliver `temporalClick` **twice** (touch event + compatibility mouse event, measured), so re-peeking the shown hood is a no-op and retap-to-unpin is restricted to pointers — a toggle fired at random. The card dismisses on an empty-map tap, deliberately the **opposite** of the pinned panel's inertness rule: that protects a surface you asked for, this is a cheap preview. **The × is doubled to 44.5 × 44** from a measured **20.3 × 21**, under half the 44px Apple HIG minimum. ⚠️ **This is the first verify script that drives a REAL pointer at the map** — all 26 others call `temporalClick()`/`openTemporal()` directly in JS, which is exactly why single-tap-opens-everything survived the whole temporal lens build unnoticed. | `web/index.html` (`openPeek`, `closePeek`, `temporalClick`, `noHover`), `web/styles.css` (`#peek`, the `(hover: none)` block), `tools/profiling/verify-peek.js`, `docs/SPEC_temporal.md` §2, `docs/MOBILE_USABILITY.md` |
| 2026-07-31 | **The touch gate is armed on EVERY tap, not just on the way in — and only `#hoodmode-btn` disarms it** (Peter: *"can we keep that behaviour consistent … they have to click for pop up, then click the peek card each time, unless they go and explicitly find that option to keep it open"*). This **reverses the *gate on the way IN only* clause locked earlier the same day**, and the reason is a hole in the original reasoning rather than a change of taste: that clause tested `state.hoodMode !== "panel"`, but **committing a peek card is itself what sets `hoodMode` to `"panel"`** — so one card commit silently bought permanent one-tap pinning, and the stray-tap problem the card was built to solve came back from the second hood onward. The test is now `panelByChoice`, set **only** by a deliberate press of `#hoodmode-btn`, so the mode a user *fell into* and the mode a user *asked for* are no longer the same state. **A different hood CLOSES the panel rather than swapping its contents** (Peter's call): the card is then the only readout on screen and never competes with a stale panel above it. An empty-map tap still dismisses the card only, leaving the panel alone — the S81 inertness rule is unchanged. | `web/index.html` (`temporalClick`, `applyHoodMode`, `panelByChoice`), `tools/profiling/verify-peek.js` |
| 2026-07-31 | **No hover tooltip renders on touch at all — `tooltipFor` returns null under `(hover: none)`** (CONFIRMED on Peter's phone). deck's `getTooltip` fires off a **hover pick**, and a tap synthesises one via the compatibility mouse event, so the full `.tip` box drew **on top of the peek card**, positioned at the finger and running **127px off the right edge** of a 390px screen with text cut mid-word. Nothing is lost by suppressing it: the card carries the same `primaryRow` line. ⚠️ **This contradicts an S81 premise that was true but too narrow** — "on a phone the `.deck-tooltip` node never exists at all" is correct about deck's *built-in* tooltip, but the app renders its **own** `.tip` via `className`, and that was never checked. ⚠️ **Found by EYE, in a screenshot** — every numeric assertion passed, and the id-based overflow table in `shot-mobile.js` structurally *could not* catch it because `.tip` has no id. That is why that script now sweeps for overflow generically, and why reviewing the render rather than the green banner stays a standing rule. | `web/index.html` (`tooltipFor`), `tools/profiling/verify-peek.js`, `tools/profiling/shot-mobile.js` |
| 2026-08-01 | **The peek card carries the LENS'S FULL READOUT, not just its headline — `viewTooltip(info, false)` renders the same rows the mouse gets, minus the heading the card prints itself** (Peter: *"when I click on stuff on mobile now the pop up doesn't appear"*). This is the **cost of the touch tooltip suppression locked the previous day**, surfacing one day later: suppressing `.tip` was right (it drew on top of the card, 127px off-screen) but it was the *only* multi-row readout a phone had, so every lens collapsed to one line — Services lost six rows, Uses lost its composition bar entirely. ⚠️ **This AMENDS the S81 "one definition" clause**: the shared definition is no longer `primaryRow` but `viewTooltip`'s body, which is strictly stronger — the card and the hover cannot drift apart in *any* lens rather than just in their headline. The sparkline and the *click to pin* hint that `tooltipFor` appends are deliberately NOT borrowed: `#peek-go` already invites the panel, and the panel draws the same chart full-size. ⚠️ **`.mixbar` was scoped `.tip .mixbar`**, so the Uses bar would have lost `display:flex` and collapsed to nothing visible — a borrowed body needs its borrowed CSS, and an id-based check cannot see it. `#peek-read .muted` was also lifted from `#6a6a80` to `.tip`'s `#b8b8c8`: most of a card's content is muted rows, and this is the one device read in daylight. | `web/index.html` (`openPeek`, `viewTooltip`), `web/styles.css` (`#peek-read`, `.mixbar`), `tools/profiling/verify-peek.js` |
| 2026-08-01 | **Money's readout is split by metric: revenue-specific rows no longer print under the assessed-value map** (Peter: *"I'd actually like pop ups to be segregated to each lens"*). `viewTooltip` was already per-*view*; what was shared wrongly was per-*metric* — the residential-revenue share and the revenue-per-road-metre row both rendered under Value, stating a revenue fact beneath a map of what land is *worth*. Both are now gated on `isRevenue(state.metric)`. **Value gains only a FULL-only `far` row**, and that asymmetry is a data fact, not an oversight: we ship no res/nonres split of assessed **value**, only of revenue, so there is no value-side equivalent to swap in. ⚠️ **`far` is `FULL_BUILD`-gated on the road rows' precedent** — the lens that explains FAR (Infill) is a full-only mode of Development, and publishing a number while hiding the lens that explains it is the exact mistake that gating exists to prevent. Consequence to accept: **Value's public readout is a single line** until the totals work lands. | `web/index.html` (`viewTooltip` money branch) |
| 2026-08-01 | **`#hoodmode-btn` CONFIRMS rather than toggling off when the panel was reached via the peek card, and the label carries a third state** (Peter: *"change button name and first press to mean yes, keep it open"* — **revises the same-day decision to leave it an honest toggle**). Arriving via the card sets panel mode with `panelByChoice = false`, so the button already read *"Readout: panel"* and pressing it turned the mode straight **off** — opting in to sticky one-tap pinning cost **two** presses on the device where presses are dearest. The button is now three-way, not a toggle: `popup → panel` (confirmed), `panel` fallen-into `→ panel ✓` (confirm), `panel ✓ → popup` (off). ⚠️ **The label needed the third state or the fix would be dishonest** — without the tick, the mode a user *fell into* and the mode they *asked for* look identical on the button while behaving differently on the next tap. ⚠️ **Implementation note: `applyHoodMode`'s early return had to learn one exception** — re-entering the mode you are in is still a no-op *except* when confirming, and the rest of the function is idempotent for that case, so it simply runs again rather than growing a second code path. | `web/index.html` (`applyHoodMode`, the `#hoodmode-btn` handler, the button `title`), `tools/profiling/verify-hoodmode.js` |
| 2026-08-01 | **The chrome-coverage figure that drove the mobile backlog for six sessions was INFLATED, and the priority it implied was wrong** — re-measured at Peter's request before deciding the bottom-sheet question. The 2026-07-27 method summed visible chrome rects **ignoring mutual overlap**, but the chrome **nests**: `#controls` contains `#views`/`#toggle`/`#optpanel`/`#layers` and `#botleft` contains `#legend`, so containers were counted two and three times. Counting each pixel once, the **default** state is **27.9% on a 390×844 phone vs 20.9% at 1440×900** — a ~7-point gap, not the ~18 that 45.1 vs 27.3 implied, and screenshot-confirmed as a quiet render. ⚠️ **The real problem is a STATE the aggregate averaged away: the UNFOLDED Options pod covers 54.3%**, over half the screen. So a bottom sheet, if built, should target the unfolded pod — the folded default needs nothing. ⚠️ **Generalisable: an overlap-blind sum over a NESTED DOM is not a coverage metric**, and any future claim here should quote the union and name the state it was taken in. **Fourth time a carried item's stated cause proved stale on re-measurement** — the *reproduce and re-measure before acting* rule paid for itself again, this time by shrinking a build item rather than closing one. | `docs/MOBILE_USABILITY.md` §2, `TODO.md` |
| 2026-08-01 | **Revenue-by-zone is computed from the zoning POLYGONS by point-in-polygon, NOT from `dkk9-cj3x`'s per-property `zoning` field — and it is NOT blocked on parcel geometry** (Peter: *"percent of city revenue, and top 3 revenue by zones"*). The per-property field is the obvious source and joins cleanly (1:1, no dupes), but it is **null for 35.7% of properties — 16.0% of citywide revenue and 42% of DOWNTOWN's**, because condo units carry no zoning; "top 3 zones" built on it showed *unzoned* as Downtown's largest entry. Every such property has coordinates, so a point-in-polygon against `zoning.geojson` places them: **11,022/11,022** of Downtown's, and citywide unplaced falls to **0.002%** (8 properties, $9,034). ⚠️ **The deciding argument is not coverage but CONSISTENCY**: those are the same polygons the Uses lens colours by area, so revenue-by-zone and the Uses composition are one map read two ways and cannot drift on what "commercial" means — `ZONE_CATEGORY` is reused rather than re-expressed. ⚠️ **This also refutes the assumption that the ask was gated on the ~$300 parcel-polygon blocker** in `PARCEL_LEVEL_OPPORTUNITIES.md`: that blocker is *polygons for parcels*, and this needs only per-property *attributes* aggregated to a neighbourhood. **`revenue_share_city` is computed in `main.py` against the FULL roll, before the boundary join** — deriving it downstream, or by summing served features in the browser, would renormalise over whichever hoods survived, so a hood's share would depend on who else made it onto the map. Fractions rounded to 1e-6: the 12 new columns cost **+4 kB gzipped rounded vs +33 kB unrounded**. | `src/revenue_by_zone.py`, `tests/test_revenue_by_zone.py`, `main.py`, `src/join_and_calculate.py` (`REVENUE_READOUT_COLUMNS`), `data/DATA.md`, `docs/ARCHITECTURE.md` |
| 2026-08-01 | **The mill-rate pod shows ALL THREE classes in every revenue cut with the billed ones lit, rather than listing only the relevant rate; and it YIELDS to the history panel rather than displacing it** (Peter: *"can we have the current relevant mill rates in the top left on this lens? since thats an open spot anyway"*). Showing all three keeps the **7.6254-vs-24.2229 differential** on screen — the fact the whole revenue map rests on, and invisible if two rows are dropped — and fixes the pod's height, which matters because the left column is **not** the open spot the brief assumed. **Both halves of that brief were wrong, and only measurement showed it:** the "~500px free at 1440x900" was measured with `#temporal` CLOSED, but the panel owns that column — it is **308px** tall (its own CSS comment says ~265) and clears the bottom-left cluster by 211px at 1440x900, **79px at 1366x768** and **31px at 1280x720**; and "top left is open" is false on two of the three cuts, because the residential/non-residential blurbs push `#title` from 196 to **256**. Peter's first ruling (push the panel down) was **reversed on the second measurement** — the push does not fit on the cuts the pod most needs to serve. The yield is a **CSS sibling selector, not a JS toggle**, so "pod is up" and "panel is up" cannot disagree; the pod's `top` is read from the **measured** title box each time, never a constant. ⚠️ That constant is exactly how the neighbouring bug happened: `#temporal`'s `top: 210px` was pinned from a sweep that covered the five views on the default metric only, and it paints over the blurb in **five** states by up to 289px (TODO.md). **Rates are published in `status.json` (`municipal_rates`) derived from `data/mill_rates.json`, never typed into the page**, and the Farmland caveat is driven by an `assumed` list so it stops printing by itself the year a real row is published — a literal would have gone stale silently. | `docs/SPEC_deployment.md` "Status manifest", `web/styles.css` (mill-rate pod), `tools/profiling/verify-millrates.js`, `TODO.md` |
| 2026-08-01 | **The mill-rate pod's phone form uses a DIFFERENT ANCHOR, not a different offset — and re-parents into the title card rather than hiding behind it** (Peter, on the desktop-only build: *"uh i see this on desktop, but no rates show on mobile"* → *"can't it be stacked, like bullet points almost, top left, where the description bubble would be. then folded in when you open the bubble"*). ⚠️ **"Under the title" is not a location on a phone:** measured at 390x844, `#title` collapsed is 20-43 but `#controls` owns **58-197** — the same fact that made `#temporal` a bottom sheet at this seam — so the pod hangs off **`#controls`** below 640px. Two more rules that do not carry over from desktop: the rates render **stacked, one per row** (the one-liner wraps at 360px and breaks between a class and its number, and the lit/muted split is the only cue for which rate the cut is billed at, so it needs a whole row), and the free-floating pod carries a **card background** the desktop form does not — a phone's map fills the screen, so the pod lands on the downtown prisms where 10.5px muted text is unreadable. **This was SEEN in a screenshot, not predicted.** "Folded in" is implemented as a **re-parent into `#title`**, not a hide: the expanded card is opaque at z-index 3, so an outside pod would simply be buried. ⚠️ **Generalisable: desktop chrome sits in a corner over dark map; phone chrome sits over the subject.** Bare text is a desktop affordance. | `docs/MOBILE_USABILITY.md` §2, `web/styles.css` (mobile seam), `tools/profiling/verify-millrates.js` |
| 2026-08-01 | **On a phone the mill rates are PART OF THE BLURB, not a surface of their own — the answer was not where to put the pod, it was not to have one** (Peter, after seeing a standalone card built to his own description of it: *"i don't like the independent mill rates panel. folding it into the tax revenue blurb is fine"*). `#millrates` is re-parented into `#title` below 640px, so the rates open and close with the description card and add **nothing to the default render**. ⚠️ **The discarded intermediate is the instructive part: every problem it had to solve was an artifact of it being an independent surface.** It needed an anchor, because "under the title" is not a phone location — `#title` collapsed is 20-43 but `#controls` owns **58-197**, the same fact that made `#temporal` a bottom sheet at this seam. It needed its own card background, because a phone's map fills the screen and the pod landed on the downtown prisms where 10.5px muted text is unreadable (**seen in a screenshot, not predicted**). And it inherited the desktop yield `#temporal.open ~ #millrates`, which shipped **ungated** under a comment claiming it was "desktop-only in effect" — reasoning about the LAYOUT (the panel is a bottom sheet on a phone, so they never overlap) and not about the SELECTOR, which matched everywhere; switching the readout to **panel mode** blanked the rates with nothing contending. Folded into the blurb, all three evaporate: no anchor, no background, and the yield is desktop-only **by construction** (a child of `#title` is not `#temporal`'s sibling) rather than by media query. ⚠️ **Generalisable: on a phone, ask whether a thing needs to be its own surface before asking where to put it.** The one phone-only rendering rule that survives is stacking the rates one per row — the desktop one-liner wraps at 360px and breaks between a class and its number. **The verify script asserts the BEHAVIOUR (rates survive panel mode, rates hide with the blurb), not the mechanism**, so it holds however a future form achieves it. | `docs/MOBILE_USABILITY.md` §2, `docs/CONTROLS_MATRIX.md`, `web/styles.css` (mobile seam), `tools/profiling/verify-millrates.js` |
| 2026-08-01 | **The Current / Change-over-time lens toggle moves out of the Options panel and becomes `#toggle`'s row 2 UNDER VALUE — and it is a different KIND of nesting from the revenue cuts** (Peter: *"move the current/change over time buttons to just be under Value. But I want them popped out in the same way the total/residential/non-residential buttons options are for revenue"*). `#revcut` nests under Revenue because the cuts are genuine **subsets** (`levy == res + nonres + farmland`, 2026-07-18); `#moneymode` nests under Value because the change lens measures movement in each hood's share of the **assessment base**, which is the value side — two *lenses* on one quantity, not a decomposition. **The old placement was the actual defect:** in `#layers` the toggle was offered under Revenue too, where it opened a lens reading a column Revenue does not own. **Exactly one row 2 shows at a time; `syncMetricButtons` owns both** and `syncChangeControls` now calls it (the temporal file lands after first paint, so `#moneymode` must default hidden in CSS — unlike `#revcut`). ⚠️ **Consequence that had to be handled, not a side effect: `#toggle` now STAYS VISIBLE in the change view.** It had been hidden there since the 2026-07-23 Money-scoping; hiding the pod that hosts the only way back to `Current` would strand the user in the lens. ⚠️ **Picking Revenue while the lens is up must LEAVE it** — Revenue owns no change lens, so without that the pod would show Revenue lit over a map still drawing share-of-base movement. `#chgwindow` (Since 2012 / Since 2019) **stays in Options** (Peter's call) — `#toggle` stays strictly two-level, so a third row was refused. **Mobile gained a tap and lost a hunt:** reaching the lens was unfold Options → find "Lens" → tap; it is now Value → Change, both on the map surface, and the pod still fits one row at 390px. | `web/index.html` `#toggle`/`#moneymode` + `syncMetricButtons`/`syncChangeControls`/`applyView`, `web/styles.css`, `docs/CONTROLS_MATRIX.md` §1/§3/§4, `tools/profiling/verify-change.js` |
| 2026-08-01 | **On Money's REVENUE metrics the pinned panel shows the hood's zone-revenue breakdown INSTEAD of the assessment history — one element, two modes** (Peter: *"under revenue, the panel still pops up with the assessment over time graph. For revenue I want the panel to just be the top contributing zones by percent of hood revenue"*). Completes phase 2 of the revenue-lens readout; phase 1 shipped the columns 2026-08-01. **`#temporal` gains a mode rather than gaining a sibling**: it already owns the three dismissals, the `CHROME_IDS` label-sweep exemption, the phone bottom-sheet form, `#hoodmode` and the peek card's commit path, so a second panel would have duplicated all of it. `openTemporal` branches to `renderHistory` or `renderRevenueMix`. **The history is not lost — it moved to Value, which is what it describes**, and the Change lens still reads it; this lines up with the same-day `#toggle` regroup, so the *panel* follows the quantity exactly as *row 2* does. **Peter's three calls:** ALL non-zero categories ranked (not top 3 — the panel has the room a tooltip doesn't, so the rows sum to 100% with no unstated remainder); shown on the Residential/Non-residential cuts too **with the denominator NAMED**, since `rev_frac_*` are shares of the hood's TOTAL levy while those cuts colour one class of it (the house rule from §6 — an unnamed denominator is how a correct number reads as wrong); and the header keeps `total_revenue` + `revenue_share_city`. ⚠️ **The categories are `USE_CATEGORIES`' OWN, with the column DERIVED as `"rev_" + u.frac`** rather than listed again — that is what stops the Uses lens's area shares and this panel's revenue shares drifting onto two category sets. ⚠️ **Three surfaces advertise this panel and all three are lens-dependent now** (`#peek-go`, `#temporal-hint`, the tooltip's `click to pin` ⇄ `click for the revenue mix`) — a teaser for the wrong panel is the failure mode. ⚠️ **A pinned panel re-renders on any lens change** (`syncPinnedPanel`, from `applyMetric`/`applyView`/`applyHoodMode`/init): a revenue breakdown left under a value map is a silent-correctness failure, so the safe branch closes rather than leaves it. ⚠️ **`fmtBig` was NOT reused for the levy** — it is calibrated for assessment totals ($10M–$10B) and rounds megas whole, printing a $1,876,137 levy as **"$2M"**, a 7% error on a fiscal headline; `fmtLevy` keeps two decimals. Degrades to the history panel when the columns are absent. | `web/index.html` `openTemporal`/`renderRevenueMix`/`syncPinnedPanel`/`REV_CATEGORIES`/`fmtLevy`, `web/styles.css` `#revmix`/`.revrow`, `docs/SPEC_temporal.md` §2, `tools/profiling/verify-revenue-panel.js` |
| 2026-08-02 | **The Display popover clears the Data & Methods pod by MOVING, not by winning a z-index contest — and both the reported direction and the suspected cause were wrong** (Peter, 2026-07-28: *"opening Display covers the Data & Methods button"*). ⚠️ **Direction reversed on measurement:** equal `z-index` falls back to DOM order and `#about` is later, so the **button paints over the MENU**, truncating *"Landmarks & nearby pla⌷es"* — found in a screenshot, confirmed by `elementFromPoint`. ⚠️ **Cause was not the z-index asymmetry** the item had flagged for six sessions: the two pods are a **stack** in one column (`#a11y` `bottom:40px`, `#about` `bottom:68px`, both buttons 26px), and `#a11y-menu` was anchored to its **own** button's top (`calc(100% + 6px)`), ignoring the sibling above it — both offsets fixed, so the ~23px collision was identical at 1440x900, 390x844 and 360x780. Fixed with `bottom: calc(200% + 8px)`, which tracks the shared button styling (own height twice + the 2px inter-pod gap + the 6px the menu wanted) rather than hardcoding 60px. ⚠️ **THE SUSPECTED FIX WAS FALSIFIED BY APPLYING IT, AND WAS WORSE THAN THE BUG:** `#a11y.open { z-index: 5 }` paints the menu over the button, and `verify-about.js` then TIMES OUT — the *"Landmarks & nearby places"* label **intercepts pointer events** and the Data & Methods button becomes **unclickable**. A visual defect traded for a dead control; caught only because that script drives a real `page.click()`, where a JS `.click()` bypasses `pointer-events` and would have passed. ⚠️ **Consequently the new checks assert GEOMETRY (no overlap), not paint order** — a "menu is on top" assertion passes for the z-index version, which is the outcome to reject. **Fifth time a carried item's stated cause did not survive reproduction**, and the first where the recorded fix would have caused a worse bug — the *reproduce before fixing* rule earned its keep on the hypothesis, not just the symptom. | `web/styles.css` `#a11y-menu`, `tools/profiling/verify-about.js` (44 → 50 checks), `TODO.md` |
| 2026-08-02 | **The temporal lens's LIVE YEAR is asserted by DERIVATION, not by a pinned literal or a band — and the front-end check now agrees with the pipeline guard instead of contradicting it.** `verify-temporal.js` had been red since the 2026-08-01 auto-refresh, pinning Downtown at 3.30% / 9.24% / $7.84B. **Measurement settled the item's own question — the data moved, the splice did not:** diffing the served `temporal.json` at `ab8bac7` against `4466fbf` across all 406 hoods and all three series gives **839 changed cells, every one of them in the 2025 column**, with 2012–2023 **bit-identical**, the hood set unchanged and the shares still conserving to 100%. Citywide 2025 value rose 0.296% while Downtown's fell 0.32%, which is exactly why its share moved 3.2991% → 3.2788%. ⚠️ **The defect was in the script, and `scripts/check_temporal_years.py` had been right all along** — it ran on that refresh and **passed**, and its docstring pre-registers the movement: *"The live year is NOT pinned to a band. It is a live snapshot that genuinely moves week to week … so a pinned band would cry wolf continuously."* A front-end **equality** on that same quantity was cry-wolf by construction and would redden after every roll update. **Deriving is chosen over the standing *pin bands, not equalities* rule** (Peter's call): a band still needs a width guess and still drifts, while deriving from the loaded series cannot cry wolf **and** additionally fails when the panel renders the wrong hood, series or index — which a band waves through. Historical anchors (2012 5.09%, peak 5.55% in 2016) stay **pinned tight**, because those are what prove the archive half of the splice held. ⚠️ **Compare PARSED NUMBERS, never a string built with the page's own formatter** — building the expectation with `fmtBig`/`fmtPct` would have made the previous session's `fmtBig` precision bug invisible; tolerance is one display ulp (0.006). ⚠️ **Falsified all three, per the standing rule:** an off-by-one live index fails 3 checks, the commercial slot rendering `share` fails 1, and `fmtBig` dropping a decimal (**"$8B"**, the S85 bug shape) fails 1. ⚠️ **The item's own premise was false and is corrected in place** — it claimed the temporal file had no equivalent of `check_value_anchors.py`; the guard exists and runs in `refresh.yml`. **Sixth time a carried item's stated cause did not survive reproduction.** Left open as a proposal: a data-only refresh still triggers no deploy and runs no front-end check, so the data side is guarded and the *render* is not. | `tools/profiling/verify-temporal.js` (42 → 43 checks), `scripts/check_temporal_years.py`, `TODO.md`, `docs/TODO_archive.md` |
| 2026-08-02 | **`#temporal` is placed from the MEASURED `#title` and `#botleft` boxes, and where they leave no room the PANEL scrolls rather than the blurb.** The panel had been pinned at the constant `top: 210px` from a sweep across the five views on their **default metric**, where `#title` is 176–179px tall; the real state space runs **140–499px**, so the panel buried the blurb in five states by up to **289px** (money's two revenue cuts 46px, the change lens 158px, Development 252px, Infill 289px). ⚠️ **Which element yields REVERSED during implementation, on a finding, and Peter re-ruled on it:** the call was to cap the blurb, but `#title` carries `class="panel"` and `.panel` sets **`pointer-events: none`**, so a capped scrollable blurb **cannot be scrolled to** — it would hide text with no way to reach it, and giving `#title p` pointer-events:auto would steal a ~360×380px region from map dragging (the exact failure `#botleft`'s own comment records having caused once). `#temporal` is already pointer-events:auto and was opened deliberately, so it is the one that yields. ⚠️ **TWO DEFECTS WERE FOUND BY MEASURING THE FIX RATHER THAN ASSUMING IT, and neither was visible in a passing check:** `max-height` is **content-box** by default, so it excluded 21px of padding + border and the panel overshot `#botleft` by exactly that minus the gap — **11px in every state that capped**, including two that never scrolled (fixed with `box-sizing: border-box`, width 300 → 328 to keep the content box identical); and **an absolutely-positioned close button inside a scroll container scrolls away with the content**, taking the hood name with it in Infill — the two things you need in order to *use* a scrolled panel — hence the new `#temporal-body` wrapper. ⚠️ **THE CHECK WAS THE OTHER HALF OF THE BUG:** `panel clears the title` was green throughout, because it only ever ran on money/value, the same narrow case the 210px constant was measured from. It sweeps **six states** now. ⚠️ **The item's own numbers were re-measured before anything was touched and came back IDENTICAL** — the first carried item this session whose figures were not stale — but two of its *claims* were: it is no longer only "the history panel" (two of the five states show the revenue-mix panel, ~15px shorter), and "moving the panel below the blurb does not fit" generalised Infill's failure to all five when it fits in three. **Residual left open:** below ~768px tall Development and Infill hit the min-height floor and reach into `#botleft`, because at 1280×720 Infill's blurb is 479px of a 720px screen and the column has 36px free — blurb length is the only lever, and that is a content decision. | `web/index.html` (`syncTemporalPos`, `#temporal-body`), `web/styles.css` (`#temporal`), `tools/profiling/verify-temporal.js` (43 → 67 checks), `TODO.md` |
| 2026-08-02 | **The stylesheet link is cache-busted at build time with a CONTENT HASH, not the commit sha — and the fix is deliberately scoped to stale-CSS-under-fresh-HTML rather than sold as "the deploy always shows up"** (Peter, 2026-08-01, on a phone after a good deploy: *"i'm still not seeing the mill rates on mobile… i can see it when i open it in a private window on my phone. but it's refusing to show on normal safari."*). `scripts/build_site.py` stamps `styles.css?v=<8 hex of sha256(styles.css)>` into **both** emitted builds. The report was created by the 2026-07-29 CSS extraction: a CSS-only change now ships in a file with **its own cache lifetime**, so a stale stylesheet can render against a fresh page and look half-deployed. **Content hash over commit sha** because a sha changes on *every* deploy — including data refreshes that never touched the CSS — discarding a working browser cache for nothing; it also needs no git at build time, so the token is deterministic and unit-testable. **Query string over a hashed filename** (`styles-abc123.css`): the renamed form is stronger against proxies that strip queries, but it would make the built tree's paths diverge from `web/`, and *the dev server serving something different from the build is already this project's known bug source* — quirk (rrr), which is exactly why PR #141 existed. The query keeps `web/styles.css` the one real path. **Measured before building, not assumed:** Pages returns **200** for `styles.css?v=deadbeef`, so the query carries none of the 404 risk flagged in `SPEC_temporal.md`. **Both builds get the SAME token** — `/full/` resolves the link through its `<base href="../" />` to the root's copy of the very file hashed, verified by driving both builds and watching the response URL come back as the *root* path. ⚠️ **THE LIMITATION IS RECORDED RATHER THAN GLOSSED:** this cannot help when the browser holds `index.html` itself stale, because then it holds the old query string too and gets the old CSS **by construction** — `index.html` is `max-age=600` and cannot bust itself. RUNBOOK §3c therefore *keeps* its private-window step; what changed is that a correctly reloaded page can no longer pair with an old stylesheet. **Drift fails the build loudly** (`subn`, `n != 1` → `SystemExit`), the same shape as the `DEFAULT_BUILD` guard, and a source-side test pins that `web/index.html` carries exactly one bustable link so a rename stops CI rather than silently shipping an unbusted page. | `scripts/build_site.py` (`css_token`, `cache_bust`), `tests/test_build_site.py` (5 → 8), `docs/RUNBOOK.md` §3c, `TODO.md` |
| 2026-08-02 | **The weekly refresh is gated on a RENDER smoke check, and every assertion in it is an invariant or is derived from the served file — nothing is pinned to a data value.** `refresh.yml` downloads, regenerates, commits AND deploys in one run; its four guards (`check_unmatched_names`, year alignment, `check_value_anchors`, `check_temporal_years`) are all DATA-side, and a `grep` for node/playwright/verify across both workflows returned zero — so until now a data change could alter what the site renders with nothing on fire. ⚠️ **The item's own premise was wrong and is corrected in place:** it said a data refresh "triggers no deploy". True of `deploy.yml`, but **`refresh.yml` deploys itself** (build_site → upload-pages-artifact → its own deploy job) — the gap was not a missing deploy, it was an *unchecked* one. **Seventh time a carried item's stated cause did not survive reproduction.** ⚠️ **"Run the existing suite weekly" was REFUSED and #139 is the proof**: those 29 scripts carry literals calibrated to a data snapshot, and `verify-temporal.js` went red on the 2026-08-01 refresh for exactly that reason. A weekly false alarm gets ignored, which is strictly worse than no check — so the design question was never *which scripts run*, it was *what can be asserted about a render whose data legitimately moves every week*. Answer: load integrity (no 4xx / console error / page exception), shape **derived from the served files** (feature count, chart points vs the file's own year list, required columns from `METRICS`' and `USE_CATEGORIES`' own keys), a garbage sweep (`NaN`/`undefined` in any of ~400 hoods × 8 lenses), and provenance **as agreement** between manifest and render. **Gated BEFORE `upload-pages-artifact`** so a red check leaves the live site serving the previous good render. ⚠️ **THE FALSIFICATION FOUND A HOLE IN THE CHECK ITSELF, and it is the project's cardinal failure shape:** renaming `revenue_per_acre` did NOT trip the garbage sweep, because `viewTooltip` guards every row with `!= null` and therefore **omits the row rather than printing NaN** — a silently dropped fact, invisible to any check looking for garbage. Fixed by `B6`, which asserts every `METRICS` column is present on every feature, derived from the config rather than listed. ⚠️ **THE INVERSE TEST CAUGHT THE CHECK CRYING WOLF, which is the reason it exists:** replayed against pre-refresh data (`4466fbf`), the first draft hard-failed because `municipal_rates` was absent — from before the pod shipped. Whether rates are *published* is a reviewed-input question that can legitimately lag a January roll, so the provenance family now asserts **agreement in both directions** (rates present ⇒ pod shows them; rates absent ⇒ pod shows none) and never the manifest's content. ⚠️ **Two checks are self-consistent against data mutation and their comments now say so rather than overclaiming:** the chart-points check catches the chart disagreeing with the data it was handed, not `temporal.json` losing a year (both sides move together — that is `check_temporal_years.py`'s job, which holds an archive); the mill-rate check catches the pod drifting from the manifest, not a wrong rate in it. **Drives no real pointer** (view switching goes through `applyView`/`applyMetric`), so it cannot inherit the concurrency flake — a gate that blocks the weekly publish spuriously would be worse than the gap it closes. **Runs against BOTH builds**, the failure PR #141 existed to fix. | `.github/workflows/refresh.yml`, `tools/profiling/verify-smoke.js`, `docs/RUNBOOK.md` §2, `TODO.md` |
| 2026-08-02 | **The transportation lens is modelled DOLLARS and a GROUP INSIDE Services — not a supply grouping, and not a new top-level view** (Peter, three choices in one pass: *"gather all the transport costs for people to see. so roads will go in there. transit. but also bike lanes?"*). Roads and transit already existed as Services rows, so the ask was never "build two lenses" — it was to make transport read as one thing and add the missing third. **A new view was rejected as duplication:** Services already owns the shared plane, the per-service checkboxes and the colour-driver radio, so a Transportation view would re-implement all of it and add a full row to the CONTROLS_MATRIX state space to show the same three services. The group captions are **labels only** — they gate nothing, carry no controls, and a caption whose rows are all data-gated away hides itself. Services stays FULL-only (the 2026-07-28 lock is untouched). | `docs/SPEC_services.md` "Transportation lens" |
| 2026-08-02 | **Bike supply counts DEDICATED cycling assets only — shared roadways, walkways and coming-soon routes are excluded, and the exclusions ARE the metric.** Of 10,417 segments in `vd4b-a4iv`, only 4,573 (981 km) count. **The two exclusions that matter are silent-correctness bugs waiting to happen:** "Shared Roadway" (280 km) is an ordinary street carrying a bike-route designation — no new asset, and `load_roads` **already counts those metres in `road_m_total`**, so including them double-counts the road network against itself; "Walkway / Breezeway" is **3,031 rows of pedestrian path**, the second-largest classification in the feed, so a naive "sum the bike routes" metric is ~50% not-bike by length. Unmatched classifications default to **EXCLUDED**, the opposite of `load_roads`' default-to-local: that feed is all roads so an unknown row is still a road, this one is mostly not bike infrastructure so an unknown row would silently INFLATE a supply metric. ⚠️ **The metric is 82% off-road pathway and peaks in river-valley set-aside hoods that generate almost no revenue** — the map's top four are all grey/off-scale, and the blurb says so. | `data/DATA.md` §15, `docs/FINDINGS_revenue_scale.md` §6.9 |
| 2026-08-02 | **The cost side publishes DISJOINT per-term columns, and the composites are named sums of them — because `svc_cost_per_acre` already contains roads.** A `transport_cost_per_acre` that also contained roads would leave two overlapping cost columns that cannot be read side by side, which is the same double-count the bike scope decision above avoids one level down. So: `cost_roads/transit/bike/fire_per_acre` are the terms, `transport_cost_per_acre` = roads+transit+bike, and `svc_cost_per_acre` keeps its **exact current value and definition** (roads+fire), computed from the terms — no consumer breaks, the Ratio view's "Per service $" denominator included. The transit term is the fire pattern exactly: allocate the annual budget by share of citywide scheduled departures, dividing by the pipeline's own citywide total, and carry the **demand-allocation-of-a-fixed-budget** caveat. **STAGE 2 IS BLOCKED on two manual reviewed inputs Peter must source** (ETS operating budget, bikeway $/m/yr — the mill-rates pattern; edmonton.ca is unreachable from the Oracle box). Both keys stay optional and the composite is **all-or-nothing** across its three terms: a two-term metric labelled "transportation" would be mislabeled. | `docs/SPEC_services.md` "Transportation lens" Stage 2 |
| 2026-08-03 | **The orientation layer's roads move from the hand-extracted Anthony Henday to the OSM motorway/trunk network, and the neighbouring municipalities gain OUTLINES** (Peter: *"additional roads as geography features… all the main highways, and having them extend out of the city a bit, like the river currently does. Also can we not have some of the county or area boundaries"*). `t="henday"` is **retired and replaced by `t="highway"`**; `t="boundary"` is new. **The requirement is the reason for the source change:** the City centreline feed does carry every main highway, but as a *City* feed it stops at the municipal boundary — the amputated look `MARGIN_M` already exists to prevent for the river. Clipping OSM to the same 60 km box puts **68% of the highway length outside the city**, extending past it on all four sides. ⚠️ **Alberta's own `transportation/highways_public` MapServer was the obvious choice and is a TRAP: it answers HTTP 200 with 510 features and NULL GEOMETRY on every one**, in both formats, with and without an envelope, while still advertising `Query` in its capabilities — a reader trusting the feature count emits an empty layer and logs success. **Retiring the Henday extractor also retired its tests** (mainline allowlist, Highway 14 concurrency, spur pruning, ring closure — 10 tests): OSM needs none of it, and the ring-closure invariant is now actively WRONG, since the new layer is deliberately 89 open-ended corridors running off the clip edge. What replaced it is a rendered-geometry assertion — the highways must extend past the city on all four sides — falsified by clipping them back to the city limit. Boundaries draw **under** the data with the river: the seven places sit outside Edmonton (0–0.7% overlap with the hood fabric), so underneath they are fully visible and can never cut across a prism. Outline only, no fill — seven grey slabs would compete with the choropleth. **OSM is ODbL, so the credit sits in BOTH builds**, unlike the full-only City service credits. | `data/DATA.md` §14, `scripts/build_reference_layers.py`, `tools/profiling/verify-reference-layer.js` |
| 2026-08-03 | **A dropped column is caught by a COMMITTED SCHEMA BASELINE on the data side, not by the render gate — because the render gate structurally cannot catch it, and the backlog item's own prescribed fix passed the failure it was written for.** The item (opened 2026-08-02 with the bike lens) asked to mirror `B6` onto `SERVICES`: *"present on every feature OR absent from every feature, never partially."* ⚠️ **That rule tolerates the exact defect it names.** A refresh that silently drops a service column drops it from **all 406** features — which is "absent from every feature" — and falsification F2 confirms it: with `bike_m_per_acre` deleted from every feature, `B7` passes green. ⚠️ **The tolerance is not removable either**, which is what makes this structural rather than a bug in the rule: `bike_m_per_acre` was *legitimately* absent for precisely that reason between the #145 merge and the refresh that followed, so failing on absence would have redded the weekly publish over a column that was not supposed to exist yet — the cry-wolf failure `verify-smoke.js` exists to avoid (#139). **Telling "dropped" from "not shipped yet" requires memory of last week's schema, and nothing derived from the served file can have it** — the same self-consistency limit already recorded for `B5`/temporal.json and the mill-rate pod: data and check move together. Hence `scripts/check_served_columns.py` + `data/expected_columns.json` (62 columns), which fails the refresh on a removal or a partial write and only *warns* on an addition — columns get added constantly here, and a guard that blocks the publish on every new metric gets switched off within a month. **Deliberately NOT folded into `check_value_anchors.py`**, the obvious host: that script's whole contract is numeric bands with a benign/dangerous direction, and it **skips wholesale** when the property-info CSV is absent — a schema guard inside it would silently disable itself on exactly the degraded runs where a missing metric matters most. For the same reason this one does **not** skip in ground-acre-only mode: legitimate locally, not for a publish. **The render side still gained the two checks worth having**: `B7` (all-or-nothing presence, catching the per-hood partial case) and `B8` (a services row is offered exactly when its column is present — falsified in *both* directions: a gate disabled over an absent column reports "offered but absent", a gate forced on over a present one reports "gated off but present"). ⚠️ **The column list is derived from the UNION of `SERVICES[].plane.col` and `RATIO_DENOMS[].col`** — the item assumed `SERVICES` was the whole list, but **Roads is a ground layer with no `plane.col`**, so `road_m_per_acre` appears only in the ratio picker; taking one config would have left it unguarded and the Ratio view underived entirely. **Eighth time a carried item's stated cause or fix did not survive reproduction**, and the second where the recorded fix would have shipped a guard that could not fail. | `scripts/check_served_columns.py`, `data/expected_columns.json`, `tools/profiling/verify-smoke.js` (`B7`/`B8`), `.github/workflows/refresh.yml`, `docs/RUNBOOK.md` §2, `docs/ARCHITECTURE.md` |
| 2026-08-03 | **The verify runner's concurrency is DERIVED FROM CORE COUNT, not a hardcoded 3 — because one script already needs ~2.75 of this box's 4 cores, so the suite's own default was manufacturing the flake it had a documented workaround for.** Quirk (mmm) — *"real-pointer scripts cannot share the box; a red batch result is a hypothesis, not a finding"* — had been live since 2026-08-02 and was being paid for by hand, by re-running every red script alone. ⚠️ **The cause was never the scripts.** Measured on the 4-core Oracle ARM box: a **single** verify script draws **~275% CPU**, because headless Chromium runs on SwiftShader where all rasterisation and every deck.gl picking-buffer readback is software. The default of 3 therefore demanded ~8 cores from 4 — observed `cpu_sum` **385–400%** (every core pegged) and load average **8.2**. ⚠️ **Parallelism was buying ~17% of wall time in exchange for determinism**, measured on the same three scripts: `--jobs 3` ~509s with **2 of 3 failed**, `--jobs 2` ~505s with **1 of 3 failed**, `--jobs 1` 615s **all green** — so even 2-up oversubscribes, and the honest setting on this hardware is serial. The trade is worse than the numbers suggest, because a genuine regression is indistinguishable from the flake: three scripts went red on PR #148, which touched none of them. **The wall time is dominated by one script either way** — `verify-peek` spends ~437s of the 615s software-picking a pixel grid, which is now its own open item. ⚠️ **The scripts had already accreted in-code workarounds for this**: `verify-peek.js`'s picking-buffer re-send names *"the 3-up runner"* explicitly as the reason it exists. The runner default was the upstream cause nobody had gone back to. **Derived rather than pinned to 1** so the setting stays right on other hardware (`floor(cores / 3)` → 1 here, 2 on an 8-core, 4 on a 12-core), with the divisor rounded **up** from the measured 2.75 to stay conservative on slower cores; `--jobs N` still forces any value. ⚠️ **`verify-temporal.js`'s hand-set `page.click(..., { timeout: 4000 })` was deliberately LEFT ALONE** — it is the only such literal in the suite and the one that visibly failed, but it is a fail-fast choice for an unclickable control and it passes at the new default. Raising it would have been treating the symptom of a cause already fixed. **Not a CI change:** `refresh.yml` runs `verify-smoke.js` directly and sequentially; `verify.js` is a local runner only. | `tools/profiling/verify.js`, `TODO.md` |
| 2026-08-03 | **The transportation cost lens ships on an OPERATING basis, because the "bikeway lifecycle $/m/yr" figure that unblocked it was not a lifecycle figure — and the arithmetic that proves it was available without leaving the repo.** A relayed research brief supplied $178/km/yr as the Stage 2 bikeway unit cost, marked *"primary-sourced… safe to hardcode"*, on the reading that the City's phrase *"replace, repair, and maintain"* bundles amortized capital into a blended annual rate. ⚠️ **REJECTED, and the rejection is arithmetic rather than judgement:** (a) the brief's own §6 derives it as ~$0.27M/yr spread over ~1,500 km, which cannot be *replacing* that network; (b) the same brief puts snow clearing on the same network at $20,100/km/yr — **113× the supposed all-in build-and-replace rate**; (c) at a 50-year life it totals **$8,900/km** for construction plus all replacement, less than paving one kilometre of anything; (d) against the City's own ~3%/yr set-aside rule — already sitting in `city_unit_costs.json` as the primary source behind the $50/m/yr roads figure — it is **~0.09%/yr, ~33× low**. It is an operating-maintenance line. ⚠️ **The failure mode this avoided is the project's cardinal one:** against the repo's existing primary-sourced $50,000/km/yr roads figure, hardcoding $178 would have published bike infrastructure at **1/281st the cost of roads per metre** as the lens's headline number. The brief also had the asymmetry backwards — an all-in bike rate against an ops-only road rate makes roads look relatively *cheaper*, not more expensive. **So the basis moved instead of the number**: all three terms use maintenance + snow with no capital, which is internally consistent and fully primary-sourced, and **a true bikeway lifecycle figure stays OPEN** (needs a primary edmonton.ca capital-$/km + service life, derived the way `roadway_om_renewal` was). ⚠️ **Every column carries an `_ops` suffix** because `cost_roads_ops_per_acre` ($4.635/m/yr) and the roads term inside `svc_cost_per_acre` ($50/m/yr) are the **same metres ~10.8× apart in the same served file**; a test pins them apart and `city_unit_costs.json` opens with a `_two_bases` warning. **DATS is excluded** from the ETS numerator ($31,966K of $468,571K) — it is door-to-door and generates no scheduled stop-events, so allocating it by departures would charge paratransit cost to any hood that merely has a bus. **No bus/LRT mode split is needed**: a combined budget over combined departures is exactly right, so the brief's peer-benchmark 1.9× LRT ratio, its CUTA/FOIP dead ends and its warning against a $/track-km metric were all answering a question the locked design never asked. **Both rate/denominator mismatches recorded rather than absorbed** (bike snow blends over a 1,500 km network that also covers pedestrian squares, bus stops, LRT platforms and staircases; road snow blends over 11,000 km including priority-cleared arterials). ⚠️ **Ninth time a relayed or carried claim did not survive reproduction**, and the second where the recommended action would have shipped a confidently wrong number. | `data/city_unit_costs.json` (`_two_bases`, `roadway_ops`, `bikeway_ops`, `transit_ets`), `src/join_and_calculate.py` (`load_unit_costs`), `docs/SPEC_services.md` Stage 2 |
| 2026-08-03 | **The three transportation cost terms ship as THREE ROWS and the composite gets NO row — because driving the real pipeline showed the composite is 90.8% transit at the median.** The originally locked design (2026-08-02) called for `transport_cost_per_acre` = roads + transit + bike as the displayed metric. Built and measured against real output: transit **$2,808/acre/yr** (90.8% of the composite), roads **$151** (4.5%), bike **$109** (3.6%) — citywide ~$436.6M vs ~$16.9M vs ~$19.7M. ⚠️ **The cause is a real asymmetry, not a scaling artefact: ETS's budget pays to RUN a service** (drivers, fuel, vehicles) **while the roads and bike figures only MAINTAIN an asset** — the City never pays for the cars, so the operating half of road transport never enters its budget at all. Both are true statements about City spending, but a map coloured by the sum is a bus-service-density map, and a row labelled "Transportation" fails the same test the all-or-nothing rule already encodes: *a metric labelled "transportation" that is 91% one term is mislabeled.* **Peter's call: three per-term rows under their own "Transportation cost — operating" caption; the composite still ships as a column for anyone reading the data, with no UI row.** What the composite was burying is the actual finding — **a bikeway metre costs 4.4× a road metre to operate** ($20.278 vs $4.635), cheap to maintain and expensive to clear, so bike lands at a cost per acre comparable to roads despite carrying ~6× fewer metres. ⚠️ **Colour transforms INHERIT rather than being re-derived**: each cost column is a positive scalar multiple of its supply column, so skew is unchanged (measured identical to 3 dp) — roads linear, transit and bike sqrt; the FINDINGS §6.9 procedure does not need re-running for a rescaling. ⚠️ **The per-departure figure is a SHARE, not a rate** — an annual budget over a mean-weekday count, meaningless as a unit but exact as an allocation; a test pins that the terms sum back to the budget, against a future "unit fix" that multiplies every hood by ~250 weekdays. **`verify-transport-cost.js` (41 checks) caught a real disclosure gap the blurbs had missed** — the transit blurb never said operating-only, though ETS capital (trains, garages, line extensions) is excluded. | `web/index.html` (`SERVICES.roadscost`/`transitcost`/`bikecost`, legend branches, `primaryRow`), `tools/profiling/verify-transport-cost.js`, `docs/SPEC_services.md` Stage 2 |
| 2026-08-03 | **The citywide budget-scale stat lives in the Data & Methods pod, publishes DOLLARS ONLY, and its ratios are derived in the page — because the ratios it arrived with were wrong.** Peter's placement call among an existing surface, a new pod and docs-only: the `#about` popover already carries vintages, licence and caveats, is collapsed by default so it costs no screen space, and this is context-for-reading-the-map rather than a finding of its own — which also answers `TODO.md` D5's standing *"where does a non-map panel live in the UI?"* with an existing surface instead of inventing one, and adds no chrome to a stack that already had a collision bug and an open mobile concern. ⚠️ **The supplied headline was arithmetically wrong and the table it sat on was right**: the brief claimed *"transit is roughly 15× the road ops budget and roughly 90× the entire active-transportation network's ops budget"*, but its own numbers give **9.2× roads, 15.4× active transport, 79.4× sidewalks** — the ratios had slid one row. Only the *"bike lanes and sidewalks together are under 1%"* claim survived (0.94%). **Hence `budget_context()` publishes values and a total and nothing else**, the UI divides, and `verify-about.js` recomputes all four shares independently and compares them to what is printed — a pinned share is precisely the artefact that failed here. ⚠️ **Kept off the spatial pipeline by construction:** these road figures span the ~11,000 km network **including arterials** while the neighbourhood lens excludes them, so the two must never share a constant; nothing in `src/` or `main.py` reads the file and the pod copy says "citywide" for that reason. **The $14.135M roads-maintenance component is flagged `derived` and asterisked** — it is $1,285/km × ~11,000 km, where that denominator is the *snow-clearing* network reused for maintenance. **DATS is IN this table and OUT of the per-acre transit column**, which is correct for two different questions and documented on both sides. ⚠️ **TWO LAYOUT DEFECTS FOUND ONLY BY DRIVING IT, neither reachable by any test:** the first version drew a proportional bar by repurposing `.revrow`'s 9px swatch, which squeezed the label column, wrapped every row and made the section **348px — pushing the whole pod off the TOP of the screen at every height from 900 down to 720**; and the pod is anchored bottom and grows upward, so it overflows upward **with no scrollbar to notice**, and was already within **~112px** of doing so at 720px tall *before* this change. Fixed by dropping the bar (four percentages carry the comparison in a text popover with no vertical room) and capping with `max-height` + `overflow-y`, `box-sizing: border-box` per the `#temporal` content-box lesson. **The pre-existing latent overflow is the more useful find of the two.** | `data/city_budget_context.json`, `scripts/generate_status.py` (`budget_context`), `web/index.html` (`renderBudgetContext`), `web/styles.css` `#about-menu`, `tools/profiling/verify-about.js`, `data/DATA.md` §16 |
| 2026-08-04 | **`verify-peek.js`'s empty-map pixel is DERIVED FROM GEOMETRY and confirmed with ONE pick, never hunted for with picks — 437s → 94s, all 27 checks unchanged.** The backlog item that prescribed this work named the wrong loop and both of its levers were wrong, which is why it was re-measured before being acted on (the standing *an open item can be stale* rule, earning its keep a third time). It blamed `findTappableHoods`; measured, that sweep is **46s of 408s** — it exits as soon as it has `n` hoods and never nears its 2,400-candidate worst case (1 candidate / 9 picks desktop, 22 / 57 touch). **The cost was the empty-pixel scan: 346s, 85% of the script**, blind-sweeping a 7px grid with **~2,470 `pickObject` calls** to find one pixel that picks nothing. ⚠️ **A pick costs ~137ms and neither `radius` nor `deviceScaleFactor` moves it** (measured r0 vs r6, dsf3 vs dsf1 — all identical): deck re-renders the **whole picking buffer** on the CPU under SwiftShader on every call, so the cost is the render, not the readback, and **the only lever that works is making fewer picks**. The first pick burst in a context additionally carries a **one-off ~20s** of shader warm-up — which is essentially all of `targets`' residual 46s, so that loop is already at the floor and was deliberately left alone. ⚠️ **The item's "coarsen the grid 9px → 18px" lever is a measured trap**: step 25 still costs 30s, and **step 40 finds no empty pixel at all** and would fail the check — empty pixels are genuinely scarce, only **17 of 4,400** grid points are clear. The fix is the item's *other* lever applied to the loop that actually cost the time: **`metric-extrusion` is the ONLY pickable layer**, so *"no hood polygon covers this pixel"* and *"`pickObject` returns nothing"* are the same statement, and `boot()` has already flattened the map so a prism's screen footprint is just its projected polygon. Project all 406 hoods' rings (**9,236 vertices, 14ms**), ray-cast with a bbox pre-filter, require an 8-point 7px ring clear too, then confirm the first candidate with **one** real pick — landing on the **same pixel (382,425)** the blind sweep found. Falls back to a 3px grid if the 7px one yields nothing, since geometry tests are free where picks are not. ⚠️ **The confirming pick stays** — the assertion under test is still *deck picks nothing here*, not *my geometry says nothing is here*. | `tools/profiling/verify-peek.js`, `tools/profiling/verify.js`, `docs/MOBILE_USABILITY.md`, `docs/TOKEN_EFFICIENCY.md`, `docs/TODO_archive.md` |
| 2026-08-04 | **`build_site.py`'s structural guards are scoped to the `<head>` slice, and the WIP badge is injected at the LAST `</body>` — because a guard that substring-searches 290KB of script to check a 5-line head is a false-positive generator, not a guard.** Carried S78 → S90 as *"propose scoping the base-tag guard"* (Peter: *"the genuinely easy remainder"*), and **reproduced before being touched**: a comment reading `// /full/ gets a <base href="../"> injected` placed ~289,000 characters BELOW `</head>` fails the build with *"source already carries a `<base>` tag"* — the exact shape that killed a green PR #117 deploy. ⚠️ **The cause is that `web/index.html` is ~4,250 lines of which nearly all are script below the head**, so `"<base " in full_html` cannot distinguish a competing element from prose; the same applied to `count("<head>") != 1` and `count("</body>") != 1`, both fixed in the same pass rather than left as an identical trap. Now: anchor on the **first** `</head>`, check `<base `/`<head>` **within that slice only**, and inject the badge before the **last** `</body>` (strictly more correct than first-plus-count — the document's own closing tag is the last one, and a quoted `"</body>"` in a JS string would previously have taken the badge). ⚠️ **The new test was falsified against the old code first** — it fails there with the original #117 error and passes after, so it pins the defect and not just the behaviour. Verified end-to-end, not only by unit test: both builds emit (`<base ` count 0 and 1) and `verify-smoke.js` is **42 checks public / 44 `/full/`**, so `/full/` still resolves its shared `data/` + `vendor/` through the injected base. | `scripts/build_site.py`, `tests/test_build_site.py` (9 tests, +1) |
| 2026-08-04 | **A backlog item gated on "wait for the weekly refresh" gets the refresh DISPATCHED BY HAND rather than waiting out the cron — the gate was the data being published, not the calendar.** Three items had been carried S89→S91 behind a Monday 08:00 UTC cron that was 6 days out; one `workflow_dispatch` closed all of them in ~20 minutes, and the served-column guard got its first-ever CI execution as a side effect. ⚠️ The dispatch is only safe because `refresh.yml` is idempotent and commits only when the data actually changed — this is not a licence to hand-run `generate_status.py`, which stamps an unearned `last_checked`. Full record in `docs/TODO_archive.md` 2026-08-04. |
| 2026-08-04 | **The mobile control column stays as a stack — NO bottom sheet, no hamburger — and the mobile-chrome quick pass is closed with no code change, because the ">half the screen" state the decision rested on had moved off the view it was recorded against.** The question had been open since 2026-07-24 and was re-measured before being decided (the standing *an open item can be stale* rule). The union method reproduced the default state to the decimal (**27.9%**), so the movement is real: ⚠️ **Money unfolded is now 47.9%, not the recorded 54.3%** — `#moneymode` left the Options panel for `#toggle` row 2 on **2026-08-02, one day after that measurement**, so the pod lost a row. The only >50% states are **Services (53.1%) and Development (52.7%)**, which had **never been measured** — the 08-01 pass took one view and generalised it. ⚠️ **The public build cannot reach the worst state at all**: Services and Ratio are full-only since 2026-07-28, so public `#views` is **two buttons (Money · Development)** — correcting a `MOBILE_USABILITY.md` line that had claimed four. The worst *public* state is **52.3%** (Development unfolded **+** peek card open), and it renders clean — nothing clips or overlaps, the middle ~40% of the map stays clear. **Peter's call on that evidence:** the >50% states are **transient and user-initiated** (you reach them only by deliberately unfolding Options, and the peek card is the answer to a tap you just made), while the state a phone user actually meets first is 27.9% — about 7 points over desktop's 20.3%. Against that, a bottom sheet is a refactor of **shared desktop+mobile DOM** (`CONTROLS_MATRIX.md`: grouping drives both), i.e. real desktop regression risk to fix a state the user can dismiss. ⚠️ **`#views` POSITION loses its vehicle**: it was parked pending "the move-2 fork", which this refuses — it now needs its own proposal if ever revisited. **Tenth time a carried item's stated basis did not survive re-measurement, and the first where re-measuring closed the item instead of redirecting it.** | `docs/MOBILE_USABILITY.md` §2 (per-view coverage table) + §3, `docs/TODO_archive.md` |
| 2026-08-04 | **The bikeway lifecycle item's CAPITAL half is sourced and recorded as an INERT input, and the item stays OPEN — because the service life it needs is not a number Edmonton publishes, and the two available shortcuts around that are both category errors.** Carried since 2026-08-03 as Peter/laptop work (edmonton.ca is unreachable from the Oracle box, DATA.md §13); run from the laptop 2026-08-04. **Found:** the Bike Plan Implementation Guide §1.2 **Table 3** gives blended capital unit costs of **$365k–$790k/km by urban form**, citywide **$452,065/km** ($306.5M / 678 km), explicitly *"the capital costs to construct bike lanes ... do not include maintenance costs."* ⚠️ **Verified rather than relayed**, which is the whole reason it is trustworthy: every row's implied $/km falls inside its own stated band, the suburban subtotal adds, and the prose's $190.8M City-borne share reconciles exactly — a web-search summary had meanwhile volunteered its own **$1.47M/km** derived from the $100M ATNE program budget over 68 km, which is a *program* figure (design, contingency, land, dearer inside-Henday contexts) and is recorded as an upper bound, not a competing value. **Not found, after searching the Development Impact page, both Bike Plan PDFs, the 2025 Infrastructure Report, the Infrastructure State-and-Condition / Inventory / Tools pages and the 2023 Capital Asset Management Audit: any service life.** The Bike Plan's own action **9.6.2(a) is to *"establish"* a bikeway asset-management program** — the City states it does not have one, so this is a number that does not exist publicly rather than a search done badly. ⚠️ **Both shortcuts are closed off explicitly, because each is the $178/km error in a new costume.** (a) The sidewalk page's *"amortized for 20 years"* is a **local-improvement tax levy term, not an asset life.** (b) **The ~3% set-aside rule does not transfer to bikeways, and this project's own data falsifies it**: at roads' implied 3.33%/yr, $452,065/km allows **$15,069/km/yr** for all O&M *plus* renewal, while measured `bikeway_ops` is **$20,278/km/yr — 1.35× that entire allowance** before a dollar of capital renewal. Bikeway cost tracks a winter **service standard**, not asset value; the source assigns fire stations *"at least 6%"* anyway, so the rate was never universal. **Peter's call: record the source, ship no metric.** `bikeway_capital` is written as $/km of asset value with a `_status` field saying it is not a rate and is read by nothing — `load_unit_costs` returns its same five keys and all 560 tests pass unchanged. ⚠️ **A third numerator/denominator mismatch is recorded, and unlike the two snow-rate ones its DIRECTION is knowable**: Table 3 blends the *future* facility mix while `bike_m_per_acre` is the *existing* stock — **82.2% Shared Pathway, 89.8% off-road, only 0.8% Local Street Bikeway** — so the blend leans on cheap on-street types we largely exclude and most likely **understates** our network. **Noted for whenever a life is chosen:** across a 20–50 yr span the result is **$29–43/m/yr, 0.6–0.9× roads**, i.e. barely sensitive to the missing number because the $20.278/m/yr ops floor dominates — against the rejected reading's 1/281×. | `data/city_unit_costs.json` (`bikeway_capital`, `_two_bases`), `data/DATA.md` §13, `TODO.md` |
| 2026-08-04 | **The roads-maintenance figure in the live budget pod was replaced with a published program line — and the derived figure it replaced was ~5× too low, on a public page.** The backlog item called it *"the one soft number in that table"* and scoped the work as *"a single value swap"*; the swap was one value, but the number was wrong, not merely soft. ⚠️ **The old figure was `$1,285/km × ~11,000 km = $14,135,000`** — a narrow unit rate multiplied across the whole network, with the *snow-clearing* network reused as a maintenance denominator. Against the City's own published program the implied rate is **~$5,900/km**. **Found by going one level below the PDF**: `budget.edmonton.ca/api/operating_budget.csv` publishes **program-level** operating budgets FY2017–FY2026 (now `DATA.md` §17), where the Approved Operating Budget PDF stops at branch level and bundles roads with parks (`Parks and Roads Services`, FY2026 gross $303.361M — 6× the entire roads row). ⚠️ **THE ERROR WAS IN OUR DERIVATION, NOT IN THE SOURCE, and a cross-check proves it**: the pod's roads snow $36.85M plus the active-transport row's path snow $30.15M = **$67.0M against the published `Snow and Ice Control` program's $67,553,815 (FY2025) — 99.2%**. Same Taproot source, two numbers: its *totals* reconcile almost exactly, its rate×network *product* was out by 5×. **The asymmetry is the finding**, and it is why the snow components were left untouched rather than the whole source being distrusted. **FY2017 chosen despite being stale, because it is the ONLY year Edmonton ever published a roads-only maintenance program** (`Roadway Maintenance` **$65,671,000**, beside `Snow and Ice Control` $63,709,000 — a 1.03 ratio, where the derived figure implied 0.38× its own snow, **4.9× apart**). The tree was **re-cut in 2018** into `Infrastructure Maintenance`, which also covers sidewalks, pathways and bridges — using it would **double-count against this table's own sidewalks and bike rows** — and **re-cut again in 2026**. **Peter's call: roads-only scope beats matching vintage, documented rather than silently mixed**, as already accepted for ETS 2025 vs fire 2026; being 2017 dollars it is if anything a lower bound, the branch having grown ~34% by 2025. **Effect: roads $50.985M → $102.521M, 1.33% → 2.67% of the operating budget, transit:roads 9.2× → 4.6×**, `derived_component` dropped so the public asterisk goes. ⚠️ **A one-value edit, because nothing downstream pins a share** — the second post-publication correction to this table and the standing argument for that rule. **The pinning test failed on the change as designed** and was updated with a warning that moving it is legitimate only alongside a sourced value change; a second test now pins the value and the absence of `derived_component` against a revert. ⚠️ **Not live until `refresh.yml` reruns `generate_status.py`** (RUNBOOK §3d dispatch, or the Monday run). | `data/city_budget_context.json`, `data/DATA.md` §16 + new §17, `scripts/generate_status.py`, `tests/test_generate_status.py` (+1), `TODO.md`, `docs/TODO_archive.md` |
| 2026-08-05 | **The cardinality guard's bands were tightened for FOUR of six anchors and deliberately left wide for the other two — because the two were not noisy, they were trending, in the guard's own dangerous direction.** The item asked to "tighten the bands once there is variance data"; the variance data turned out to live in a place nobody had looked — **the guard logs every anchor value in CI on every run**. ⚠️ **`gh run view --log` returns nothing even for a COMPLETED run here, while the jobs API works** (`gh api .../actions/jobs/<job>/logs`), extending quirk (qqqq)'s in-progress half. ⚠️ **Five runs but only THREE independent observations** — the 2026-08-02 and 08-05 runs committed `status.json` only, so their anchors re-measure unchanged input; checking what each auto-refresh commit actually touched is what separates an observation from a re-reading. **Four anchors were effectively frozen** — `dup_parcel_points` constant at 33, `lot_needle_ratio` 0.00%, `dedupe_effect_pct` 0.01%, `dup_parcel_value_frac` 0.11% — against bands ±50% wide, and were tightened **2× to ±25%**. ⚠️ **`ineligible_points` and `ineligible_value_frac` moved monotonically upward on EVERY independent data change** (56→58→60; 0.00517→0.00575→0.00633, no reversal), which is **"more assessed value silently leaving the lot-acre denominator"** by the guard's own `DANGER` map. **Tightening those would red the weekly publish on the next real data change and read as a false alarm**, so they keep their wide bands and the drift became its own open item; `ineligible_value_frac` has already used ~72% of its band and would breach in 2–3 more moves of the observed size. ⚠️ **THE ITEM'S PRESCRIBED MECHANISM COULD NOT EXPRESS ANY OF THIS** — `--write-baseline --tolerance` applies ONE global tolerance to every anchor, so the bands are now hand-set per anchor (no code change needed: the comparator reads `min`/`max` per key and never looks up `_`-prefixed notes) and that flag would silently flatten them, which the baseline now says in `_bands_are_per_anchor`. ⚠️ **Not tightened beyond ±25% on purpose: no observation across a January year-roll exists**, and that roll is the event the guard was built for — three weekly readings say nothing about reassessment. **Three tests added where nothing pinned the committed baseline at all**, and the tightening test was **falsified against the old baseline first**. **Eleventh time a carried item's stated basis did not survive re-measurement**, and the second where re-measuring changed what got built rather than merely confirming it. | `data/expected_value_anchors.json`, `tests/test_check_value_anchors.py` (+3), `TODO.md`, `docs/TODO_archive.md` |
| 2026-08-05 | **`notebooks/verified/01_money_lens.py`'s rendered output is PUBLISHED to the live site (`web/verified/`), refreshed weekly, and linked from the About panel via a new doc — not just committed for GitHub's own notebook renderer.** Peter's ask: a durable, physically-pointable answer to "how do I know this actually ran correctly," more rigorous than `docs/METHODS.md`'s prose and more digestible than the pipeline source. ⚠️ **`web/verified/` had to get the same `deploy.yml` path exclusion as `web/data/`**, or the weekly data-bot commit (which now also touches `web/verified/**`) would double-fire the code-only deploy — exactly the "deploy loop" that file's own comment already warns against widening into. **The About-panel link points at the new `docs/VERIFICATION.md`, not directly at the rendered HTML** — mirrors how the existing Methods link points at a doc, not a data file, and sidesteps a bootstrap problem: `verify-about.js` fetches every panel link against production regardless of test target, so a direct link would 404 until the first live deploy, while a repo-file link resolves as soon as the PR merges. `tools/run_verified_notebooks.py --execute` needed `ipykernel` added to `requirements-ci.txt` alongside `jupytext`/`nbconvert` — confirmed by installing only that file's pins into a clean venv, since nbconvert's own dependency chain doesn't pull in a kernel to actually run against. | `docs/VERIFICATION.md`, `.github/workflows/refresh.yml`, `.github/workflows/deploy.yml`, `web/index.html` `#about-menu` |
| 2026-08-06 | **The Services lens has NO hood panel, and "does this view have a panel" is a SEPARATE test from "does this hood have history" — `hoodPanelLens()` vs `temporalFor()`.** Peter asked for two things: the assessment-over-time panel must not trigger in Services (a service-specific panel is coming), and the tooltip must list only the CHECKED services rather than every column with data. The tooltip half was straightforward. ⚠️ **The panel half shipped a LIVE REGRESSION (#169) by writing the view rule into `temporalFor()`, the shared DATA accessor** — elegant, one line, and wrong, because the **touch peek card gates on the same function**. `.tip` is suppressed on touch by an earlier locked decision (it drew on top of the card, 127px off a 390px screen), so **the card is the only per-hood readout a phone has**: tapping a neighbourhood in Services returned *nothing at all*. It reached production, and **every `verify-*.js` in the suite passed through it** — the suite covers the card, and covers Services, but had no case for *the card in a view with no panel*. The fix splits the two tests and applies the view rule at each of seven consumers (`tooltipFor` sparkline **and** its panel-mode reduction, `temporalClick`'s two branches, `openPeek`'s `#peek-go`, the card's own tap, `syncPinnedPanel`, `applyHoodMode`, `syncHoodModePod`). ⚠️ **The panel-mode tooltip reduction is gated too**: it is a TRADE (detail for a panel that carries it), so where no panel opens it is a deletion, not a reduction. ⚠️ **Writing the regression test exposed a SECOND hole in the fix** that reasoning had missed — the deliberate opt-in (`panelByChoice`, bought by pressing `#hoodmode-btn`) routes a tap *past* the card branch to pinning, so with it armed the tap fell through to the now-inert pointer path and died again; views with no panel take the card path **regardless of the opt-in**. `#hoodmode` is hidden in Services (a control offering a destination that does not exist), and `applyHoodMode` refuses panel mode there outright so no future caller can reopen the empty box. **Also fixed, PRE-EXISTING and reproduced on master first:** nothing re-rendered an open peek card and `openPeek` is idempotent per hood, so a card opened in Money kept printing Money's `$/acre` after switching to Services — the wrong lens's number on screen, the same silent-correctness class as a revenue breakdown left under a value map. `verify-peek.js` gains the whole case and is **falsified** against the broken build (exactly 4 failures, all the new checks, zero collateral). **Ninth time a "one-line" change's real blast radius was wider than its diff.** | `web/index.html` (`hoodPanelLens`, `syncHoodModePod`), `tools/profiling/verify-peek.js`, `docs/CONTROLS_MATRIX.md`, `docs/MOBILE_USABILITY.md` |
| 2026-08-06 | **Mill rates stay a MANUAL, reviewed input — the automation goes on DETECTION instead, as a monthly digest that files a GitHub issue.** Auto-fetching "the latest" rates is the bug, not the feature: rates are year-keyed and must match the assessment roll, so a fetcher would silently bill an old roll at new rates. The source is also less stable than it looks — its vocabulary broke 4× in 12 years (`Non-Residential`→`Non Residential`, `Municipal Tax Rate`→`Municipal`, `Farmland` deleted in 2025), and the Farmland deletion is exactly the case that needs a human's flagged assumption rather than a fetcher's guess. It also changes once a year, so weekly polling buys nothing. Delivery is a GitHub ISSUE, not SMTP: no second credential, and the issue is itself a checklist with state. An issue is filed every month even when green, because a silent digest and a broken digest look identical. | `scripts/vintage_report.py`, `.github/workflows/vintage-digest.yml`, `docs/RUNBOOK.md` §0, `tests/test_vintage_report.py` |
| 2026-08-07 | **Ward-level rollups are IN scope, and the ward join is an attribute regroup — not a boundary ingestion.** `SPEC_phase1.md` and `ARCHITECTURE.md` both list "per-ward breakdowns" under Phase-1 **Out of Scope**; those remain accurate as records of Phase 1 and are not edited, but the exclusion no longer binds. The work turned out to be far cheaper than the exclusion implied: `Ward` is already an **attribute column on the property-info CSV** (post-2021 names), so there is no polygon to fetch and no spatial join — and measured across 439,685 parcels, **every one of the 398 ward-tagged neighbourhoods sits in exactly one ward**, making a ward figure a regroup of neighbourhood results rather than a re-aggregation from parcels. `load_wards` **raises** if that 1:1 relation ever breaks, because the failure mode is a silent double count. Rejected: ingesting a ward boundary layer (unnecessary), and mapping the 8 served/CSV name variants (they are the same hoods that carry a null ward upstream, so mapping them recovers nothing). | `src/load_wards.py`, `tools/ward_rollup.py`, `data/DATA.md` §13 |
| 2026-08-07 | **`ward_rollup.py` keeps reading the COMMITTED SERVED FILE and no fresh pipeline run is taken — because the renewal side is provably refresh-invariant, so a rerun would reproduce $138.5M exactly.** The S99 handoff left this open as "a real choice that should be settled before anyone quotes $16.6M or $138.5M". **Measured across the 2026-08-03 → 08-04 auto-refresh (the last one that changed the file): `road_m_per_acre` changed in 0 of 406 hoods**, citywide sum identical to the cent (11,610.65). The renewal figure is `road_m_per_acre × area_acres × $38`, so with the metric frozen the only remaining input is acres. ⚠️ **The acres come from a DIFFERENT FILE THAN THE ONE CI USED, and the tool's own comment ("the same boundary acres the metric was divided by") asserts an invariant nothing enforces** — `data/raw/` is **gitignored**, so the local `neighbourhoods.geojson` is a **2026-07-06** download while the served metric was built in CI from a fresh Aug-4 fetch. **Checked rather than assumed, by recovering CI's acres from the served file itself (`total_revenue / revenue_per_acre`, both served): all 406 hoods agree to floating-point precision (max relative difference 2e-13, citywide 193,237 acres both ways), and road km / renewal $ are identical either way (3,644 km, $138.5M).** Boundaries are stable; the reconstruction is sound **today**, and the cross-check is the cheap way to re-confirm it if that ever changes. **What DOES drift is the revenue denominator**, which only touches the `% of revenue` column: **77 of 406 hoods moved, citywide +$5.9M (+0.22%)**, immaterial at one decimal — except that **nearly the entire citywide delta is ONE hood**, `WEST MEADOWLARK PARK` **$4.63M → $10.63M (+130%)**, which alone moves ward **sipiwiyiniwak +2.8%** while every other ward moves ≤0.06%. That hood is unexplained and is now its own open item; it is a revenue-side question and does not touch any renewal number. **Also weighed: the served file is not hand-built** — `refresh.yml` regenerates it weekly (Monday 08:00 UTC) and the Aug-5 run left it **byte-unchanged**, so "the packet matches the live map" is a property that maintains itself rather than a snapshot going stale. Rejected: dispatching a refresh to "freshen" the numbers (would change no renewal figure), and re-deriving acres locally from a `download_data.py` run (reintroduces the `TODO.md` stale-raw-data hazard to fix nothing). `--served` already exists for pointing at any other build. | `tools/ward_rollup.py` (unchanged — decision is to leave it), `session-summary/2026-08-07-s99.md` §4 |
| 2026-08-07 | **A renewal-fund framing publishes the RENEWAL HALF of the roads lifecycle rate ($38/m/yr) and not the O&M half ($12/m/yr)** (Peter, 2026-08-07). The $50/m/yr `roadway_om_renewal` decomposes into its two published components ($600k O&M + $1.9M renewal per km, both over the same 50-yr life). Publishing the **$12 O&M half would put a second recurring road number beside `roadway_ops`' $4.635/m/yr** — both annual, both non-capital, ~2.6× apart, different sources — the arithmetically-true/descriptively-false collision `_two_bases` exists to prevent. **$38 renewal does not compete with $4.635 operating because they are not the same kind of number.** ⚠️ This is a DECOMPOSITION of basis (1), **not a third basis**: never sum `roadway_renewal` with `roadway_om_renewal`. ⚠️ The output is an annual **REQUIREMENT, not a funding gap** (a gap is requirement minus funded, and we have no funded-side data), and it is **collector+local only** — arterials are excluded from the metric, so every total understates the network. Rejected: the full $12+$38 split (two recurring numbers on screen), and leaving $50 blended (yields no renewal-specific figure). | `data/city_unit_costs.json` `roadway_renewal`, `tools/ward_rollup.py` |
| 2026-08-07 | **A per-neighbourhood revenue-delta guard WARNS AND NEVER BLOCKS, and its threshold is TWO conditions (≥10% AND ≥$1M), not one** (Peter, 2026-08-07: *"warn and continue, but email and say big delta"*). Motivated by `WEST MEADOWLARK PARK` +130% on a **green** 2026-08-03 refresh, unnoticed for four days: every existing guard is a citywide aggregate or a schema list, and citywide that event was +0.22%. **Warn-not-fail** because a hood's revenue can legitimately double when a large parcel completes — a guard that reds the weekly publish on good data gets switched off, so `check_revenue_deltas.py` always exits 0 and the refresh commits and deploys regardless. **Both threshold conditions are load-bearing, set from measurement not guess:** across every auto-refresh that has ever changed the served file, the worst movers were CANON RIDGE +3.1%/+$67K (07-20), **ALCES +12.7%/+$501K** (07-27), and WEST MEADOWLARK +129.7%/+$6,001,962 (08-03). ALCES is why the dollar condition exists — small edge hoods clear 10% on a few completed houses. The pair fires on **exactly one event in all recorded history**, ~100× clear of the next-largest mover in the same run, and was **falsified against the real `f76fc7d → f464bdf` pair** before being trusted. **Notification is a filed GitHub issue**, not a log line: a warning inside a green run reaches nobody, so `refresh.yml` gains `issues: write` — a notification capability, not a publish one — matching `vintage-digest.yml`'s channel and reasoning. ⚠️ **The guard must run BEFORE the commit step**; its baseline is `git show HEAD:` of the served file, which after the commit becomes the new data and makes the diff always empty. Rejected: failing the publish (would red on legitimate reassessment), a percentage-only threshold (ALCES), and routing through the monthly digest to avoid the permission change (up to 30 days' latency on a number that is live and wrong). | `scripts/check_revenue_deltas.py`, `.github/workflows/refresh.yml`, `docs/RUNBOOK.md` §0b |
| 2026-08-07 | **Roll-continuity is measured by POSITION, never by an identifier — because account numbers, addresses AND neighbourhood names all churn independently.** Established while explaining `WEST MEADOWLARK PARK` +130%. ⚠️ **The answer inverted twice before landing**: first read as a new parcel arriving, then as a taxability question, finally as a **renumbering gap** — Misericordia was continuously assessed 2012–2025 as `10095840`, renumbered to `11495573`, and simply absent from the published current roll in between, so **the map UNDERSTATED that hood by ~$250M assessed / ~$6M/yr and the +130% was the CORRECTION**. **Three one-off checks each produced false positives, one per identifier:** account-based said the hospitals had never existed (they had, under old numbers); address-based called **$469.8M "missing"** that is not (Westmount Shopping Centre was re-addressed); per-hood aggregates put **OLIVER at −100%** (renamed to WÎHKWÊNTÔWIN, 12,237 parcels). **Renumbering is routine** — 0.15–0.37%/yr in the historical roll, 2023→2024 spiking to 0.91% / 3,893 accounts — so a vanished account cannot itself be a finding. **Position is stable to under 2 m across a renumber** (Royal Alexandra 1.3m, Grey Nuns 1.6m, Misericordia 0.7m, Cross Cancer 0.6m), which is what `audit_roll_continuity.py` matches on. Validated against known answers: all four renumbered hospitals match, and the address method's flagged properties match too. ⚠️ **`legal_description` (plan/block/lot) would be a better key and is immutable, but exists ONLY in the historical roll** — recorded so nobody re-derives that dead end. ⚠️ **The two datasets disagree about account numbers for the SAME assessment year 2025**, so cross-dataset account joins are unreliable for renumbered parcels. Rejected: matching on rounded coordinates (0.1 m tolerance reproduces the false positive — pinned by a falsification test), and reporting the 1,534 unmatched parcels upstream (no baseline for how much is ordinary demolition; needs a second run to separate transient from permanent). | `tools/audit_roll_continuity.py`, `data/DATA.md` "Tax-exempt flag", `docs/AUDIT_LEDGER.md` 2026-08-07 (S100) |
| 2026-08-08 | **Edmonton's own legal boundary is drawn, and the 14% of the city it exposes is left as EMPTY SPACE rather than filled or annotated** (Peter, 2026-08-08). The map had never drawn the city limit — what read as Edmonton's edge was only where the neighbourhood polygons stop. ~~**Measured: the legal boundary is 782.1 km², the 406 rendered hoods cover 672.4 km², so 109.6 km² (14.0%) is inside the city and absent from the map** (annexed and undeveloped land carrying no neighbourhood).~~ ⚠️ **THE MEASUREMENT IS RETRACTED 2026-08-09 (see the line below); the DECISION — draw the limit, leave the space empty — STANDS.** The gap is one-directional — **0.0 km² of hood fabric falls outside the legal limit** — so the map understates the city's extent and never overstates it. **No metric is affected**: every metric is per-neighbourhood, so land with no neighbourhood enters neither a numerator nor a denominator. ⚠️ It does bear on any future *citywide-per-acre* figure, where 672.4 vs 782.1 km² are answers to different questions. Drawn in the same faint `BOUNDARY_COLOR` as the seven neighbouring municipalities, unlabelled, unfilled, under the data. **Four rural municipalities added alongside it** — Strathcona, Sturgeon, Parkland and Leduc County — so the city reads as backed up against its regional neighbours rather than floating. ⚠️ **`REGIONS` outlines are NOT clipped to the view extent** unlike the river/highways: a municipality is a closed ring, so clipping replaces its far side with a straight run along the clip box that draws as a fake border; Parkland (66.7% inside) simply runs off the edge. ⚠️ **`boundary` is no longer 1:1 with `place`** (12 vs 7) — a `verify-reference-layer.js` invariant retired deliberately, not broken. Rejected: filling the 109.6 km² as a distinct "city land, no data" category (adds a visual class to every view and needs a legend entry), labelling the counties (Parkland alone is 3.5× Edmonton — far too large to name at city zoom), and all 13 metro-region members (most do not touch Edmonton). | `scripts/build_reference_layers.py` `REGIONS`, `data/DATA.md` §3 + §14, `tools/profiling/verify-reference-layer.js` |
| 2026-08-08 | **The river's blue is raised by CHROMA, not brightness** (Peter, 2026-08-08: *"the river to be more blue"*). `RIVER_COLOR` `[50,66,94]` → `[42,74,128]`: the R→B spread nearly doubles (44 → 86) while relative luminance moves only 64.6 → 71.1, against `SET_ASIDE_COLOR`'s ~108. That ceiling is what keeps the river reading as landform rather than as data, and lifting all three channels — the obvious way to make it "more blue" — would have pushed it toward the data band while barely changing the hue. The inline comment now names chroma as the lever so the next adjustment does not reach for brightness. | `web/index.html` `RIVER_COLOR` |
| 2026-08-08 | **The $125.4M/yr AJ/UF/UI/PU figure is published only as a GROSS MODELLED number — never as "revenue lost" — and the direction of its error is stated as UNKNOWN, not as an understatement** (Peter, 2026-08-08). Required wording: *"Gross modelled tax if every institutional/public-zoned parcel were fully taxable. Alberta law exempts many of these properties (hospitals, universities, government land); the actual taxable share is not publicly known."* ⚠️ **This corrects a claim that was LIVE ON THE SITE**: the revenue-mix panel printed *"Tax-exempt land is not on the roll, so it is absent from every share above"*, which is the retracted premise, **published**, and pointing the wrong way — it promised the shares UNDERSTATE institutional hoods when the pipeline levies those 2,254 parcels and may be OVERSTATING them. Replaced with a note that asserts no direction at all. ⚠️ **`FINDINGS_revenue_scale.md` §4-5 was the cited authority and had been triaged as "narrative, not a computation others cite" — that triage was wrong, because nothing had checked whether the prose was quoted by CODE.** §4's conclusion survived its own premise (measured, not assumed) and is marked do-not-fix; §5's direction was inverted. Rejected: adding the $125.4M to the UI in order to caveat it (it is not surfaced anywhere today, so "add a caveat where it appears" had no target, and publishing a new number is a larger decision than a disclosure fix), and hand-flagging U of A / hospital parcels as exempt (no verified per-parcel source). | `web/index.html` `renderRevenueMix`, `docs/FINDINGS_revenue_scale.md` §4-5, `data/DATA.md` "Tax-exempt flag" |
| 2026-08-08 | **GIPOT is recorded as adjacent context with three limits, and its dollar figure is NOT published until primary-sourced.** Offered as the one hard anchor for the exemption exposure ("$15.7M for 2021-22"). Held back because: **the City of Edmonton publishes no GIPOT dollar amount at all** (its page gives only a percentage history), so the number is unsourced — secondary reporting says only "$15 million per year"; **2021-22 falls inside the 2020–2024 window when Alberta paid 50% of the calculated amount** (75% in 2019, 75% in 2025, 100% from 2026), so any receipt from those years reads roughly **2× low** as a tax equivalent; and **GIPOT covers Government of Alberta property**, so the claim that universities and hospitals are excluded is *inference* — the program page enumerates no exclusions. GIPOT therefore cannot net off the $125.4M even if the figure were confirmed. | `data/DATA.md` "Tax-exempt flag" |
| 2026-08-08 | **The route to resolving AJ/UF/UI/PU is an open-data request for the liability code — drafted, not sent — and it must NOT claim that Calgary publishes exemption status.** No public per-parcel exemption source exists, so this is the only path short of FOIP. ⚠️ **The claim "Calgary's open data already publishes exemption status" is FALSE**, verified against the live schema of Calgary's `4bsw-nn7w` (22 fields, no exempt/taxable flag). What IS verified is that Calgary's **assessment notices** carry it (*"Tax exemption status is noted on your assessment notice"*, calgary.ca, verbatim). The request therefore argues the field exists internally (Alberta liability codes, reported through ASSET, exempt property excluded from equalized assessment) and is disclosed to owners elsewhere in Alberta — true and sufficient — rather than the stronger checkable-and-wrong version. ⚠️ The ASSET paragraph is itself **not fully verified**: the Municipal Affairs manual returned HTTP 520, so it rests on search-result text; the draft carries a fallback. Rejected: sending it bundled with the `qi6a-xuwt` bug report (one asserts a defect, one requests a field), and including the $125.4M in the message (it depends on the question being asked). | `docs/DRAFT_open_data_request_exemption_status.md` |
| 2026-08-08 | **Regional geography is NAMED on the map — reversing the 2026-07-27 "regions are unlabelled" decision** (Peter). The four counties, nine neighbouring municipalities, Alberta's Industrial Heartland, Nisku and the Edmonton International Airport all carry labels; **Edmonton's own limit is drawn but deliberately NOT named** (the title says it, and a centre label sits on the choropleth). The original reasoning — *"far too large to label sensibly at city zoom, and a county name is not what the outline is saying"* — is right for an ORIENTATION map and wrong for a REGIONAL one: once the question is who else levies here and what Edmonton does not tax, an unnamed edge cannot answer it. ⚠️ **The old objection survives in the ANCHOR, not the decision:** county centroids sit **27–58 km** from Edmonton's centre, so a centroid label lands off the default camera — `_region_anchor` places names on the county's visible strip instead. ⚠️ **`reference.geojson` gained `kind` on every feature and `name` on boundaries**, because until this all 13 outlines shipped as bare `{"t":"boundary"}` and the front end **could not distinguish Edmonton's legal limit from Devon's town outline**. ⚠️ **The Industrial Heartland's identity is INFERRED**: Alberta publishes it unnamed, and it was identified by overlap (Sturgeon 171.8 / Strathcona 134.3 / **Edmonton 53.2** / Fort Saskatchewan 29.5 km²) — so **53.2 km² of it is inside Edmonton** and it is not a clean "infrastructure Edmonton doesn't tax" shape, unlike the airport and Nisku. | `data/DATA.md` §14, `scripts/build_reference_layers.py` (REGIONS / ECON / ZONE), `tools/profiling/verify-reference-layer.js` |
| 2026-08-08 | **Reference outlines are split into one layer PER STROKE with CONSTANT accessors — never one layer with a per-feature `getLineColor`.** deck.gl compiles a constant accessor to a uniform but builds a **per-vertex attribute buffer** for a function one, and the four county polygons are large enough that the difference blew `verify-temporal`'s 4 s click timeout under SwiftShader. ⚠️ **Hoisting the arrow out of the layer literal fixes only half of it** — stable identity stops the per-rebuild churn (measured 5,284 ms → 347 ms on one metric switch) but the attribute buffer is still paid on every layer update, and the suite stayed red 3/3. **Bisected, not guessed:** label count (16 vs 7) and the new geometry were each A/B'd and cleared first; master ran 5/5 green throughout. | `web/index.html` (`referenceSplit`, `boundaryLayer`), `docs/AUDIT_LEDGER.md` |
| 2026-08-09 | **A doc citation must name a SECTION, never a line number — banned outright and enforced by `scripts/check_doc_citations.py`.** The project already banned line numbers for `CODEMAP.md`; S103 found the same failure one file over (`audit_exempt_institutional.py` cited *"DATA.md line ~308"*, **correct when written on 2026-07-09** and ~240 lines off by 2026-08-09, plus two more copies in `docs/`). ⚠️ **Banned rather than validated, because a line number fails SILENTLY AND PLAUSIBLY** — it lands on other real content instead of erroring, so no reader can tell. The guard covers only the **mechanical** failure class (doc missing, `§N` absent, line-number cited); the other two the sweeps found are deliberately out of scope: a figure duplicated across code and docs drifting apart (S102's 2,322 vs 2,448 — the fix is to stop duplicating it, not to check it), and prose no longer supporting the claim that cites it (judgement — that is what a periodic sweep is for). ⚠️ **It runs as a TEST, not a `refresh.yml` step**, deliberately: it is a static repo check with no data dependency, and a docs typo must not red a good weekly data refresh. **Falsified before shipping** — run against `49903c7` it fails with exactly the 3 real citations; on the fixed tree it passes. Rejected: validating line numbers against the doc's current length (encodes the bug), and hard-failing on a quoted section title that has been reworded (legitimate, so it warns). | `scripts/check_doc_citations.py`, `tests/test_check_doc_citations.py`, `docs/AUDIT_LEDGER.md` 2026-08-09 |
| 2026-08-09 | **Pushing to a branch whose PR is already MERGED is blocked by a `pre-push` hook, not by a remembered rule.** Peter merges mid-session, often within minutes; commits pushed after that merge reach origin, leave the branch looking up to date, and are **not on master**. Stranded 9× — **three times on 2026-08-09 alone**, twice while the rule was already written down in two places. ⚠️ **That is the reasoning: the rule fires once (before committing), the danger is every push AFTER the merge.** The hook fires at the only moment that matters and prints the recovery cherry-pick. ⚠️ **It FAILS OPEN** — no `gh`, no auth, no network, no PR → the push proceeds — because on an ephemeral remote VM an unpushed commit is a lost commit, and a guard must never be the reason work cannot be saved. ⚠️ **Hooks are NOT cloned**, so a fresh clone must run `git config core.hooksPath .githooks`; assume it is OFF until verified. It does **not** replace `git merge-base --is-ancestor <sha> origin/master` after a merge — that catches what a push-time hook structurally cannot. **It caught itself while being written**: PR #190 merged mid-build and the hook blocked its own commit. Rejected: warning instead of blocking (the warning-in-a-green-run problem `check_revenue_deltas.py` already documents), and validating rather than banning the related line-number citation (encodes the bug). | `.githooks/pre-push`, `CLAUDE.md` "Session Management", `docs/REMOTE_VM.md` |
| 2026-08-09 | **RETRACTS the 2026-08-08 "14% of Edmonton has no neighbourhood" measurement: the hood fabric DOES tile the city, and the 109.6 km² was a DISPLAY ARTIFACT.** Found by the judgement half of the doc-citation sweep (S104), which re-derived the figure from `data/raw/` instead of comparing the docs that quoted it. In `EPSG:3400` the 406 rendered hoods measure **782.0 km² in the raw boundary file** against a **782.4 km²** legal outline — they agree to **1.4 km²**, inside that outline's own 100 m simplification noise. The 109.6 km² is `main.py`'s **`SETBACK_M = 45.0`** inward buffer: **all 406 hoods lose area** (median **18.3%**, min 2.7%, max 65.9% — perimeter-proportional, the signature of a shrink rather than of missing land), and `buffer(-45)` + `simplify(10)` reproduces the shipped **672.42 km²** to the decimal. ⚠️ **The failure was reading a number off the SHIPPED DISPLAY FILE and attributing it to the world** — `neighbourhood_value_per_acre.geojson` is post-setback, post-simplify geometry, and `join_and_calculate.py` already documents both as DISPLAY-only. ⚠️ **NO WRONG NUMBER REACHED THE PUBLIC PAGE** — it lived in two code comments, `DATA.md` §3/§14 and this index, never in user-visible copy. **The 2026-08-08 decision itself survives**: the city limit is still the only line showing where Edmonton ends rather than where the prisms stop, so it keeps its lighter/wider stroke — but the empty margin inside it is the setback's own gap and must NOT be annotated as missing coverage. **Consequence for any future citywide-per-acre figure: there are TWO denominators (~782 km² total, ~771 km² land), not three** — 672.4 was never a candidate. | `data/DATA.md` §3 + §14, `scripts/build_reference_layers.py`, `web/index.html` (`CITY_LIMIT_COLOR`), `docs/AUDIT_LEDGER.md` 2026-08-09 (S104) |
| 2026-08-09 | **A figure that appears in both the site's copy and a guard's assertion gets ONE source and is SHIPPED IN THE DATA — the guard must never carry its own copy.** S102 found `verify-temporal.js` pinning **2,448** while the published note said **2,322**; both are real numbers (2,322 is 2024's own missing-account count, 2,448 the cumulative shortfall in the 2025 slice) and they disagreed for weeks **with every check green**. ⚠️ **A guard holding a private copy of the number it guards validates itself, not the site.** Fix is **de-duplication, not another checker**: `HISTORICAL_DEFECT_ACCOUNTS` in `load_temporal.py` is now the only place the counts are written, it ships in `temporal.json` as `defect_accounts`, the panel note renders from that field and the verify check asserts against it. **The hardcoded `2024` in the copy went too** — the omitted year moves at the January roll-forward, so copy naming it would have gone stale silently. ⚠️ **THE COUNTS ARE INCREMENTAL AND MUST NOT BE SUMMED**: 2,322 + 131 = **2,453**, but the true cumulative shortfall is **2,448**, because 5 of 2024's missing accounts returned (`SPEC_temporal.md` §0.1). Both consumers therefore state a **single** year's count or fall back to wording with no number — neither can fabricate a total. Pinned by `test_defect_counts_are_incremental_and_must_not_be_summed`. **Falsified in both directions before shipping**: poisoning `defect_accounts` to 9,999 changed the rendered note to 9,999 (proving no literal survives), and re-introducing the literal while the data said 9,999 turned the check **red**. ⚠️ **Accepted trade, stated plainly: the verify script can no longer catch a WRONG count** — it has no independent copy to disagree with, so it now checks rendering fidelity, not truth. Correctness of the number itself rests on the python constant, its tests, and `check_temporal_years.py`. That is the intended exchange: a silent disagreement between two copies is worse than a single value that one guard cannot second-guess. ⚠️ **This closes ONE of the three failure classes in `check_doc_citations.py`'s docstring (class 2); the other ~18 sites are prose in docs and are NOT de-duplicable** — narrative about a one-time audit, not a live value. | `src/load_temporal.py`, `web/index.html` (`temporal-note`), `tools/profiling/verify-temporal.js`, `tests/test_load_temporal.py`, `docs/ARCHITECTURE.md` |
| 2026-08-09 | **EXTENDS the same-day line-number ban: `file.md:207` and `file.md#L207` are banned too, and the colon form is the one that mattered.** The first cut required the literal word *"line"*, so it failed "DATA.md line ~308" and passed "DATA.md:207" — **the form the terminal renders as a clickable link**, and therefore the one anyone actually types. ⚠️ **Two such citations sat unflagged in `TODO.md` from the hour the guard shipped**, both pointing at a line that had since become the *correction* of the very claim citing it: plausible, wrong and silent, which is the failure the ban exists to prevent. **The lesson is not about regexes — a rule enforced against one spelling of a mistake is enforced against the spelling nobody uses**, and the guard reported OK the whole time, which is worse than no guard because it was read as evidence. Found by asking what the *next* item's own citation looked like, not by a sweep. A bare `#anchor` stays legal: it is a real markdown fragment and the normal way to link a heading. Pinned by five parametrised cases plus `test_a_bare_anchor_is_not_a_line_number`. | `scripts/check_doc_citations.py`, `tests/test_check_doc_citations.py`, `TODO.md` (both sites re-pointed at `DATA.md` §1 "Tax-exempt flag") |
| 2026-08-09 | **The served `neighbourhood_value_per_acre.geojson` is rounded at WRITE time to 5 dp / 6 significant figures — 51% off the gzipped payload every visitor pays at boot.** Found by asking whether adding lenses slows the site down. It does not: fetches are lazily memoized per lens and `buildViewLayers()` early-returns on `state.view`, so an unopened lens costs nothing to download and nothing to render. **The one cost that DOES scale with lens count is this file**, which every visitor fetches before the map can draw data and into which each lens adds per-hood columns — at 66 columns its attributes (176 KB gzip) had already outgrown its geometry (138 KB). GDAL's 15-digit default was half the payload: **340 KB → 166 KB gzip**, two kwargs on the existing `to_file`. ⚠️ **Significant figures, not decimal places** — scale-invariant, so dollars-per-acre keep ~1 dp and fractions keep 6 without a per-column table. **6, not 3**, because 3 breaks two real floors: the 0.01 mix-shift comparison in `check_revenue_deltas.py`, and figures re-derived in the audit record. ⚠️ **Rounding is at WRITE time only** — `export_geojson` still returns the full-precision frame, pinned by `test_export_returns_unrounded_frame`. **Verified, not assumed:** 642 tests green, `verify-smoke.js` and `verify-reference-layer.js` both ALL PASSED against the regenerated file with legend bounds **identical** to S104's recorded values (roads $264–$3,253, the $50k/$4M clamps), the revenue-delta guard clean across all 406 (sum drift **$182 on $2.71B**), and the new test **falsified** — it fails on the unrounded write. 5 dp matches `WEB_PRECISION` already used by `load_roads`/`load_bike`/`load_zoning`; this file was the only exporter not following it. **Rejected: the same treatment for `value_grid.json`** — measured at only 16% (896 → 757 KB) because its values are already integers, it is lazy-loaded so no one pays it at boot, it needs hand-written rounding rather than two kwargs, and it carries `median_year_built`, where scale-invariant rounding is actively wrong (3 sf turns 1986 into 1990). | `src/join_and_calculate.py` (`WEB_PRECISION`, `WEB_SIGNIFICANT_FIGURES`), `tests/test_join_and_calculate.py`, `data/DATA.md` §3 |
| 2026-08-10 | **The Services lens gets a hood panel confronting revenue per acre with service cost — and it publishes NO TOTAL, because two locked rules each forbid one.** Closes the last lens with no panel (opened 2026-08-06, when the assessment history was gated out of Services as the wrong content). ⚠️ **The design is forced, not chosen:** `svc_cost_per_acre` is a **lifecycle** basis ($50/road-m/yr) while the three transport costs are **operating** ($4.635/road-m/yr) — the same metres, **~10.8× apart** (`DATA.md` §13) — and *even within* the operating basis the three are deliberately not summed (2026-08-03: the total is 91% transit at the median and buries the per-mode finding). A single "cost" headline would therefore be arithmetically true and descriptively false, which is the failure `TODO.md` flagged when the item was opened. So every cost keeps its own row and its own ratio, **grouped by basis** — the incompatibility is in the LAYOUT, not only the caption, because a reader who skips prose still cannot add across a heading. **Revenue is the reference and each bar is a fraction OF IT**, never a shared scale between bars: a common axis across two bases is the visual invitation to add them. ⚠️ **`verify-peek.js`'s Services block was rewritten, and the invariant restated: "a tap produces a READOUT", not "a tap opens the CARD".** Five of its checks asserted the panel's *absence*; with a panel present the armed-opt-in tap now opens the panel instead of falling through to nothing — the gap closing, not the 2026-08-06 production regression returning. Both readout paths (armed → panel, unarmed → card) are now exercised and each must land on something readable carrying service content; a new check asserts no row is presented as a total. ⚠️ **`hoodPanelLens()` gates on `state.hasSvcCost`**, so a geojson predating the cost columns keeps the old card-and-hover behaviour rather than advertising a panel that opens empty. Verified: 642 tests, verify-peek 9/9 on the rewritten block, verify-temporal / verify-smoke / verify-controls-clickable all ALL PASSED. | `web/index.html` (`SVC_COST_BASES`, `servicePanelFor`, `renderServiceCost`), `web/styles.css`, `tools/profiling/verify-peek.js`, `docs/SPEC_services.md` "Hood panel" |
| 2026-08-11 | **The "rate-adjusted revenue per acre" lens is REFUSED — it is the Value map already on the site, re-derived — and only the vs-citywide-average half is built, as an EXPERIMENTAL full-only lens with negative extrusion.** Asked for as two sub-lenses of a new experimental family. Sub-lens A was to divide the non-residential levy by the class-rate ratio and re-map it; that operation cancels the only thing separating the two classes, so it collapses to *assessed value × the residential rate* — **measured against the served file: Pearson r 0.99980 and Spearman 0.99983 against `value_per_acre`, median multiplier 1.0022, and only 2 of 406 hoods move more than 10 rank places** (the 0.2% residue is `Other Residential`'s 8.3116 rate and 509 farmland parcels). The 2026-06 decision already says so in words — *"the gap between them IS the class-differential mill rates"* — so building it would have shipped a third button for the gap between the two that exist. ⚠️ **Three inputs in the brief did not survive checking and are recorded so they are not re-supplied:** the rate constants labelled 2025 (0.0077419 / 0.0252216, ratio 3.25) are the **2026** rates — 2025 is 7.6254 / 24.2229 mills, **ratio 3.1766**, and `main.py` is pinned to `ASSESSMENT_YEAR = 2025` with 2026 explicitly inert; the citywide levy figure **$2,317,789,000 is the City's budgeted municipal levy, not this pipeline's modelled $2,714,729,507** (17% apart, because we apply published rates to every record on the published roll including the ~$125.4M/yr institutional question that is still open); and the cited "hard-gate rule" for confirming figures does not exist here (the hard gate is the neighbourhood-name guard in `refresh.yml`). ⚠️ **The average is therefore computed FROM THE SERVED FEATURES, never from an external total** — Σ`total_revenue` ÷ Σ acres recovered as `total_revenue / revenue_per_acre` = **$14,048.73/acre over 193,237 acres** — so numerator and denominator come from the same universe as every bar on screen. Substituting the budgeted levy moves the zero line to $11,995 and **reclassifies 30 hoods across it, invisibly**; `verify-deviation.js` asserts against exactly that wrong answer. No acreage source was needed: the per-acre denominator is already the full ~782 km² boundary area (`DECISIONS.md` 2026-08-09). **The lens is EXTRUDED and the deficit half extrudes DOWNWARD** — the Urban3 idiom Peter asked for — **on Money's own `elevationScale`, true to scale**, so a bar here is directly comparable to the same hood's bar on the Revenue map. ⚠️ **The deficit bars are consequently shallow and that IS the finding**: revenue per acre cannot be negative, so the drop is floored at **−$14,049 against a +$234,399 peak (17×)**, and **284 of 406 hoods sit above the area-weighted average**. Rejected: a zero-centred symmetric colour scale (renders 404 of 406 hoods near-white), and independently-scaled elevation arms (would draw a deficit and a surplus of equal size on unequal numbers). Colour alone takes the per-arm p95 clamp, the `chgStats`/infill precedent. ⚠️ **It must never be called Cost of Service or COSA** — it is a re-centred revenue map that models no cost — and `verify-deviation.js` greps the rendered copy for both. ⚠️ **This also RETRACTS a false technical premise recorded in two places**: `changePlaneLayer` and `verify-change.js` both said *"a prism cannot have negative height"*; deck.gl 9.0.38 renders negative elevation below the plane (`tools/profiling/probe-negative-elevation.html`). The Change lens's flat-plane DECISION stands on its own reasoning and is not re-opened. | `web/index.html` (`deviationStats`/`deviationLayer`/`deviationBlurb`), `tools/profiling/verify-deviation.js`, `tools/profiling/probe-negative-elevation.html`, `web/styles.css` `#moneymode .beta` |
| 2026-08-11 | **The deviation lens moves OUT from under Money into "the Lab" — a full-build-only TOP-LEVEL container for unfinished work, not a mode of a published lens** (Peter, 2026-08-11: *"a whole separate button outside of revenue or money… this is very far from going public in any way"*, and *"I may add other experiments to that lens"*). It first shipped the same day as a third `#moneymode` button under Revenue, which was wrong on the point that matters: **a mode reads as a variant of the lens hosting it**, so an experiment parked under Money inherits Money's credibility, and the whole reason this work is separate is that it has not earned it. The Lab is its own `#views` button with a `beta` tag, hidden outright in the public build by the `FULL_BUILD` gate, and `#moneymode` is restored to exactly what it was (Value's row 2, Change only). ⚠️ **The load-bearing part is that an experiment keeps its OWN state: the deviation lens reads `state.labCut`, never Money's `state.metric`.** Under the mode design the two were the same variable, and entering from the **Value** map would have computed a "citywide average" over **assessed value** (~$1.8M/acre) and printed it under a title saying *Revenue* — arithmetic that is internally consistent and labelled wrong, with nothing on screen to give it away. `verify-deviation.js` drives exactly that path (`applyView('money')` → `applyMetric('value_per_acre')` → `applyView('deviation')`) and asserts the average does not move. It also cost the Money coupling entirely: no "switching to Value exits the lens" special case, no `applyMetric` branch, no per-quantity button gating in `syncMetricButtons`. **Adding an experiment is one line in `LAB_EXPERIMENTS` plus its view**; the picker renders from that registry and **stays hidden until there are two of them** — a chooser with one option is not a choice, the rule `syncServiceControls` already applies to the service radios, so the container costs no dead chrome today. Rejected: keeping it as a Money mode (the above), and pinning the lens to Total revenue to avoid duplicating the cut row (a real capability loss when the Lab can simply own its own copy). | `web/index.html` (`LAB_EXPERIMENTS`, `syncLabControls`, `applyLabCut`, `#views` `data-view="lab"`), `tools/profiling/verify-deviation.js`, `web/styles.css` `.beta` |
| 2026-08-11 | **The Lab needs no separate "is it good enough to show" gate — the specialist build IS that gate — but coverage must render wherever a Lab number does** (Peter: *"the specialist lens is literally built for this, especially how the lab lens is marked beta"*). Raised against the proposed break-even lens (`docs/SPEC_breakeven.md` §8.3), where the concern was concrete rather than abstract: on today's inputs it would report **every neighbourhood running a 5.7× surplus**, because we model $2,715M of revenue against $473M of cost — **12.3% of the City's $3,846M tax-supported operating budget**. That is not an uncertain answer but a wrong one, wrong by roughly the same factor everywhere, which is exactly what would make it look plausible and rank hoods in a defensible-looking order. **The resolution is that `/full/` + a `beta`-marked container already answers "who sees this and with what warning"** — inventing a numeric threshold on top would be a second gate for the same job, and one that gets negotiated later while looking at an answer you like. **What is NOT optional is the display requirement**: a Lab number that omits its own coverage IS dishonest at any coverage level, so the guard moves from "reach X% before shipping" to "the number never renders without the fraction of city spending it is measured against". Recorded as a Lab-wide rule, not a break-even one — it binds every future experiment whose value depends on how complete its inputs are. | `docs/SPEC_breakeven.md` §0 + §6 + §8.3, `TODO.md` |
| 2026-08-12 | **The deviation lens divides by DEVELOPED acres, not boundary acres, and its comparison population follows the revenue cut** (Peter: *"take out any areas that are held out. we only want active places with comparable infrastructure... especially in regards to like, residential only"*). ⚠️ **This reverses the 2026-08-11 line "Set-aside hoods ARE in the average (it is a citywide figure)"**, which had the lens saying two different things at once: set-aside hoods were already null on the colour scale AND flat on the extrusion, but still inside the average — the map said they did not count while the arithmetic said they did. **The magnitude is why it matters rather than being a tidy-up:** held-out land is **48 wholly set-aside hoods carrying 63,434 acres — 33% of Edmonton's acreage — against $17.8M, 0.65% of its revenue**, and the median *surviving* hood is still **12.4% set-aside**, so a boundary-acre denominator charged every hood for river valley and rural reserve it cannot levy on. Denominator is now `area x (1 - set_aside_frac)` applied to **both sides**, so a hood's rate and the average always share a basis; the boundary acreage cancels out of the per-hood rate and survives only as the weight in the average. ⚠️ **The lens's headline reverses: 21% of hoods below average becomes 63%** (74 -> 227 of 358), the average moves **$14,049 -> $26,838**, and **127 hoods cross the line**. ⚠️ **A hood-level filter alone was measured and REJECTED as the incomplete version of the same idea:** dropping the 48 set-aside hoods but keeping boundary acres gives $20,777, while the acre-level rule gives $26,838 whether or not the 48 hoods are also dropped (**$26,732 vs $26,838, 0.4% apart**) — the acre rule SUBSUMES the hood rule, and the 90% `SET_ASIDE_THRESHOLD` is a crude proxy for it. ⚠️ **`revenue_per_lot_acre` was available and free and is REJECTED on substance:** lot acres exclude road right-of-way, and ROW consumption is precisely the cost low-density development imposes — the column that removes it would quietly cancel the sprawl penalty this project exists to measure. **The population is a property of the cut** (`DEVIATION_POP`): Total scores all 358 developed hoods ($26,838), Residential only the 226 residential ones ($19,729, 61% below), Non-residential only the other 132 ($23,691, 55% below). The residential case is the one Peter named and the effect runs backwards from intuition — a citywide residential average is **diluted by industrial land that levies almost no residential tax**, so nearly every residential hood cleared a bar that was never about it. Non-residential is the mirror, taken for symmetry: leaving one cut citywide while the others are not is the silent inconsistency the change exists to remove. ⚠️ **TWO CLAIMS RETIRED, not merely restated.** (a) *"A bar here is directly comparable to the same hood's bar on the Revenue map"* is now FALSE — Money divides by boundary acres, and a half-set-aside hood's bar roughly doubles; `elevationScale` is unchanged so the visual scale is not affected. (b) The lens is no longer **rank-identical** to Money: **Spearman 0.900, 242 of 358 hoods moving >10 rank places, max move 318** — measured the same way sub-lens A was **refused** on 2026-08-11 (Spearman 0.99983, 2 hoods), and passing the test that one failed is the reason this denominator earns a lens. **Excluded hoods are now null everywhere consistently** — off the scale, flat, and out of the average — which forced three pieces of copy that had asserted the opposite: the tooltip's *"off the scale; still inside the city average"*, the legend's *"Set aside — off-scale (still in the average)"*, and both titles saying *"vs the Citywide Average"*. ⚠️ **The tooltip also had to stop printing `p[state.labCut]`**: the served column is per BOUNDARY acre, so beside a developed-acre average it showed two numbers whose difference is not the bold one — on every hood with any held-out land. `verify-deviation.js` now asserts the two printed terms reconcile to the signed number, and pins the old boundary-acre average as the specific wrong answer to regress to. **45 checks green, up from 35.** ⚠️ **Title box grew 217px -> 258px**; measured at 1440x900 / 1400x900 / 1366x768 / 1280x720 / 1024x768, worst clearance to `#botleft` is **272px** and the legend label never clips. | `web/index.html` (`DEVIATION_POP`, `inDeviationPop`, `deviationRate`, `deviationStats`, `deviationBlurb`, `deviationKind`/`deviationPeers`, legend + tooltip branches), `tools/profiling/verify-deviation.js`, `docs/LENS_INVENTORY.md`, `docs/CONTROLS_MATRIX.md` |
| 2026-08-12 | **A deviation-lens hood that is ≥25% institutional draws NO PRISM — it is replaced by two bare outlines, one per scenario, and asserts no value** (Peter: *"could we have those replaced by like, a bar of a different color that goes both up and down"*). The roll publishes assessments and a tax class but **not exemption status**, and this pipeline levies every record on it, so for AJ/UF/UI/PU land there are two coherent worlds and no public source picks between them. Rather than show a number that is unknown rather than merely imprecise, those hoods show the range. **15 hoods on Total, 2 on Residential, 13 on Non-residential.** ⚠️ **NEITHER ENDPOINT IS SOLID, deliberately** — a filled endpoint would give one of two unknowable worlds visual primacy. ⚠️ **And it must never be drawn or described as revenue the City fails to collect**, which would assert an exemption status nothing establishes (2026-08-08 above); `verify-deviation.js` greps the rendered copy for *lost / uncollected / foregone / should be / really / actually*. ⚠️ **BOTH SIDES MOVE TOGETHER**: the exempt endpoint is scored against the **exempt-scenario average** ($25,535 on Total, not $26,838), because in that world the citywide average falls too — mixing a hood's exempt rate with the levied average is the same cross-universe error the budgeted-levy substitution makes. ⚠️ **`exempt` IS NOT ALWAYS THE LOWER END, and a first verify check asserted that it was and was WRONG — the code was right.** Removing institutional revenue drops the citywide average by **$1,303/acre**, so a hood losing less than that in absolute dollars GAINS ground: **EVERGREEN +$87** (loses $1,215) and **RIVER VALLEY CAMERON +$842** (loses $461), both near-floor hoods with a high institutional *share* but few dollars behind it. Fields are named `levied`/`exempt` rather than hi/lo for exactly this reason, and `deviationBandSpan` sorts for display so a printed range never reads high-to-low. **Widest band: UNIVERSITY OF ALBERTA, +$144,839 to −$8,013** — $152,852, crossing the plane, so it is either the second-strongest hood in Edmonton or a slightly below-average one. **5 of 15 bands cross zero.** ⚠️ **Three rendering premises MEASURED before designing on them** (`probe-uncertainty-band.html`), the discipline this lens has now needed three times: `filled:false` + `wireframe:true` draws outline only; the outline renders below the plane; and two prisms on one footprint do **not** z-fight. **verify-deviation.js: 57 checks green, up from 45.** ⚠️ Title block grew 258px → 314px (217px before 2026-08-12); worst clearance to `#botleft` **215px** at 1280x720 — still clear, but this is the third growth in one day and Development's 442px blurb is what collides below 768px. Rejected: keeping the modelled value solid and annotating it (gives the levied world primacy), a Lab toggle (adds chrome to a row already wrapping at ≤640px), and banding every hood with any institutional revenue (sub-$100 bands as noise). | `web/index.html` (`INST_UNCERTAIN_MIN`, `isUncertain`, `deviationBand`, `deviationBandSpan`, `deviationBandLayers`, `avgExempt`), `tools/profiling/probe-uncertainty-band.html`, `tools/profiling/verify-deviation.js`, `docs/LENS_INVENTORY.md` |
| 2026-08-12 | **The uncertainty band is WHITE and achromatic, and the banded hood has no floor under it — the amber it shipped with was measurably unreadable and semantically wrong** (Peter: *"that hollow prism is super hard to see, and has a grey floor anyway... make it off color. Maybe white? Or blue?"*). ⚠️ **Measured with the dataviz validator against the lens's own ramp** (surface `#0a0a0f`, all-pairs, dark mode): the amber `#e1be5a` against the deficit pole `#f49642` is **ΔE 5.3 under deuteranopia and 9.5 under NORMAL vision** — below the hard floor of 15 — so the hoods whose value is *unknown* read as *below average* to every reader, colour-blind or not. That is the locked no-direction wording rule being broken by the palette while the copy obeyed it. **White `#ffffff` clears at ΔE 13.0 CVD / 20.6 normal**, the best of four candidates tested (white / near-white `#eef2f7` 9.1-17.1 / blue `#7aa7ff` 14.6 deutan but tritan 11.5 / violet `#c9a6ff` WARN 7.8). ⚠️ **Blue was rejected on MEANING as much as on numbers**: it is a cool hue and the surplus pole is teal, so it leans toward one side of a scale the band exists to abstain from. **Only an achromatic mark cannot imply a pole**, which is why `verify-deviation.js` now pins `R === G === B` rather than a specific hex — the rule is the achromacy, not the colour. **The floor is removed** (`[0,0,0,0]`, was the band colour at alpha 90): a filled slab under a hollow prism reads as a value the hood does not have, and at low alpha over the dark surface it only muddied the outline. The wireframe draws its own base ring at 0, so the footprint is still marked — a second verify check pins the fill alpha at 0. Line weight went to `lineWidthMinPixels: 2` and full alpha. ⚠️ **Not re-opened: the diverging ramp's own poles fail the validator's dark lightness band** (teal 0.829, orange 0.756 against 0.48–0.67). That is pre-existing to every signed lens, not introduced here, and re-stepping the ramp would repaint Infill and Change too. Recorded, not fixed. **verify-deviation.js: 59 checks green.** | `web/index.html` (`UNCERTAIN_COLOR`, `deviationLayer` fill, `deviationBandLayers`), `tools/profiling/verify-deviation.js`, `docs/LENS_INVENTORY.md` |
| 2026-08-12 | **The ASSET / liability-code claim in the open-data request is VERIFIED IN THE PRIMARY SOURCE, and the request is upgraded from "I believe this field exists" to "it is recorded per account under a Ministerial Order."** The 2026-08-08 HTTP 520 on open.alberta.ca was transient; the manual was downloaded and read 2026-08-12 (*2025 Recording and Reporting Information for Assessment Audit and Equalized Assessment Manual*, **Ministerial Order No. MAG:016/25**, 137 pp, dataset `1718-1771`). ⚠️ **It is stronger than the draft assumed on three counts:** the liability code's seven components include a **Tax Code** that already separates taxable (`T`) from *"assessable but exempt from taxation"* (`E`), plus `S` (council-bylaw exempt, still requisitioned) and `G` (PILT); coverage has **no gap** — *"Every assessed property, including taxable property, must be assigned a tax exemption code"*, taxable property taking `NAA`, and the codes are *"mandatory in ASSET"*; and Appendix G names **our exact parcels** by MGA section, `MGA362(1)(d)` university boards of governors and `MGA362(1)(e)` hospital boards receiving Crown assistance. The draft's concession that statutory detail is *"valuable but not necessary"* was therefore **too weak and is retired** — the statutory basis IS the code, so asking for the exemption reason code costs no more than asking for a boolean. The fallback paragraph is struck. ⚠️ **This changes NOTHING about the $125.4M**: the manual establishes the field exists, is mandatory and is reported to the province — **not what Edmonton coded for any given parcel** — so the institutional question stays open with **direction unknown**. Rejected: citing the CKAN resource URL (a UUID artifact; the dataset holds every edition back to 2006 — cite the Ministerial Order). ⚠️ **The blocker is now purely logistical — no submission channel is recorded, and `edmonton.ca` is unreachable from the Oracle box** (`000` on 2026-08-12 while `data.edmonton.ca` / `alberta.ca` / `open.alberta.ca` all resolve). | `docs/DRAFT_open_data_request_exemption_status.md` |
| 2026-08-15 | **The institutional-exemption caveat reaches the MONEY tooltip — the same 15 hoods the Lab bands now say so on the default map, but their prisms stay solid** (Peter: *"shouldn't we be applying that whole glass on neighbourhood prisms with a lot of possible untaxed value to the money and revenue lenses?"*). The asymmetry was real and one-sided: the Lab drew U of A as a range and called its value unknown, while the public map printed **$171,670/acre at rank 2 of 358** for a 90%-institutional hood with no hint attached, and `rev_frac_inst` was read in exactly one place in the whole front end. **15 of 358 live hoods cross `INST_UNCERTAIN_MIN` on `revenue_per_acre` — the same 15 the Lab bands** — and four are top-11 (U of A 2, Spruce Ave 6, Central McDougall 7, Tawa 11; on the exempt endpoint U of A falls to **206**). **$131.2M of the $2,714.7M levy (4.8%) sits on institutionally-zoned land and 71% of it is inside those 15**, so flagging 15 localizes nearly the whole question instead of smearing doubt city-wide; the headline claim is untouched (Downtown #1 is 5% institutional). ⚠️ **REVENUE CUTS ONLY, NOT Value, and not for the road rows' reason** — exemption changes whether a levy is *collected*, not what a parcel is *assessed at* (Alberta assesses exempt property), so `value_per_acre` carries no such doubt and there is deliberately no `value_frac_inst`. ⚠️ **"ZONED" IS LOAD-BEARING**: the row above is an assessment-CLASS share, this is a ZONING share, and without the word they read as contradictory — EVERGREEN is 89% residential and 41% institutional, **130%**. ⚠️ Both surfaces read `INST_UNCERTAIN_MIN` itself, never a copied 0.25, and `verify-inst-caveat.js` pins **set-equality with the Lab's threshold** rather than a hard-coded hood list. Copy echoes the revenue panel's existing footer note (the panel was never silent — the tooltip was the only surface that was). **Not decided: whether the prisms go hollow on Money too** — that costs the colour of 4 of the top 11 and reaches only 1 of Money's 3 modes (grid cells have no institutional share), left open in `TODO.md`. Also corrected `SPEC_revenue.md`'s "permanent caveat", which still claimed revenue/acre **understates** exempt-institution hoods — the direction retracted on 2026-08-08. | `docs/SPEC_revenue.md` "The institutional caveat on the Money tooltip", `web/index.html` (`viewTooltip` money branch), `tools/profiling/verify-inst-caveat.js` |
| 2026-08-15 | **SHARE decides the WORDS, CONSEQUENCE decides the GEOMETRY — the institutional band narrows to the hoods whose two worlds actually draw differently, on BOTH lenses** (Peter: *"if we see how badly some prisms drop, can't we just isolate the worst drops as ones we should make into hollow prisms"*). ⚠️ **This AMENDS the 2026-08-12 decision above**, which used the ≥25% share for both and over-selected at the floor: **RIVER VALLEY CAMERON moves 0 rank places and 0.02 of the ramp, EVERGREEN moves $87 against a ±$21–48k clamp, and U OF A FARM's endpoints both clamp past the deficit floor and draw the same prism.** `INST_UNCERTAIN_MIN` (0.25 share) now gates the tooltip caveat only; new `INST_CONSEQUENCE_MIN` (0.25 of movement on the lens's own colour ramp) gates the outlined range. **Money: 15 caveat, 6 hollow** (Northlands, U of A, Tawa, West Meadowlark, U of A Farm, Virginia Park) — **only 2 of the top 11**, so Downtown/Wîhkwêntôwin/Garneau/Spruce Ave/Central McDougall/Boyle St keep their colour and the central core survives; same six under both denominators. **Lab: 15→9 on Total, 13→8 Non-res, 2→1 Residential**, and the six it drops KEEP the caveat rows — narrowing the range must not drop the warning. A hollow prism prints a **range**, never a single figure. ⚠️ **MEASURED ON THE RAMP, NEVER IN DOLLARS**: SPRUCE AVENUE has the city's second-largest dollar drop ($30,310/acre) and moves **12 rank places / 0.08 of the ramp**, while EDMONTON NORTHLANDS drops fewer dollars and swings **0.62** — a dollar ranking hollows the wrong hoods. ⚠️ **FIXED TRANSFORM, not `state.colorAdjust`** — a display preference must not change which prisms exist (linear silently drops U of A Farm); the DENOMINATOR toggle is followed, because lot acres are a different number rather than a different rendering. **Structural, both asserted: an inverted band can never be drawn** (inversion needs the hood to lose less than the average's $1,303/acre, so its span is under $1,303 against $21,470/$48,047 clamps — Δt < 0.061 < 0.25), and **Δt ≥ 0.25 contains every zero-crossing band** so no sign-flip rule is needed. ⚠️ **THE OUTLINE IS `#2ec4ff` AZURE, NOT THE LAB'S WHITE, AND THAT IS MEASURED**: `glow` peaks near-white at `#fff6e4` by design, where white scores **ΔE 3.5 normal / 3.4 deutan** — invisible on exactly the tall hoods it marks. No achromatic value clears 15 (mid-grey 8.6 vs cividis's `#7c7b78`), and no single colour clears all four ramps, so **FORM carries the shared identity and colour is per-lens**. Azure: worst pair **21.5 normal / 19.5 CVD** over 15 stops + 3 backgrounds, 27.1 vs set-aside grey. The Lab's "no cool hue" rule does not apply — it exists because a cool hue leans toward that lens's TEAL SURPLUS POLE, and Money's sequential ramps have no poles. Rejected: ranking by absolute dollar drop (picks Spruce Ave over Northlands), rank movement (unstable — depends on how crowded that part of the distribution is), and one shared outline colour (no such colour exists). ⚠️ `verify-money-denom.js` probed U of A and had to move to DOWNTOWN — U of A is hollow in both denominators now, so its colour check was passing only because transparent ≠ grey. | `docs/SPEC_revenue.md` "The consequence tier", `web/index.html` (`INST_CONSEQUENCE_MIN`, `instShiftMoney`, `instShiftDeviation`, `instHollowMoney`, `instCaveatOnly`, `instBandLayers`, `INST_OUTLINE_COLOR`), `tools/profiling/verify-inst-caveat.js`, `tools/profiling/verify-deviation.js` |
| 2026-08-16 | **Money's banded hoods render as TRANSLUCENT PRISMS at alpha 128 with opaque edges, not bare wireframes** (Peter: *"given that this lens has no transparent prisms, can't we just make these into 50 percent transparent blue prisms"*). **The premise was checked and holds:** there is no `money` entry in `VIEWS`, so its prisms are fully opaque and the prism-opacity slider is hidden there — translucency is an unused channel on this view (elsewhere it means ghost/context: `ratio` 5%, `uses` 35%, `development` 50%, `glass` 60%). Bare wireframes were the Lab's own weak point (*"that hollow prism is super hard to see"*, 2026-08-12), and legibility matters more on the default view than in the Lab. ⚠️ **THE 2026-08-12 "NEITHER ENDPOINT IS SOLID" RULE IS KEPT, NOT BROKEN** — it exists to stop one unknowable world being asserted over the other, so **both endpoints carry the SAME alpha**; it never mandated wireframes. `verify-inst-caveat.js` pins the two alphas equal and the edge at 255. ⚠️ **THE DENSER BASE IS FREE AND IT IS THE RIGHT SEMANTICS**: the prisms overlap from ground to the exempt height, so that section composites to ~75% while the cap stays at 50% — and on an **absolute rate** the lower endpoint is NOT one of two unknowable worlds, it is the revenue levied in both, so a denser floor under a fainter cap says exactly *this much regardless, that much depending on an unpublished status*. **The Lab cannot borrow this** — both its averages move, so neither of its endpoints is the certain one, and it keeps bare white outlines. Layer order (exempt before levied) is asserted because it is what produces the effect. ⚠️ **THE OPAQUE EDGES ARE LOAD-BEARING**: a translucent fill composites with whatever is behind it, so #2ec4ff's measured ΔE 21.5/19.5 stops holding pointwise once it goes see-through — the wireframe stays at full alpha and carries the contrast guarantee while the fill only has to make the mass legible. Rendered and compared at 128/128 against a denser-base variant (165/70) before choosing; 50/50 already produces the base/cap reading via overlap, so the exaggerated variant was unnecessary. `instHollowMoney` renamed `instBandedMoney` — the prisms are no longer hollow and a stale name is how the next reader gets misled. | `docs/SPEC_revenue.md` "Translucent prisms, not wireframes", `web/index.html` (`instBandLayers`, `INST_BAND_ALPHA`, `instBandedMoney`), `tools/profiling/verify-inst-caveat.js` |
| 2026-08-16 | **The citywide budget readout is a PANEL, not a lens, and never enters `#views`.** Branch budgets are citywide totals with no neighbourhood dimension, so there is nothing to draw on the map and none of the hooks a view requires (`buildViewLayers`, `viewTooltip`, `refreshLegend`) has anything to fill; forcing it into the Lab would have produced a "view" with no prisms. Same boundary `city_budget_context.json` already states. Placed in the left column on the `#millrates` idiom rather than the bottom-right pod stack, because `#a11y-menu`'s `calc(200% + 8px)` is a measured fix for clearing a TWO-button stack and a third pod would silently overlap it again. ⚠️ Rejected a z-index bump for the `#botleft` collision (it buries the legend instead of clearing it); the height cap clears the compass and legend instead — the list ran **117px through them**, identically at 900/800/720, while a viewport-only assertion passed. Desktop-only by decision, not by omission. | `data/DATA.md` §18; `tools/profiling/verify-budget-panel.js` |
| 2026-08-16 | **The budget ranking splits "delivers a service" from "only moves money", and the split is DERIVED from each branch's category mix — never a branch-name list.** `Capital Project Financing` is $687.6M, larger than Police, so an unsplit ranking answers "what does Edmonton spend most on?" with debt service. ⚠️ Rejected classifying by *category*, measured wrong: `Transfer to Reserves` spans 11 branches **including Police ($14.3M)**, so a category filter strips service branches of their own budget. ⚠️ Rejected a hardcoded branch list: the budget tree gets re-cut (two re-cuts inside Parks and Roads alone) and a name list misclassifies silently on the first rename. `Corporate Expenditures` lands in "other" despite not being debt — it delivers no service. FY2026: 43 service / 5 other. | `data/DATA.md` §18 |
| 2026-08-16 | **Socrata `da9s-v9j8` is the SAME publication as `budget.edmonton.ca/api/operating_budget.csv`, and the two must never be cited as corroborating each other.** Arrived as a "new dataset" suggestion; verified by identity instead — same 8 columns, same 7,283 rows, same FY2017–FY2026, and FY2025 tax-supported ($3,855,881,010) plus FY2026 Parks and Roads ($307,325,053) tie **to the dollar**. Adopted anyway for the host, not the data: `data.edmonton.ca` is the host the pipeline already uses. ⚠️ It is an *approved* budget (`rowsUpdatedAt` 2026-06-05), so it is NOT continually updated and must not ride the weekly refresh — manual reviewed input on the mill-rates pattern. | `data/DATA.md` §18, §17 |
| 2026-08-16 | **The budget panel's phone form is a BOTTOM SHEET — the mill-rates "does it need its own surface?" test was applied and came out the other way.** `DECISIONS.md` 2026-08-01 locked *"on a phone, ask whether a thing needs to be its own surface before asking where to put it"*, and four mill rates fold into the blurb card; **43 ranked branches plus a note cannot**, and folding them would bury the blurb they were folded into. So this takes `#temporal`'s answer instead, for `#temporal`'s reason: its desktop home (left column under the title) is where `#controls` lives at ≤640px (`#title` collapsed is 20–43, `#controls` owns **58–197**). ⚠️ **The 2026-08-04 bottom-sheet refusal does not reach it** — that refused moving the CONTROLS into a sheet; this is a readout. Near-opaque `0.985` not the desktop `0.92`, because a sheet sits over the legend and both bottom-right pods and their labels read through at 0.92. Capped at **52vh** so the map stays the subject. ⚠️ **The body scrolls, not the panel** (the `#temporal-body` split): with the panel as scroller the `$4.04B` that every percentage is a share OF scrolls away first. Phone-only `×`, because a sheet is at the opposite end of the screen from its opener. ⚠️ Verified by **real `tap()`**, not `.click()` — the standing caveat that verify scripts bypass `pointer-events`. Long branch NAMES wrap at 360/320; the **values never split**, which is the mill-rates failure mode, and that is asserted rather than eyeballed. | `web/styles.css` (mobile seam), `tools/profiling/verify-budget-panel.js`, `docs/CONTROLS_MATRIX.md` |
| 2026-08-16 | **The hover sparkline rides only the tooltips whose panel IS the history panel — the revenue cuts now get the invite and NO chart.** AMENDS 2026-07-29's *"the sparkline rides every view's tooltip"*, whose unstated premise (clicking always opens the history) expired on 2026-08-01 when the revenue cuts got the zone-mix panel; that change rewrote the invite's **wording** and left the **chart**, so Money/Glass on a revenue cut plotted assessment share over a click that delivers a revenue breakdown. Rejected: dropping the invite too (click-to-open is undiscoverable without it, 2026-07-29) and dropping the teaser from Value (there the click really does open the chart). ⚠️ **Services is the same defect and is deliberately still live** — its own cost panel since 2026-08-10, its hover still says `click to pin` under a history line; the replacement copy is Peter's call (`TODO.md`). | `web/index.html` (`tooltipFor`), `tools/profiling/verify-hoodmode.js`, `docs/SPEC_temporal.md` §2 |
| 2026-08-17 | **A banded prism is its own hover/tap target — Money's band layers become pickable, the Lab's get a separate INVISIBLE pick target with `depthMask: false`.** Peter: *"i want people to be able to just mouse over prisms… cuz they're gonna wanna see why they're special color."* ⚠️ **The defect was not a missing tooltip but the WRONG HOOD'S**: a banded hood's own geometry is flattened to 0 and painted `[0,0,0,0]`, so an unpickable band fell through to whatever stood behind it (measured at pitch 60 over the U of A: MCKERNAN, RIVER VALLEY VICTORIA, WÎHKWÊNTÔWIN) — and a flat overhead check cannot see it, because at pitch 0 the transparent footprint picks correctly. Rejected: making the Lab's OUTLINE layers pickable (with `filled:false` only the 2px wireframe picks) and giving them an alpha-0 fill instead (**499 px** of render — a depth-writing fill hides the prism's own back edges and the see-through cage becomes a box, which would also have broken the existing *outline only* assertion); rejected `depthMask` left on (**818 px**). Both fixes measured at 21–24 px against a **45 px** no-op-rebuild noise floor. ⚠️ `autoHighlight` stays OFF on every band layer — the pick lands on one of two shells and lighting one is the primacy the band refuses. | `web/index.html` (`instBandLayers`, `deviationBandLayers`), `docs/SPEC_revenue.md` "The banded prism is its own hover target", `tools/profiling/verify-inst-caveat.js`, `tools/profiling/verify-deviation.js` |
| 2026-08-18 | **The band's hover glow is INDEX-DRIVEN so both shells light as one — `highlightedObjectIndex`, not `autoHighlight`.** AMENDS the 2026-08-17 row, whose `autoHighlight: false` bought the no-primacy rule at the cost of any hover confirmation at all: on the shipped build, hovering a banded prism moved **0 pixels** (Peter: *"lacking proper confirmation you hovered (or tapped on mobile) on the intended hood"*). Rejected `autoHighlight` again, now on measurement as well as principle — it lights only the picked shell, and in the Lab that is the crossing band's **downward, mostly buried** endpoint, which wins the pick and glows on **276 px** against the levied prism's **19,654**. Lighting BOTH is what satisfies the no-primacy rule, rather than lighting neither. Standard `[255,255,255,60]`: Money's band measures mean Δ73 and the Lab's Δ98 against an ordinary prism's Δ34, so no alpha bump (110 and 160 were measured and dropped). ⚠️ **A layer rebuild CLEARS the index** — it is subset-relative and the revenue-cut/denominator toggles re-select the banded set, so carrying it would light the wrong neighbourhood. ⚠️ Hover clones the live layers instead of calling `buildLayers()`, which would recompute ~89k `topRings` vertices per hover transition. | `web/index.html` (`bandHover`, `buildLayers`, `instBandLayers`, `deviationBandLayers`), `docs/SPEC_revenue.md` (the amendment under "The banded prism is its own hover target"), `tools/profiling/verify-inst-caveat.js`, `tools/profiling/verify-deviation.js` |
| 2026-08-18 | **The Industrial metric gains a 100 m detail grid, and it is measured in DEFLATED construction value — not permit count.** Peter: *"can we not have a 100 m grid activity map for industrial?"* Measured before building: **89% of 5yr industrial cells hold exactly one permit** (81% long), so a count grid is a dot map wearing a density map's clothes. ⚠️ **Rejected a bigger cell, on measurement**: 100 m → 400 m is a **16× area increase that removes 19 of 184 cells** (singletons 89%→78%), and even 800 m leaves 145 cells and 75% singletons — industrial permits are not clustered at the sub-kilometre scale, so merging cells merges nothing; the $ spread is near-invariant to cell size (281× at 100 m, 292× at 400 m), which is what proves the differentiation comes from permit SIZE. The 100 m cell also keeps the shared geometry with the Glass grid. ⚠️ **Rejected `floor_area` as the intensity measure** — populated on only **51%** of industrial permits vs `construction_value`'s 99.6%. ⚠️ **The dollars are a DECLARED ESTIMATE at permit application** (the City's own field description), not audited spend: the permit fee derives from it, land is excluded, and 78% of values end in `000`. Blurb says so. | `docs/SPEC_industrial.md` A3 (amendment), `data/DATA.md` §10, `src/load_permits.py` (`export_dev_grid`) |
| 2026-08-18 | **Construction values are deflated to constant 2025 dollars from a PINNED, manually-reviewed StatCan index — nominal sums were encoding inflation as development.** An identical building permitted in 2009 drew a spike **1.72× shorter** than a 2025 one; the permits' own warehouse $/sqft corroborates independently at 1.92×. Even the 5yr window is not safe (2021→2025 = 1.33×). Source: **StatCan 18-10-0289-01**, Edmonton / `Industrial buildings [62211]` / `Division composite` — the type-matched series, deliberately NOT the all-non-residential one. ⚠️ **Both predecessor tables are ARCHIVED** (18-10-0135 ends 2022-Q2, 18-10-0276 ends 2024-Q2) and **still download and still answer queries**, so a stale pin fails SILENTLY — the fetcher checks `archiveStatusEn` and warns. ⚠️ **NOT on the weekly refresh** (mill-rates pattern): a price index that moved silently would restate every historical spike at once. A permit year with no deflator **hard-fails** rather than passing through at nominal. | `scripts/fetch_construction_price_index.py`, `data/construction_price_index.json`, `data/DATA.md` §10 |
| 2026-08-18 | **Industrial cells are kept on the permit COUNT, not the dollars, and floored to 6 m — a $0-declared permit must not vanish.** 12 of 1,281 industrial permits declare exactly $0 (118 ≤$10k); on a dollar-driven height those are permitted buildings simply not on the map. ⚠️ **The floor is small on purpose, and the first attempt was wrong**: 60 m lifted **64% of 5yr cells and 78% of long cells** — including projects worth up to **$4.6M** — to the same height as $0, erasing the differentiation the dollars exist to provide. 6 m reaches only cells under ~$460k, all of which rendered under 6 m anyway. ⚠️ **Why residential never needed this**: dwelling units are QUANTISED (a permit adds ≥1, so **0%** of residential cells render under 5 m) while declared dollars are CONTINUOUS to zero (**39–44%** of industrial cells do). Display height only — the tooltip dollars are never floored. ⚠️ **Consequence not resolved**: the 2026-07-23 row tagged Industrial `/full/`-only *because* it was choropleth-only and would leave the Detail selector with dead options; that rationale no longer holds. Whether it goes public is Peter's call — nothing was changed. | `web/index.html` (`DEV_GRID_MIN_M`, `devGridLayer`), `tools/profiling/verify-ind-permits.js` |
| 2026-08-18 | **`Engineering (490)` and `Transportation Terminals (440)` are REMOVED from `INDUSTRIAL_BUILDING_TYPES` — they are parkades and LRT, not industry.** Found because the new 100 m grid drew a **$91.2M spike on DOWNTOWN** (Peter: *"uh what are those downtown industrial permits"*) — a four-storey underground parkade. Measured on `job_description` 2009–2026: **490 is 95% parkades** ($189.3M of $202.1M, $10.5M anything else); **440 is 100% LRT/transit** ($326.1M stations/pedways/platforms + $33.4M parkade vs $1.1M else). Together they were **19% of the metric's dollars**. ⚠️ **The full-string-enumeration rule did NOT prevent this, and the SPEC's own comment shows why we thought it would**: it assumed one physical thing carries one label, but the City files underground parkades under BOTH `Parkade (490)` and `Engineering (490)` — excluding the obvious string left the same buildings in under the other. **Enumerating by string only helps if the strings partition the things; check what is IN a category, not that its name sounds right.** ⚠️ **Rejected stripping 450/480 too** (30%/24% transit dollars): LRT maintenance shops and utility facilities are genuine industrial operations, and separating them needs `job_description` keyword matching, which this project avoids. ⚠️ **Rejected keeping them and renaming the metric** — the neighbourhood ranking would still put downtown parkades beside warehouses. Effect: 5yr permits 285→249, dollars −15%; **7 hoods drop to zero and all 7 are RESIDENTIAL** (BONNIE DOON, GORMAN, LYMBURN, POTTER GREENS, SUMMERLEA, TWIN BROOKS, WEST JASPER PLACE). ⚠️ **The choropleth carried this for a month unnoticed** — per-acre counts smeared it across a hood; the spike could not hide. **A finer rendering is a correctness check on the aggregate above it.** | `src/load_permits.py` (`INDUSTRIAL_BUILDING_TYPES`, `NON_INDUSTRIAL_400_SERIES`), `docs/SPEC_industrial.md` "Two 400-series types are not industrial", `data/DATA.md` §10 |
| 2026-08-19 | **The Glass 100 m grid marks institutional uncertainty at CELL level — `inst_frac` ships in `value_grid.json` and flagged cells get Money's two-endpoint azure band** (Peter: *"could we have a very transparent prism over those neighbourhoods... or like, any any spike that has a large percentage of possibly exempt data inside it be made blue"*). ⚠️ **It was never blocked on data, contrary to `TODO.md`**: `revenue_by_zone` already computed a per-property zone category and discarded it after its neighbourhood `groupby`. Now extracted as `property_zone_categories`, computed ONCE and passed to both consumers — a second join would cost another ~440k point-in-polygon lookups AND let the hood and cell shares drift apart. ⚠️ **ONE threshold, not the hood level's two, and the distribution is the argument:** of 714 cells above 10% institutional, **467 are above 99%** and only 159 sit between 10% and 50% — at 100 m a cell is institutional or it is not, so a consequence tier would separate almost nothing (the hood equivalent narrowed 15 hoods to 6). Moving the threshold across 0.10–0.99 changes the marked set by 247 of 34,671 cells. `GLASS_INST_MIN` = 0.25, matching `INST_UNCERTAIN_MIN`. Marks **624 cells (1.8%), 4.9% of city levy**. ⚠️ **The ramp fill SURVIVES here, which Money's band deliberately destroys** — a knowing divergence: Money flattens a banded hood so "a ramp-coloured floor under the band" cannot assert the withheld value, but doing that to 624 cells would delete them from a view whose entire content IS ramp-coloured cells, including the **tallest flagged cell in the city ($5.66M/acre, 100% institutional, inside the top 100 by height)**. Losing the magnitude of a huge uncertain spike is a worse error than over-asserting it. ⚠️ **The wireframe "cage" of the first attempt was REJECTED BY MEASUREMENT, not taste:** deck 9.0.38's `GridCellLayer` ignores `filled: false`, draws its **default opaque black** fill and declines to render the wireframe — the flagged cells came out solid black, and **every layer prop read back correctly, so only the screenshot caught it**. Final form is Money's own: two filled prisms at the SAME alpha (levied base + full-height cap), denser where they overlap. ⚠️ **Azure survives all three ramps as a translucent FILL** (it had only been validated as an outline): mint over cividis's yellow high end, bright cyan over its navy low end, ice-blue against `glow`'s near-white peak — the case that defeats a white outline. Revenue cuts only, same rule as the hood caveat. | `src/revenue_by_zone.py` (`property_zone_categories`), `src/export_value_grid.py` (`inst_frac`), `main.py`, `web/index.html` (`GLASS_INST_MIN`, `glassInstBandLayers`, `glassInstBlurb`), `tools/profiling/verify-glass-inst.js` |
| 2026-08-22 | **The capital budget enters the repo as a committed manual reviewed input, and the monthly digest watches it by CONTENT because there is no date to watch.** `data/capital_budget.csv` (1,884 rows, FY2023–2037, $11.51B) hand-fetched from `budget.edmonton.ca/api/capital_budget.csv` — §17's host, whose capital endpoints had never been probed. ⚠️ **The open data portal has NO capital budget**: `da9s-v9j8` and `m84q-ghmu` are both OPERATING, `552h-hjwj` is a 214-row app feed, and the only other candidates are 2015 relics — do not hunt for a Socrata sibling again. ⚠️ **No freshness header exists** — `Last-Modified` merely echoes `Date` behind `Cache-Control: no-cache`, so unlike Socrata's `rowsUpdatedAt` **the committed file IS the pin** and the check diffs content. It hashes **sorted** rows, not raw bytes: the endpoint is byte-stable today (two fetches identical) but is generated per request, so a server-side reorder would otherwise be reported as a budget change. Cadence is a **four-year cycle** moved by supplemental adjustments — never the weekly refresh, and nothing in `src/` or `main.py` reads it, so a re-fetch cannot change the live site. ⚠️ **New-vs-renewal is NOT a published field** — only two profiles say it outright; splitting the rest on words like "Reconstruction" is the T8 hand-enumeration shape (a value-sum over a name-matched set with no self-check) and is **indicative only, not publishable**. ⚠️ **Grain is not one row per project** (1,884 rows, **399 profile_ids**) and **`approved` goes negative** (87 rows, −$161.3M funding-source swaps that straddle fund types), so naive `len()` and `fund_type` filters are both wrong. ⚠️ **A point map would omit 25% of the dollars NON-RANDOMLY** — only 104 of 399 profiles carry a coordinate ($8.58B of $11.51B placed); what falls out is fleet replacement and LRT/road corridors, i.e. things a point could not honestly represent. | `data/capital_budget.csv`, `scripts/vintage_report.py` (`check_capital_budget`, `_capital_fingerprint`), `tests/test_vintage_report.py`, `data/DATA.md` §19, `docs/RUNBOOK.md` §1a, `docs/ANALYSIS_BACKLOG.md` §11 |
| 2026-08-22 | **A cell-grain Infill score measures activity over a RADIUS, not inside the cell — and proximity to LRT/schools rides ALONGSIDE the score as an attribute + filter, never as a weighted term in it.** Peter's two calls on the granularity question. ⚠️ **The radius is not a refinement, it is what makes the metric exist**: at 100 m the default activity column is **88% zeros**, so `−(z(far) + z(activity))` degenerates to `−z(far)` plus a constant for 88% of cells and the lens becomes an inverted-FAR map still labelled *mismatch*. A 600 m disc reaches **73.0%** non-zero (400 m → 58.8%, 800 m → 81.8%); the curve has **no cliff**, so the radius is a judgement, and 600 m is also the conventional TOD walkshed. The hood unit hides the whole problem — every hood has some activity. ⚠️ **Proximity is a DESIRABILITY input, not an UNDERUSED input**: folding it into suitability turns a descriptive metric into a weighted index whose weights nothing can falsify, against Lens B's own framing of the view as *relative and exploratory, not a target*. As a filter the reader sees both numbers and the conjunction ("underused AND near transit") stays inspectable. | `docs/FINDINGS_infill_granularity.md`, `docs/SPEC_development.md` Lens B |
| 2026-08-22 | **Distance to a transit station is ROAD-NETWORK distance, not straight-line — measured, not assumed.** A multi-source Dijkstra from the 33 GTFS-derived LRT stations over a 186,931-node graph built from `data/raw/roads.geojson`, against 3,982 sampled properties: at a 600 m band **55% of the properties euclidean calls in-band are false positives** (400 m → **68%**, 800 m → 48%); median network/euclidean ratio **1.35**, p99 2.55, max **7.90**. ⚠️ **The error runs in the direction that matters** — it manufactures transit-adjacent opportunity that cannot actually be walked to; the river valley and the rail/freeway corridors are the mechanism. The network version costs one Dijkstra over data already in the repo, so the euclidean form has no cost argument behind it either. ⚠️ **Deriving the stations is itself a `T8` membership question**: light-rail routes → trips → `stop_times` → `parent_station` yields 33 parents, and that set includes `Heath Sciences Tail Track`, `Kathleen Andrews Platform` and `DL Macdonald Platform` — tail track and bus garages, not passenger stations. The 58 `location_type == 1` stops are not the answer either; that set mixes LRT with bus transit centres. | `docs/FINDINGS_infill_granularity.md`, `data/raw/roads.geojson`, `data/raw/gtfs_stops.csv` |
| 2026-08-22 | **`far` is NULL where no floor area was recorded — a missing `gross_area` must not sum to 0, because 0 is the maximum-OPPORTUNITY end of the Infill scale.** `gross_area` is null/zero on ~6.2% of rows and `build_hood_lot_acres` summed it with `NaN → 0`, so absent data and nothing-built produced the same number — and the number happened to be a finding. **Null and zero are treated identically** (DATA.md already counts them together as *not recorded*), masked before the sum, `min_count=1`. Effect: **16 of 410 hoods go null**, 12 already set-aside grey, **4 newly off the scale, 1 of them residential (EVERGREEN — 4 eligible rows, none with a floor area)**. ⚠️ **EVERGREEN's teal was never a measurement**, which amends `SPEC_development.md` Lens B's *"legitimately stays teal"*. Re-scored before/after: population 358→348, `clampPos` 0.765→0.774, **WESTVIEW VILLAGE still saturates and becomes #1**, MCLEOD joins the saturating set, ordering otherwise unchanged — verified, not predicted. ⚠️ **The shipped lens was NOT wrong before this** — 69 in-scale hoods exceed 50% missing but only 2 are residential, so the asymmetric residential gate was already barring the rest from the teal end; **the gate has been absorbing a data-completeness gap as well as a land-use one**, undocumented until now, and that is exactly what it cannot do per cell. ⚠️ **PARTIAL coverage is deliberately NOT handled**: MAPLE RIDGE records floor area on ~33% of rows, keeps an understated `far` and sits #2 on the teal arm. A threshold or a coverage-scaled FAR needs a *what fraction is enough* call — the 0.90 set-aside shape — and was not guessed. | `src/export_value_grid.py` (`build_hood_lot_acres`), `tests/test_export_value_grid.py` (3 tests), `data/DATA.md`, `docs/SPEC_development.md` Lens B (amendment), `docs/FINDINGS_infill_granularity.md` §6a |
| 2026-08-23 | **The LRT station set is decided STRUCTURALLY — a passenger station has a street entrance — not by a hand-written name list.** The T8 membership pass the granularity findings owed. Deriving stations from the light-rail routes gives **33 parents**, three of which are not passenger stations (`Heath Sciences Tail Track`, `DL Macdonald Platform`, `Kathleen Andrews Platform` — a tail track and two bus-garage platforms). ⚠️ **Trip counts cannot separate them**: they sit on the alignment and take a scheduled time on *every* trip, so two of them tie exactly with Belvedere and Clareview at 3,601. What separates them is `location_type == 2` **entrance** children — the three have **zero**, all 30 real stations have **at least one**, a clean split with nothing near the boundary. Rejected: a committed exclusion list (freezes a judgement the feed can outdate) and the 58 `location_type == 1` stops (mixes LRT with bus transit centres). The dropped set is logged every run, so a feed that gives a garage an entrance surfaces instead of silently resizing the amenity set. | `data/DATA.md` §20, `docs/ARCHITECTURE.md` §load_transit |
| 2026-08-23 | **Railway centrelines are excluded from the walk graph as a CORRECTNESS filter, and the same filter leaves the graph better connected.** Routing over railway centrelines lets a walk travel *along the LRT track itself* to reach an LRT station — a shortcut no pedestrian has. ⚠️ **S115's probe did not have this filter**: its 186,931 nodes are the all-rows count, so its network distances could take that shortcut, biasing them toward euclidean and **understating its own 55%-false-positive conclusion**. Keeping only `centerline_type == "Road"` gives **163,841 nodes at 99.83%** in one component against **186,931 at 99.45%** unfiltered — fewer nodes AND better connected, because alleys and railways contribute disconnected fragments. `responsible_party` is deliberately NOT filtered (unlike `load_roads`): a provincial or private road is still walkable, and excluding it carves holes. | `docs/ARCHITECTURE.md` §amenity_distance, `data/DATA.md` §20 |
| 2026-08-23 | **A cell's amenity distance is the MEDIAN of its properties, not the minimum.** The minimum would let one corner property near a station make a whole 100 m cell read as served — the same optimistic error, in the same direction, as the euclidean distance the network graph exists to replace. The remaining approximation runs the safe way too: points snap to the nearest graph **node** rather than the nearest point on an edge, which always ADDS distance (median 3.5 m of excess over 20,000 real properties, p90 35 m, 0.19% over 100 m), so a "within 600 m" filter under-claims proximity rather than manufacturing it. Rejected: cell-centroid distance (a centroid can land in a park or across a freeway from every property it represents). | `docs/SPEC_development.md` "Amenity distance", `data/DATA.md` §20 |
| 2026-08-23 | **"Distance to school" means distance to a CATCHMENT school — the 19 city-wide/specialized programs are excluded by enumerated category.** EPSB `sch_type == 'SP'` (15) and ECSD `grade_level == 'Outreach'` (4) sit in the same tables as neighbourhood schools but are Learning Store storefronts, the Glenrose Hospital school, Metro Continuing Ed., the AB School for the Deaf and the CCAC outreach centres — nobody walks a child to those from the next block, and including them would put a fake "school nearby" on downtown and hospital cells. Membership is an explicit dict per board (the `ZONE_CATEGORY`/`ROUTE_MODE` philosophy); an unknown category is **KEPT and logged**, never silently dropped. ⚠️ **Coverage gap by source, not by choice:** private, charter and francophone schools are absent from the portal entirely, so the column overstates distance wherever one of those is nearest. | `data/DATA.md` §20, `docs/ARCHITECTURE.md` §load_schools |
| 2026-08-23 | **The amenity filter is TWO independent bands, not one "amenity-served" switch — and out-of-band cells DIM rather than disappear.** The two amenities are ~14× apart in what they select: at 600 m the LRT band is **1.6%** of cells and the school band **21.8%**, and ANDing the conventional pair (LRT 600 m + school 800 m) leaves **276 of 34,671 — 0.8%**. A single switch would hide that collapse behind one word and would be **a weighted index by another name**, which is precisely what the 2026-08-22 decision refused for the score; two rows make the reader watch the collapse happen. Dimming over hiding for the same measured reason: at 1.6% a hard hide leaves scattered dots with nothing to place them against, and it keeps ONE stable cell array so a toggle cannot re-tessellate 34k cells. ⚠️ **A null distance is OUT of band** — the 0.1% of properties reaching no amenity over the road graph must never read as "near". ⚠️ **The grey is `SET_ASIDE_COLOR`, which the legend already spends on set-aside land**, so the blurb must say grey means "out of band, not zero and not set-aside". | `docs/UI.md` "Amenity bands", `docs/CONTROLS_MATRIX.md` §4, `docs/SPEC_development.md` "Amenity distance" |
| 2026-08-23 | **The break-even lens locks OPERATING basis + OPERATING-ONLY coverage denominator — $3,055.2M, not the full $3,855.9M budget.** `SPEC_breakeven.md` §8 #1/#7, settled together because they can contradict each other made separately. Operating basis: the larger, better-sourced universe (§4a, 656 rows/144 programs, already ingested), vs. lifecycle's two unit costs and a bikeway service life Edmonton doesn't publish. Operating-only denominator: an operating-basis numerator over the full budget is 20.8% capital, the exact basis-mixing §3 forbids elsewhere — just hiding in the coverage ratio instead of the composite, the one place the hard-error check can't catch it. Coverage reads **15.5%, not 12.3%** — also the higher, more flattering number, so no correctness-vs-optics tradeoff was made. ⚠️ **Disclosure debt this creates**: the lens must state the $800.7M capital exclusion explicitly, and `Alley Renewal` ($174.4M of it) is allocatable lifecycle spend, not unallocatable — must not silently drop out of the register once lifecycle-basis work exists. Nine sessions (S108–S116) sat on this before it was decided. | `docs/SPEC_breakeven.md` §0, §8 |
| 2026-08-25 | **Infill's new 100 m amenity grid is a NEUTRAL highlight, not a real per-cell score.** Peter asked why the amenity-distance filter (originally requested for Infill, `DECISIONS.md` 2026-08-22) ended up in Glass instead — answer: Infill had no 100 m grid of its own to host it on. Given three options (neutral filter-only grid; cells inherit their parent hood's colour, which needs a new hood-id join in `export_value_grid.py`; the real per-cell suitability score), Peter picked the cheapest: reuse `value_grid.json` as-is, no data-contract change, hood-level `infill-plane` stays exactly as it renders today. The real per-cell score stays blocked on `ANALYSIS_BACKLOG.md` §12 items 3 (a per-cell residential-gate equivalent) and 5 (height semantics for a diverging score) — this highlight answers neither and doesn't presuppose an answer. | `docs/SPEC_development.md` "Amenity distance" § Extended to Infill, `docs/UI.md` "Amenity bands" § Extended to Infill, `docs/CONTROLS_MATRIX.md` §4 |
| 2026-08-25 | **The `edmonton.ca is unreachable from the Oracle box` blocker asserted in the 2026-08-12 row above is RETRACTED — it was a mis-diagnosis, and correcting it produced the external anchor the project believed did not exist.** The `curl` `000` reproduced, but the cause was never checked: DNS resolves, TCP connects, TLS negotiates and the City presents a **valid** certificate (`CN=*.edmonton.ca`, Entrust OV TLS Issuing RSA CA 2 → **Sectigo Public Server Authentication Root R46**); this box's `ca-certificates-2023.2.60` bundle (142 roots) simply lacks that root, so the chain-embedded root reads as `self signed certificate in certificate chain`. Fetching with `certifi`'s bundle returns 200. **Consequences, both material:** (1) the open-data request's submission channel is `opendata@edmonton.ca`, read from the live portal footer — the request is unblocked and awaits only Peter's decision to send; (2) ⚠️ **the "no City-published total exists to check our $2.71B against" premise is FALSE** — Alberta FIR **Schedule MR** (the same workbooks `scripts/fetch_fir_debt.py` already downloads for Schedule AA) publishes Edmonton's 2025 **taxable assessment and municipal tax levy by class**, and its mill rates match ours exactly. ⚠️ **A decision this file recorded as logistically blocked was actually blocked by a client-side trust gap for 13 days — re-measuring a stale blocker means re-deriving its CAUSE, not re-confirming its SYMPTOM.** | `TODO.md` (open-data-request item), `data/DATA.md` §11 |
| 2026-08-25 | **The assessment roll's year is derived FROM THE DATA, not from Socrata metadata — and the pin rolled to 2026.** `check_year_alignment.py` decided the roll year by parsing the hand-maintained `Period of Coverage` string; Edmonton left it reading `2025-01-01 to 2025-12-31` through the whole 2026 roll, so pin and metadata agreed with each other while both were a year stale, **no banner ever raised, the monthly digest printed "aligned", and the pipeline billed a 2026 roll at 2025 mill rates** (citywide levy understated $69.5M / 2.5%). The replacement measures parcels: `check_roll_year_against_fir.py` compares our residential taxable base against Edmonton's FILED base (Alberta FIR Schedule `MR(2)`, `data/fir_tax_base.json` via `fetch_fir_tax_base.py`) — residential land is barely exempt, so the right year fits within ~1% and a neighbouring year misses by ~10%. `check_year_alignment.py` is kept but downgraded: a coverage year older than the calendar year now returns **INCONCLUSIVE, never "aligned"** — deliberately not HOLD, because an untrustworthy string is not evidence about the roll and a banner would publish a guess. ⚠️ **The general rule: a guard that reads a publisher's free-text metadata field is not measuring the data.** Rejected: trusting `Period of Coverage` with a tolerance; auto-fetching FIR in the weekly refresh (kept manual/reviewed, the mill-rates pattern). | `docs/RUNBOOK.md` §1, `data/DATA.md` §21 |
| 2026-08-25 | **Exempt-candidate zoning is its own set (`EXEMPT_CANDIDATE_ZONES` = AJ/UF/UI/PU/**PS**), deliberately NOT the `inst` category — one mapping cannot answer two questions.** The map's levied/exempt uncertainty band read `ZONE_CATEGORY`'s `inst`, which excludes `PS` ("Parks and Services") because `PS` is `never` — **correct** for the development lens, since parks are not infill, and **wrong** for exemption, since parks are prime exemption candidates. The band therefore ignored **$88,038,783/yr (3.16% of citywide levy)** and drew park-dominated hoods as fully certain, MILL WOODS PARK worst of all: its **entire** levy sits on `PS` and it rendered with no band. Fixed by adding an independent set rather than moving `PS` into `inst`, which would have repaired the band and broken the development lens. Hoods over the 0.25 gate 17→24; tooltip caveat tier 15→21; band-prism set unchanged at 6 (consequence still decides geometry). Columns renamed `inst_*`→`exempt_*` through pipeline and client — a column named `inst_frac` that includes parks is a lie, and that mislabel is what hid this. ⚠️ **Candidacy is not exemption**: `UF`/`PU` include privately-owned facilities and Edmonton publishes no per-parcel exemption status; the set sizes an UNCERTAINTY and asserts nothing about any parcel. Rejected: merging the two sets; renaming `rev_frac_inst` (kept — the `rev_frac_*` family partitions levy and sums to 1.0, so `rev_frac_exempt` cuts across it as a new column). | `docs/SPEC_revenue.md`, `src/load_zoning.py` |
| 2026-08-25 | **A roll-year mismatch measured against FIR HOLDS the weekly publish — banner + last-good data, not a warning.** Wiring `check_roll_year_against_fir.py` into `refresh.yml` (it shipped the same day but ran in no workflow, so with the coverage string stale CI had **no positive confirmation of the roll year at all**) forced the question of what exit 3 should do there. Chosen: the full hold state the metadata guard already used. Reasoning: this guard **measures parcels**, so a mismatch is evidence, not the guess that made the 2026-08-25 row above downgrade `check_year_alignment.py` to INCONCLUSIVE-never-HOLD — and it is the precise failure that billed a 2026 roll at 2025 rates for months. False-positive risk is bounded by construction: the script returns 4, never 3, unless another year fits within 5% **and** beats the runner-up by 3%, so a hold means a clean rival year, not a wobble. ⚠️ **Two guards can now hold, so all seven publish steps gate on BOTH** and the banner step fires on either, preferring the FIR banner. A step gated on only one would regenerate and publish during the other's hold — `test_refresh_workflow_gates_every_publish_step_on_both_guards` parses the workflow and asserts it, because the way this regression returns is a future step copying a neighbour's `if:`. Rejected: warn-only (a warning in a green run reaches nobody — the same channel that hid the original roll); filing an issue as well (the banner already reaches visitors, and the hold is self-announcing). | `docs/RUNBOOK.md` §1, `.github/workflows/refresh.yml` |
| 2026-08-26 | **Public-facing evidence notebooks are STANDALONE (`notebooks/standalone/`), not `verified/` — portability was chosen over drift-protection, knowingly.** Peter picked this from three options (verified+CI-gated+published / standalone / exploration-only) for the exemption notebook, and it set the pattern for the `qi6a-xuwt` gap report too. A standalone notebook imports nothing from `src/`, downloads its own inputs from the public portals, and can be handed to a skeptic — or to the City — who has never seen this repo. ⚠️ **The cost is real and was stated before choosing: it restates class names, zone codes and one published mill rate that live authoritatively in `src/`/`data/`, nothing executes it on a schedule, and `docs/VERIFICATION.md` records that the previous walkthrough notebook rotted exactly this way.** Mitigations, not fixes: every restated definition is marked `ASSUMPTION` in place with what would falsify it; each notebook asserts structural invariants (11 and 6) and raises rather than printing nonsense; the committed outputs come from a COLD-CACHE run that fetched everything live, so the artifact proves its own portability. Rendered to `web/notebooks/*.html` — deliberately NOT added to `deploy.yml`'s exclude list, because unlike `web/data/**` and `web/verified/**` nothing regenerates these, so they are code and should deploy on push. Rejected: CI-gating them (adds weekly runtime and a failure surface for pages whose whole point is to be handed to outsiders); exploration-only (unpublishable, and nothing keeps it honest either). | `notebooks/standalone/`, `docs/VERIFICATION.md` |
| 2026-08-26 | **The amenity filter is INFILL-ONLY — the Glass copy is removed, and the removal is asserted rather than assumed.** The bands were built in Money's Glass mode 2026-08-23 for a reason that was never about Money: Glass was the only 100 m grid UI in the app and the distances ride on the `value_grid.json` it already renders. When Infill got its own 100 m overlay 2026-08-25 the gate was **widened** (`v === "glass" || v === "infill"`) rather than moved, so both views carried the same two checkboxes for a day; Peter, seeing them under Money → 100 m grid: *"remove it from money."* Kept in Infill because that is the lens the filter was asked for and the one whose question it answers ("where is there room near transit?"); two copies read as clutter, not as two questions. ⚠️ **Band state persists across view switches like the rest of the layers panel, so the Glass path can still be entered with a band ON** — the render must behave as though the filter does not exist, and `verify-amenity.js` enters Glass mid-run with the school band checked and requires the section hidden, `dimmed === 0` and no band sentence in the blurb. Deleted rather than left dormant: `AMENITY_DIM_ALPHA`, `amenityBlurb()`, the `glass-grid` fill branch and its `state.amenity` updateTriggers. ⚠️ **`ensureGridData()`'s fetch gate stays WIDER than the control gate** (`glass || infill`) — Glass still needs the grid file for its own spikes. ⚠️ The cross-view correctness check (Infill's highlighted count vs Glass's lit count) died with the second copy and was replaced by a stronger one: the rendered count is now checked against a count re-derived from the **raw `dist_lrt_m` column** at a hardcoded 600 m — the render measured against the file, not against the rule that drew it. Rejected: keeping both (the status quo Peter rejected); hiding the rows but leaving the dim code (dormant code that only runs when a gate regresses is how the gate regresses silently). | `docs/UI.md` "Amenity bands" § Removed from Glass, `docs/SPEC_development.md` "Amenity distance", `docs/CONTROLS_MATRIX.md` §4 |
| 2026-08-26 | **The change lens EXTRUDES — gains rise, losses sink below the plane — reversing the 2026-07-30 "flat choropleth, never spikes" call.** Peter, pointing at the deviation lens: *"like how positives go up, but negatives actually go down before the floor… the lens is organized by since 2012/2019, so that should already provide a natural 0."* The original decision rested on two reasons and only one survived. ⚠️ **"A prism cannot have negative height" was FALSE** and had already been retracted on 2026-08-11 (`probe-negative-elevation.html`: deck.gl 9.0.38 renders a negative `getElevation` below the ground plane, and the deviation lens ships on it) — the code comment had recorded the retraction while the render kept the conclusion, which is how a lens ends up flat for a reason nobody holds. The second reason — *extruding a percentage reads as magnitude when the metric is deliberately size-independent* — is **still true and is now deliberately accepted**, because the window picker supplies the explicit zero the reader needs and the sign is the whole point of the lens; the blurb carries the cost with a ⚠️ that height is a RATE, and that the tallest prisms are small new subdivisions off a near-empty baseline rather than the biggest tax bases. ⚠️ **ONE shared elevation scale across both arms** (`CHG_ELEV_SCALE = 9000`, fixed, NOT per-window and NOT per-arm) — the deviation lens's rule and its reason: per-arm scales would draw a −5%/yr loss and a +34%/yr gain as visibly equal bars. Colour keeps its per-arm p95 clamps, which is a different question — a ramp has two ends to spend, a height has one origin. Measured before choosing the constant: the fastest hood in either window is +51.6%/yr and the deepest −22.3%/yr, so the down arm renders at **35–43%** of the tallest and is legible without help. `verify-change.js` asserts metres-per-point are EQUAL on both arms and that the rendered deepest/tallest ratio matches the raw file's, because the way this regresses is someone "fixing" the asymmetry. The 46 degenerate hoods (no baseline / no endpoint) are **flat at 0**, not absent — a hole in the height would read as "did not move", the one reading the colour scale already refuses for them. Also dropped: the `hood-hover` layer (the prisms are pickable, and a flat `depthTest:false` target would steal every pick). | `docs/SPEC_temporal.md` §6b, `docs/CONTROLS_MATRIX.md` §4, `docs/UI.md` |
| 2026-08-26 | **A guard that fails BY DESIGN is wired into no workflow, and says so where it is defined.** `check_temporal_archive_year.py` exits 3 on today's archive because the defect it detects is real and unresolved (`DATA_ISSUES.md` §2). Gating the weekly publish on it would hold the live site over a decision, and **warn-only was rejected for the reason the 2026-08-25 row already gives** — a warning inside a green run reaches nobody, which is the same channel that hid the stale roll for months. So it ships as a standalone check whose docstring states that it fails deliberately and that wiring it is part of closing the decision. ⚠️ **The alternative considered and left open for Peter: the MONTHLY VINTAGE DIGEST**, which already holds `issues: write` and already opens a `⚠️`-titled GitHub issue — the one channel in this repo that actually reaches a human on a schedule. That is the honest home for a guard that cannot gate a publish but must not go quiet. ⚠️ **A year outside `fir_tax_base.json`'s range is reported as NOT CHECKED and named, never counted as passing**: an unverifiable year silently reading as verified is the same failure shape as the one being detected. | `scripts/check_temporal_archive_year.py`, `docs/DATA_ISSUES.md` §2 |
| 2026-08-26 | **Defects in data we do NOT control get their own register, and its load-bearing column is whether anyone has been told.** `docs/DATA_ISSUES.md` — distinct from `data/DATA.md` (what a source *is*), `TODO.md` (our build work) and `AUDIT_LEDGER.md` (audits of our own pipeline). ⚠️ **The failure it exists to prevent is documented rather than assumed**: the `qi6a-xuwt` dropout was measured 2026-07-31 and by 2026-08-06 had been archived UNSENT as a sub-item of a closed parent, invisible for six days. Status is therefore a closed vocabulary (**NOT SENT · SENT · ACKNOWLEDGED · FIXED · WONTFIX**) and the file states outright that **"the artifact exists" is not sent** — evidence, report text and send status are tracked as **three separate states**, because collapsing them is precisely how an unsent finding reads as handled. A "Possible issues" section holds candidates that are real enough to record but not measured enough to send; promotion requires an artifact that reproduces the defect. ⚠️ **Identifier churn is explicitly EXCLUDED with its reason**, so a future sweep does not re-add it as a discovery: renumbering runs 0.15%–0.37%/yr and `data/DATA.md` states a vanished account number is not by itself a finding. `TODO.md` mirrors the status one line per issue and names this file authoritative in both directions. | `docs/DATA_ISSUES.md`, `CLAUDE.md` |
| 2026-08-27 | **The temporal archive's mislabelled `2025` entry is DELETED, not relabelled or rewritten — and the real 2025 is accepted as unrecoverable.** The entry was the 2026 roll captured under a stale pin (`DATA_ISSUES.md` §2). Relabelling to 2026 was rejected because a correct `2026` entry already existed: 342 of 406 hoods byte-identical, citywide totals **+0.0021%** apart, so the two are the same roll four weeks apart and the later capture is the marginally better copy. ⚠️ **Deleting does NOT restore 2025, and that consequence was measured before the delete, not discovered after** — 2025 is in `HISTORICAL_DEFECT_YEARS`, so `publishable_years()` omits it rather than falling back to the historical file, and the published series becomes **2012–2023 + 2026** with a two-year hole. That is not a new decision: the historical 2025 slice carries the *same* cumulative 2,448-account / $2.93B defect (53% Downtown) on which §0.2 already omitted 2024, so publishing it would have contradicted a locked call on identical evidence. ⚠️ **`CHG_WINDOW_LABEL` stays HARDCODED** (`2012–2026`) rather than reading the last year from the data — a self-deriving label would have silently renamed the phantom year instead of exposing it, which is how the defect survived a month. The tooltip's `(2024 n/a)` **was** a literal and is now derived; it understated a two-year hole with every check green, while the panel note beside it had been derived since 2026-08-09 for exactly this reason. `expected_temporal_years.json`'s 2025 anchor removed with an in-file prohibition on re-pinning it from the archive ($237.2B–$239.6B against a true $220.07B). ⚠️ **The `x is year-scaled` ratio-3.01 failure carried as a suspected rendering defect was NOT one**: with the phantom present the detached run held two points and stroked as a path, so the measurement's `g circle`[0] silently fell through to the live-year marker — the renderer was correct throughout and the *measurement* had lost its subject. **General rule this pays for: a safety mechanism whose input is unverified does not merely fail, it consumes the one opportunity it existed to protect.** | `docs/DATA_ISSUES.md` §2, `docs/SPEC_temporal.md` §0.4 |
| 2026-08-27 | **The archive-year guard is wired into the digest BY MEMBERSHIP of `vintage_report.CHECKS`, not as a workflow step — and the stale-metadata downgrade is now enforced for every caller, not just the guard that owns it.** Executes the 2026-08-26 row's open option (the digest as the honest home for a guard that cannot gate a publish). ⚠️ **No CI change was needed and none was made** — `vintage-digest.yml` already runs `vintage_report.py` and already folds `⚠️` into the issue title, so a workflow step would have added a failure surface for nothing; the consequence is that **membership in `CHECKS` IS the wiring**, so dropping a name is silent and a test asserts both archive checks are present. Reuses `filed_bases()`/`detect_year()`/`archived_residential_bases()` rather than reimplementing, so the digest and the standalone guard cannot disagree about what a mislabelled year is; reads only committed files, so unlike its neighbours it cannot fail on the network. Two reporting choices locked: a year outside `fir_tax_base.json` is named **NOT CHECKED and never counted as passing** (an unverifiable year reading as verified is the defect's own shape), and **a green over ONE archived year carries its own thin-population caveat** in the row text — after the 2026-08-27 deletion it checks exactly one year, and a bare ✅ reads stronger than that supports. ⚠️ **The sibling `check_temporal_archive` was GREEN THROUGHOUT the defect this catches** — it confirms the live year was CAPTURED, and on 2026-07-28 it was; the capture was simply the wrong year. **Presence is not correctness, and two checks are needed because those are two questions.** ⚠️ **Second half, and it is not a new decision but an old one finally enforced:** `check_assessment_roll` compared the coverage string to our pin *itself*, bypassing `check_alignment()`'s stale-metadata branch locked 2026-08-25 — so with Edmonton's field a year stale and our pin correct it would have reported "Roll has moved to 2025, pin is still 2026" and sent Peter to the year-roll runbook **every month from 2026-09-01**, in the only channel that reaches a human. Now routed through `check_alignment()`, reported UNKNOWN, with the FIR guard named as the authority. **A duplicated comparison is a bypassed decision: the downgrade lived in the function, and the second caller never got it.** | `docs/RUNBOOK.md` §0, `scripts/vintage_report.py`, `docs/DATA_ISSUES.md` issue 1 |
| 2026-08-28 | **BOTH builds carry a work-in-progress badge, and their labels differ — the public one reads `Beta build`, the specialist one gains `(beta)`.** Peter: *"we have a nice banner on the bottom… I actually want that for the public one, but beta build label instead. Although you should add beta to the specialist build label as well."* Until now the badge was full-only, sized as the mitigation for `/full/` being discoverable-but-unlisted (2026-08-04 row); the public build — the advertised URL — carried no on-page statement of its own maturity at all. ⚠️ **The two labels must stay distinct, and a test pins it**: the badge is the reader's only on-page cue to *which* build they are in, so a public page announcing the specialist build would tell every reader they are somewhere they are not. Implementation is a parameterization, not a new mechanism — `WIP_BADGE` became `BADGE_LABELS` + `wip_badge(label)`, and the last-`</body>` injection the 2026-08-04 row locked is now a shared `inject_badge()` used by both builds rather than duplicated. Verified in a real browser, not only by unit test: both badges render `visible: true` and neither overlaps the legend (bottom-left) or the MapLibre attribution (bottom-right). | `scripts/build_site.py`, `tests/test_build_site.py`, `docs/SPEC_deployment.md` |
| 2026-08-28 | **`deploy.yml`'s path filter lists `scripts/build_site.py` — the trigger rule is "what changes the SERVED ARTIFACT", not "what lives under `web/`".** ⚠️ **Found by verifying a merge, not by a red**: the beta-badge change (same-day row above) merged, passed 745 tests, reached `master` — and deployed **nothing**. `deploy.yml` was filtered on `web/**` alone, so the live site kept serving the OLD shaping, and would have until an unrelated `web/**` push or the weekly refresh happened to rebuild it. Silent, green and stale, with no red anywhere. `build_site.py` is the one file outside `web/` that shapes the artifact (the two builds, the `<base>` tag, the badges), so the old comment's claim that *"non-web changes don't alter the served artifact"* was true of `docs/`/`src/` and false of it. ⚠️ **The test was falsified against the pre-fix workflow first** — it fails there and passes after, so it pins the defect rather than the behaviour. The stranded deploy was shipped by `workflow_dispatch` and both badges confirmed live. | `.github/workflows/deploy.yml`, `tests/test_build_site.py` |
| 2026-08-28 | **The proxy-reading class is a real class, its tiers rank by REVERSIBILITY rather than by correctness, and replacing a proxy with a measurement does not by itself close it.** First sweep as a set (`docs/FABLE_AUDIT_proxy_guards.md`, reusable; findings in `docs/FINDINGS_proxy_guards.md`) of the five instances that were each found by accident. ⚠️ **The hinge fact, and the reason the S119 remedy did not finish the job: a check is only as good as its CANDIDATE SET, and the candidate set is often controlled by the same party whose claim is under test.** `check_roll_year_against_fir.py` genuinely measures parcels instead of reading Edmonton's metadata — but it can only return a year **Alberta has FILED**, and Alberta files months after Edmonton rolls, so it is blind in exactly the window the roll moves. ⚠️ **Consequently the archive freeze is still exposed** (F1, reproduced by calling `write_archive` twice: the freeze protects *other* years while the **pinned** year is reassigned every run, so a stale pin overwrites a correct archived year with the new roll). **The design error is that an irreversible step shares a gate with recomputable ones** — "inconclusive → proceed" is right for regenerating `web/data` and wrong for a permanent capture. ⚠️ **Also established: nothing runs the test suite on a change** (F3 — `pytest` only in the weekly cron, no `pull_request` workflow, `master` unprotected), so a green suite in a PR body is a claim about the author's machine, which is the same substitution one level up. **All three remedies are PROPOSED, NOT BUILT** — they change a data contract, CI behaviour and an output schema respectively, and CLAUDE.md requires proposing each first. **Recorded as cleared so it is not re-litigated:** platform-emitted metadata (`rowsUpdatedAt`) is NOT the same thing as hand-typed prose (`Period of Coverage`), and `check_capital_budget` — which refuses `Last-Modified` and content-hashes the body — is the model implementation for the class. | `docs/FABLE_AUDIT_proxy_guards.md`, `docs/FINDINGS_proxy_guards.md`, `docs/AUDIT_LEDGER.md` 2026-08-28, `TODO.md` (3 items) |
| 2026-08-28 | **An unproven archive capture is WRITTEN but can never overwrite a proven one — and the merge gate exists.** Executes audit F1 and F3 (`docs/FINDINGS_proxy_guards.md`) on Peter's *"do f1 and f3"*. ⚠️ **F1 shipped a DIFFERENT remedy than the audit proposed, and writing the proposal out is what exposed it.** "Require a positively confirmed year and SKIP on inconclusive" would have left the archive empty through the months-long FIR lag — Alberta files long after Edmonton rolls — so a January where Alberta ran late would cost a year outright, **reintroducing from the other side the exact loss the archive exists to prevent**. The two things conflated were **the data and the label**: the data is irreplaceable, only the label was ever wrong. So `write_archive(..., confirmed=)` always writes, records whether the capture was *measured* to be its filed year, and **refuses only to overwrite an entry already recorded as confirmed** — unproven ≠ wrong, and an unproven entry is upgraded in place the moment FIR lands. The caller measures the **frame about to be written** and **reuses `detect_year`** rather than comparing locally (the S122 rule: a duplicated comparison is a bypassed decision); `_year_confirmed` was backfilled from `check_temporal_archive_year.py`'s own re-derived verdict, never asserted. ⚠️ **The regression test was falsified against the old logic on the DEFECT rather than the signature** — the pre-fix function was given the new keyword and left otherwise untouched, and fails on having archived where it should have refused. ⚠️ **Verified on the real roll** (`capture measures as 2026, residual +1.17%`), which also caught that a local `--write-archive` run re-captures from this box's older CSV and would commit a DOWNGRADED archive. F3: `.github/workflows/tests.yml` runs pytest + the citation guard on `pull_request` and `push: master`, **offline and secret-free on purpose** (a gate that reddens on an upstream outage is one people learn to ignore), and **`refresh.yml`'s own pytest step stays** — that gates the weekly data publish, this gates the change; a test pins both, because the tempting cleanup is to drop the "duplicate". ⚠️ **Branch protection remains OFF and only Peter can change it**, so the gate reports without blocking. | `src/load_temporal.py`, `scripts/check_temporal_years.py`, `.github/workflows/tests.yml`, `tests/test_ci_workflows.py`, `tests/test_load_temporal.py` (+3), `data/temporal_archive.json`, `docs/SPEC_temporal.md` §0 |
| 2026-08-28 | **The loading overlay lifts on the first PAINTED FRAME after the deck layers attach, not on a second map `idle`; the min-display floor is a remainder computed at gate time, not a raced promise; and the justification originally committed for both was measurement error.** Peter asked for an overlay covering initial render pop-in, specified against a MapLibre + deck.gl + **pmtiles-from-R2** stack. ⚠️ **This frontend has no basemap at all** — the style declares `sources: {}` and one background layer, so there are no tiles, no pmtiles and no R2; everything is local GeoJSON. The `idle`-over-`load` reasoning in the brief was sound and its stated cause (progressive tile streaming) does not exist here, so the basemap gate is kept for a different reason: it is the only signal MapLibre came up at all, and it resolves in ~50ms, which also means **the `Loading basemap…` label is never actually read by a human**. The data gate is the map's own `render` after `addControl`, because interleaved deck draws inside MapLibre's render pass; the next `idle` waits on the lazily-fetched secondary layers (reference, temporal, roads) that the load handler deliberately does not await, so gating on it covers a map that already looks right. ⚠️ **The 5-second figures committed with the overlay (#267) were WRONG and are retracted** — they were measured with leftover Chromium processes competing for this 4-core box, the `run-verify-scripts-alone` failure mode applied to *measurements* rather than to pass/fail. Clean re-measurement, three runs each on an idle box: **shipped 549–568ms, idle-gated 1045–1091ms, raced-500ms-floor 1049–1132ms**. The decision stands on ~500ms, not ~5s. ⚠️ **The floor is 300ms and applied as a remainder** because a raced `setTimeout` is starved by the post-paint JSON parsing — the anti-flash floor becomes the dominant delay. ⚠️ **`verify-loading-overlay.js` asserts the MECHANISM, not a clock reading**: a millisecond ceiling cannot separate 568ms from 1045ms without flaking under load, but an idle-gated lift lands **within a millisecond of an idle event** and a render-gated one does not — that holds on any machine. **Falsified against both defects**; an earlier version compared against "the last idle observed" and would have passed an idle-gated build as soon as any later idle appeared. ⚠️ **Its first version went green while testing nothing** (the idle array was empty, because `window.map` is the `<div id="map">` that the id-global rule puts on window, while the MapLibre instance is a top-level `const` visible only to a bare identifier) — a vacuity guard now fails loudly rather than letting the real check pass on an empty set. Failure path: a non-ok `DATA_URL` fails immediately (otherwise a 404 serving an HTML error page surfaces only as a JSON parse error after the app is committed) and a 15s timeout catches a hang; both offer a reload, since there is no partially-built app to retry into. | `web/index.html` (`--- loading overlay ---`), `web/styles.css`, `tools/profiling/verify-loading-overlay.js`, `docs/ARCHITECTURE.md` Testing |
| 2026-08-28 | **The loading screen carries a title and ONE line, sized to a measured ~900ms dwell — and its wrap behaviour is explicitly NOT verifiable on this box.** Peter: *"the loading was feeling a bit long, but in hindsight only time to glance at one or two things."* The overlay is up ~560ms and fully faded by ~900ms, so title plus one line is the ceiling rather than a compromise. Wording is the page's **own `<h1>`** — "Edmonton Tax Viz" is the repo name, and using it here would give the project a second name for the same thing — and the blurb is the first clause of `#title-p`, which runs six lines. Rendered as a `<p>`, **not** an `<h1>`: the page already has one and a second competes in the accessibility tree. The status text drops to the smallest, dimmest element (a debugging aid, not the message) and takes the foreground back in the error state, where it *is* the message. ⚠️ **This box has three font families (Cantarell, DejaVu Sans Mono, Fixed) and none of the CSS stack, so `sans-serif` falls back to MONO and every width measured here reads ~15–20% long.** A first pass tuned `max-width` against that — i.e. against a font no reader will see — and was backed out. `text-wrap: balance` is the response instead: it evens a wrap wherever it lands rather than orphaning the last word, and degrades to normal wrapping where unsupported. `max-width` caps the measure and the comment says so rather than promising one line. **Real-device wrap remains unconfirmed** (`docs/MOBILE_USABILITY.md` §2b). | `web/index.html`, `web/styles.css`, `docs/MOBILE_USABILITY.md` §2b |
| 2026-08-28 | **The loading screen's title is GENERAL — `Edmonton: Property Tax Visualization` — deliberately reversing the same-day row above that pinned it to the page's own `<h1>`.** Peter: *"change the loading screen text to 'Edmonton: Property Tax Visualization'… the big words to be more general."* The earlier rule was *"use the page's `<h1>`, not a second name for the same thing"*, and it was wrong for one reason not considered then: **the `<h1>` names the DEFAULT LENS, not the app.** `Edmonton: Tax Revenue per Acre` describes the landing view, while the overlay covers a build that also carries the development, services, ratio, uses and temporal lenses — so a cover naming one lens misstates what is loading. ⚠️ **The blurb stays specific** (`Municipal property-tax revenue per acre, by neighbourhood.`) and that is what makes the general title safe: the title names the app, the blurb says what you are about to see. **Not a second name for the project** — no new branding, and it is still not the repo name. ⚠️ **The title is 36 chars against the previous 30 (~20% longer), which re-opens the phone wrap question `MOBILE_USABILITY.md` §2 had just closed** — Peter confirmed the 30-char title on a real device, and this string is not that string. Unmeasurable here (three font families, none of the CSS stack); `text-wrap: balance` is why two lines would be even rather than orphaned. Headless: one line at 1280, two at 390 in the mono fallback, no overflow at either width. | `web/index.html` (`#loading-title`), `docs/MOBILE_USABILITY.md` §2 |
| 2026-08-29 | **A guard's tolerance is DERIVED from the served precision, not chosen — and one of the three "verify scripts failing on master" was never failing on master.** Closes the two backlog items carried since 2026-08-11/16, both reproduced before being touched. ⚠️ **`verify-temporal.js` does NOT fail on unmodified `master`: 6/6 PASS on an idle box, and under deliberate contention (12 concurrent chromiums) it fails on the EXACT selectors the old item named**, both `Timeout 4000ms exceeded`. That is the already-documented run-alone condition, so **the 4000ms budget was deliberately NOT raised** — nothing is wrong with it under the protocol the project already requires, and raising it would have bought silence for a condition that is forbidden anyway. The 2026-08-16 measurement of "4 failures in 4 sequential runs" was taken on a box that was not actually idle; **the same contention artifact that corrupted a timing measurement in S124 also manufactured a backlog item.** ⚠️ **`verify-revenue-panel.js`'s stale diagnosis hid the real finding**: it recorded 3 failures with one cause, there were **6 with two**, and the new three exposed that **`rev_frac_*` is two OVERLAPPING dimensions, not one partition** — `rev_frac_exempt` is a cross-cutting flag (an exempt institutional parcel counts in `rev_frac_inst` AND `rev_frac_exempt`), so the test's "every `rev_frac_*` > 0" ground truth summed to 1.054081 for DOWNTOWN, the excess being exactly its 0.054082 exempt share. **The app was correct throughout** — `REV_CATEGORIES` excludes exempt by construction and the note discloses it separately — so the fix is to the test, and **a new check asserts exempt IS cross-cutting** so narrowing the ground truth did not silently drop coverage. ⚠️ **`verify-nonres-revenue.js`'s epsilon is now COMPUTED, not picked.** `1 + 1e-9` predated the 6-significant-figure export decision (2026-08-09 row) and failed on 127 of 406 hoods. **The invariant was checked against what the rounding can produce BEFORE the tolerance was touched**, which is what the old item demanded: across all 406 hoods every breach sits inside the half-ULP bound of the three independently-rounded values and **none exceeds it** (worst WÎHKWÊNTÔWIN, 0.4 against 0.6), so the decomposition is exact upstream and only the write-time rounding breaks it. The test now derives that bound per hood. **Falsified against injected breaches — 0.5× the bound passes, 2× and 10× fail** — so it discriminates at the rounding boundary rather than being widened into uselessness. **The general rule: when a guard's tolerance must change, derive it from the locked decision that created the slack and falsify it against a real breach; a number chosen to make a red go away is the thing this project's rule forbids.** | `tools/profiling/verify-nonres-revenue.js`, `tools/profiling/verify-revenue-panel.js`, `TODO.md`, `docs/TODO_archive.md` |
| 2026-08-29 | **An evidence notebook for an ABSENCE is built the opposite way round: it runs the searches that would DISPROVE its own headline, and its invariants FAIL when the publisher fixes the gap.** Issue 5 (`DATA_ISSUES.md`) was the last of the four sendable issues without published evidence; `notebooks/standalone/school_coverage_gap.py` closes it, so **every remaining blocker on all four is report TEXT, not measurement**. ⚠️ **The other three notebooks demonstrate a wrong value, which can be shown directly; this one asserts that a dataset does not exist, and no query returns that.** So the argument is: state what the missing dataset would look like, run the catalogue searches that would find it (`private school`, `charter school`, `independent school`, `francophone`, `Centre-Nord`), and show they return no school point set for any missing operator — plus classify the entire broad `school` result set by operator, so "only two boards" is checked against every result rather than asserted. ⚠️ **The epistemic limit is stated in the notebook rather than left implicit**: it establishes only that the portal exposed no such dataset on the run date, not that the City holds none internally. ⚠️ **Two invariants are written to FAIL the moment the missing schools are published** — that is the outcome the report asks for, and a report that keeps agreeing with itself after being acted on is a report nobody will notice has succeeded. Also recorded because it cost a run: the roll's land-use check matched `house_number` on a substring test for `use`, so the check now matches on WORD BOUNDARIES — **an invariant that fails for a reason unrelated to the claim is worse than no invariant**, and the first execution failed exactly that way. The workaround section stands on the live schema (the current roll has **no** land-use or occupancy field at all) rather than on the string search `ANALYSIS_BACKLOG.md` §13 recorded, which targeted a different dataset. | `notebooks/standalone/school_coverage_gap.py`, `web/notebooks/school-coverage-gap.html`, `docs/DATA_ISSUES.md` §5 |
| 2026-08-29 | **The evidence notebooks are SNAPSHOTS with a lifecycle, not living documents — and a report that gets FIXED stays published, with its invariants failing.** Peter, rejecting a proposed monthly re-run in the vintage digest: *"each report mostly matters as a snapshot in time for me. I'd either close it if they fix it, if we detect it morphed we just change it, and if it gets killed and replaced by a whole other type of bug we just archive it and make a new report."* ⚠️ **The digest proposal was dropped as the wrong solution** — it was continuous re-validation for artifacts whose value is being point-in-time. (It was cheap and would have worked: deps already in `requirements-ci.txt`, ~10s per notebook, no workflow edit needed since membership in `vintage_report.CHECKS` IS the wiring. Rejected on purpose, not on cost.) ⚠️ **Building the snapshot model exposed that TWO OF THE FOUR PUBLISHED PAGES CARRIED NO DATE AT ALL** — `historical-2024-gap.html` and `exemption-uncertainty.html` had none anywhere in the rendered HTML, and `roll-year-metadata.html` carried one only incidentally that DISAGREED with the index (page 2026-08-24, index 2026-08-26). **The published page is the artifact handed to someone, usually without the index that would otherwise date it**, so all four now stamp themselves with BOTH `FIRST_MEASURED` (the finding's date, never changes on re-run) and the re-execution date — *"first found on"* and *"still true today"* are different claims. The stamp is self-contained with aliased local imports because `exemption_uncertainty` imports `IPython.display` INSIDE a function, so a module-level stamp relying on the file's own imports NameErrors; a first attempt did exactly that and was reverted. **Status vocabulary: ACTIVE / RESOLVED / AMENDED / SUPERSEDED.** ⚠️ **On RESOLVED the report STAYS PUBLISHED and its invariants FAIL — both deliberately.** It becomes the record that the defect was found, reported and fixed, which is the only durable evidence the work mattered; and several invariants are written to fail on the fix, so "repairing" them destroys the signal. **SUPERSEDED is archived under `notebooks/standalone/archive/` + `web/notebooks/archive/` with a pointer to the replacement — archive, never delete**, the `TODO_archive.md` rule: a public URL that 404s is worse than one that explains itself, and outreach may already have cited it. ⚠️ **Only the fix transition is automatically detectable** (invariants flip); morph and replacement are human judgement triggered by main-project work, which is what `EVIDENCE_NOTEBOOKS.md`'s per-source table is for. All four re-executed 2026-08-29, every invariant still passing — nothing overtaken. | `docs/EVIDENCE_NOTEBOOKS.md`, `notebooks/standalone/*.py`, `web/notebooks/index.html` |
| 2026-08-29 | **Map-state transitions get a rule set before the first one ships: animation implies a relationship, so it is allowed only where that relationship is real — and the DEFAULT for a new metric is cross-fade, not tween.** Peter, via advice relayed from Claude web, asked for a reference doc for future metric/layer work. Grounded on Heer & Robertson (*Animated Transitions in Statistical Data Graphics*, IEEE InfoVis 2007): object constancy is worth preserving where identity persists, and motion across a semantic discontinuity asserts a continuity that does not exist — the same failure the project already refuses for unnamed denominators and for the institutional band. The operative test is the **halfway frame** (pause midway: does that state mean anything real?). ⚠️ **The relayed rule set's own flagship example was the one pair this repo cannot draw** — "tax revenue 2023 → 2024" — because 2024 AND 2025 are omitted by decision and `DECISIONS.md` 2026-07-29 locks the invariant that 2024 must LOOK absent; a year tween would animate two full years of smooth motion across data that does not exist, destroying in the map exactly what the sparkline is built to preserve. **Step across the gap, never tween across it.** ⚠️ **Its "noise complaints / population density" examples are not our metrics** (every repo hit is unrelated — statistical noise, and "population" as the z-scoring set), so the rules are restated against the real inventory. **Rule 3's "same unit, different metric" gray area RESOLVES for the three revenue metrics**: they share `elevationScale: 0.033` deliberately so the bars read as comparable subsets, and residential/non-residential are locked decompositions of the levy (2026-07-16, 2026-08-01) — a subset↔superset relationship is real, so they tween. `value_per_acre` is dollars too and does NOT: assessed **stock** vs annual **levy flow**, on a 55× different scale. The denominator toggle is left as the genuine open gray area (perfect object constancy, but the halfway frame is a height against no denominator that exists). ⚠️ **MEASURED, and it constrains the rules: height transitions are NOT currently available at all.** `elevationScale` is a layer *uniform*, and NO numeric uniform prop opts into transitions in this build — the bundle carries `uniformTransitions` machinery, but every `transition: true` in it sits on an accessor-backed attribute, so neither `elevationScale` nor **`opacity`** can be interpolated (which is why a cross-fade must be DRIVEN rather than declared) — and `top-edges` is a `PathLayer` whose *data* is rebuilt by `topRings()` at the new heights, so roof outlines would snap while prisms still moved. Colour is nearly free by contrast — `metric-extrusion` holds a stable layer id across every Money swap and already declares the `updateTriggers`. **Nothing is implemented**; the map still hard-cuts. | `docs/TRANSITIONS.md` |
| 2026-08-30 | **An identifier may ACQUIT a roll-continuity candidate even though it must never MATCH one — the asymmetry is the licence.** Amends 2026-08-07 (*roll-continuity is measured by POSITION, never by an identifier*), which stands as written for matching. Matching **on** an identifier creates false negatives, because account numbers, addresses and hood names all churn; using one only to **acquit** can only remove false positives, since a parcel whose account is on the roll is on the roll whatever its coordinates say. Forced by measurement: **121 of 1,578 position-unmatched parcels ($592M, 35.6% of the value at risk) never left the roll** — they were recentroided past the 5 m tolerance. ⚠️ **The remedy is NOT a wider tolerance**: the <2 m drift the 5 m figure came from is a four-hospital sample, the acquitted moved a **median 58 m (max 559 m)**, and a tolerance wide enough to catch them starts matching the neighbouring parcel — trading a visible false positive for a silent false negative. Verdict restated at **1,457 / $1.07B**, and the three largest cases the backlog had named for three weeks were all on the roll the whole time. | `tools/audit_roll_continuity.py` (module docstring — the asymmetry, the displacement measurements, why not to widen the tolerance), `TODO.md` (roll-dropout item), `docs/AUDIT_LEDGER.md` 2026-08-30 (S126) |
| 2026-08-30 | **The temporal commercial cut stays CLASS-1-ONLY — a spliced series must measure the same thing on both halves, even when one half could be measured better.** From the T8 sweep. `COMMERCIAL_CLASSES` tests only the first assessment class, but a parcel carries up to three: **1,094 parcels / $9.06B** hold a class 2, so 439 parcels ($3.37B) count as wholly commercial when they are not and 127 ($492M) with a commercial component count as zero. **Citywide the error nearly cancels (−0.50%, 22.10% vs 22.21% apportioned); per hood it does not — max 6.5 pp, 23 hoods >1 pp, 3 >5 pp.** ⚠️ **Rejected: apportioning the live half by the published class percentages.** `qi6a-xuwt` publishes `mill_class_1` / `tax_class_pct_1` and **no class 2 or 3**, so the archive half cannot be apportioned at all; fixing only the live half would make the two halves of one line chart mean different things — a worse error than the bounded one it removes. Accepted instead: **state the limit and its per-hood cost**. ⚠️ Also logged from the same run: `other` is purely `DEFAULT_CATEGORY` (no zone code maps to it) yet `NONRES_CATEGORIES` counts it as non-residential — inert while nothing is unmatched, and `_categorize` only warns, so **decide it before the next zoning-bylaw update, not after**. | `docs/SPEC_temporal.md` §3 (the cut + why not to fix it), `docs/AUDIT_LEDGER.md` 2026-08-30, `docs/DATA_INTEGRITY.md` T8, `TODO.md` |
| 2026-08-30 | **Copy that restates a pinned constant is a scheduled defect, and the fix is one constant plus a guard — not a data-derived label.** Closes audit F4. `FIRE_YEARS` / `PERMIT_YEARS` / `PERMIT_YEARS_RECENT` are pinned in `main.py` and bumped by hand each January so a partial year is never averaged or summed in; ~15 user-facing sites in `web/index.html` restated the same ranges as literals, so that bump rolled the NUMBERS and left the LABELS naming the old years. ⚠️ **Nothing could have caught it**: the drift guards in `load_fire`/`load_permits` and `vintage_report.check_window_pins` all watch the pin, and RUNBOOK §1 step 4 asserted a stale pin "can't be missed silently" — true of the pin, false of all 15 strings. Same shape as the `(2024 n/a)` defect (S122) at 15×, with the same tell: correctly-derived copy sat beside it (the vintage footer reads `status.json`). ⚠️ **Rejected: publishing the windows in `status.json`.** It is the purer architecture but a worse fix — the manifest is fetched async and lands after first render, so every label would need a literal fallback anyway and there would be **two** sources of truth instead of one. Accepted: a single `WINDOWS` block, `${WIN.<key>}` in JS and `{{<key>}}` placeholders in static tooltips, with `tests/test_window_labels.py` asserting it equals the pins. ⚠️ **The change-lens ends stay PINNED, not read off `temporal.json`** — that was locked when the endpoint moved 2025→2026 and a self-reading label would have silently renamed a phantom year; `WINDOWS` is a hand-edited pin checked against `main.py`, so the protection is unchanged. ⚠️ **The guard gates the weekly refresh and that is deliberate**: unlike the CODEMAP staleness test rejected 2026-07-30, nothing the refresh does can turn it red — both sides are hand-edited constants, so it fails only inside a PR that bumped one and not the other. Verified by mutation (a stale pin and a reintroduced literal each fail it) and against the live page: all six ranges render byte-identical to before, no unsubstituted token, no page error. | `web/index.html` (`WINDOWS`/`WIN`), `tests/test_window_labels.py`, `docs/RUNBOOK.md` §1 step 4 |
| 2026-08-31 | **An aggregate that nothing consumes is deleted, not corrected — and the contradiction it carried goes with it.** Closes the T8 latent follow-up. `NONRES_CATEGORIES` summed the unmatched `other` bucket into `frac_nonres`, so unclassified land was asserted as non-residential, contradicting `DEFAULT_CATEGORY`'s own comment (*"does NOT claim it as residential or any specific non-residential use"*). ⚠️ **It was not an oversight** — `data/DATA.md` §5 records that before the 2026-07-03 nonres split unknown codes defaulted straight into `nonres`, and folding `other` back in is exactly what kept `frac_nonres` meaning what it used to. So the question was whether that continuity is still worth paying for. It is not: **`frac_nonres` never reached the served GeoJSON and was read by nothing** — two test assertions and three doc lines, no consumer in `src/`, `scripts/` or `web/`. Deleted rather than adjusted, per the project's prefer-deletion rule; a regression test pins both halves (the column is gone, the unclassified share is still reported). ⚠️ **Measuring the exposure changed the fix, and the audit's framing was wider than the defect.** `AUDIT_LEDGER.md` 2026-08-30 filed this as a trap gated on the next zoning-bylaw update, when a new code "would silently become non-residential". The served path was already honest — an unmatched code lands in `frac_other`, which the map draws as **"Unclassified"** neutral grey (`web/index.html`, use-mix categories). What was genuinely weak is that `_categorize` only **WARNS**: in a weekly CI refresh a log line is effectively silent, so a new bylaw code would arrive unnoticed. ⚠️ **Reported, not enforced.** `vintage_report.check_unclassified_zoning` reads `frac_other` off the served file and files an ACTION row naming the worst hoods. Deliberately NOT a pipeline failure: unclassified land is drawn honestly, so a new zone code is a go-look signal, not a reason to stop publishing — the same proportionality test that kept the CODEMAP staleness check out of the refresh (2026-07-30) and that admitted the window-label guard (2026-08-30) on the opposite finding. Membership in `CHECKS` IS the wiring, so a registration test guards it. | `src/load_zoning.py` (`NONRES_CATEGORIES`/`frac_nonres` removed; `DEFAULT_CATEGORY` comment carries the do-not-reintroduce), `scripts/vintage_report.py`, `docs/RUNBOOK.md` §0, `data/DATA.md` §5, `docs/DATA_INTEGRITY.md` T8, `docs/ARCHITECTURE.md` |
