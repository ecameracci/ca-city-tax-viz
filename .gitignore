# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/

# Virtual environments
.venv/
venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# Rendered output of notebooks/verified/ (tools/run_verified_notebooks.py) — regenerated, not source
/_verified/

# Data files (scoped to raw inputs and outputs — track dirs via .gitkeep)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
output/*
!output/.gitkeep

# Environment variables
.env
.env.*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Profiling harness (tools/profiling) — Node deps, browser binaries, render artifacts
node_modules/
tools/profiling/*.png
# shot-mobile.js writes screenshots to CWD (repo root when run per the docs)
/mobile-default.png
/mobile-after-tap.png

# Claude Code personal settings (per-machine permission grants)
.claude/settings.local.json

# Standalone notebook download cache (~90 MB of public data, re-fetchable)
notebooks/standalone/exemption_data/

# Standalone notebook download cache (public data, re-fetchable)
notebooks/standalone/historical_gap_data/

# Standalone notebook download cache (two FIR workbooks, re-fetchable)
notebooks/standalone/roll_year_data/

# Local GitHub Pages build artifact
_site/
